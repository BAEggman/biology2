#!/usr/bin/env python3
"""s20p01a·s20p01b·s20p02 — 이미 그려져 있는데 pc 에만 있던 호르몬 카드를 행으로 내린다.

★ 오늘 세 번째로 같은 것을 찾았다
  s20p03(뿌리 굴중성) · s20p02(시계탑) 에 이어 s20p01b(멈추게 하는 셋)도
  네 행 전부에 카드가 하나도 안 걸린 채 pc 에만 일곱 장이 얹혀 있었다.
  ★ 새 그림을 그리기 전에 **덱을 먼저 뒤진다** — 이 세션에서 가장 값이 큰 습관이었다.
"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from pc_demote import demote
from link_cards import link

DEMOTE = {
    's20p01b': ['P1-6', 'P1-6#2', 'P1-7', 'P1-8', 'X-PL-27'],
    's20p02': ['P1-28'],
}

LINK = [
    ('s20p01b', '잎의 셔터를 끌어내려', ['P1-6', 'P1-6#2', 'X-PL-27'],
     '<b>ABA</b>가 하는 일은 넷이다 — <b>생장 억제 · 기공 닫기</b>(건조 스트레스) · '
     '<b>종자 휴면</b> · 건조 내성. ★ 넷이 한 방향이다 — <b>멈추고 닫고 참는다</b>. '
     '휴면을 <b>거는</b> 것이 ABA이고 <b>푸는</b> 것은 지베렐린과 브라시노스테로이드다(옆 판의 끌)'),

    ('s20p01b', '가스 맞은 열매만', ['P1-7'],
     '<b>에틸렌</b>이 하는 일 — <b>과일 성숙 · 낙엽(잎 탈리) · 삼중반응 · 노화 촉진</b>. '
     '★ 기체라서 <b>옆 열매까지</b> 익힌다 — 한 상자에서 하나가 익으면 나머지도 익는 까닭이다'),

    ('s20p01b', '부목 댄 줄기가', ['P1-8'],
     '<b>브라시노스테로이드</b> — <b>세포 신장·분열</b>과 <b>관속 분화</b>, 그리고 <b>종자 발아</b>. '
     '줄기가 굵어진 것이 신장과 분열이 함께 일어난 결과다'),

    ('s20p01a', '끌로 씨앗 껍질을', ['P1-8#3'],
     '<b>발아</b>를 밀어 주는 것은 <b>지베렐린</b>만이 아니다 — '
     '<b>브라시노스테로이드</b>도 종자 발아를 돕는다. 그 호르몬의 부목은 옆 판에 있다'),

    ('s20p02', '바늘이 아예 없고 모래시계만', ['P1-28'],
     '★ 개화를 실제로 정하는 것은 <b>낮 길이가 아니라 연속된 밤의 길이</b>다 — '
     '이 탑에 시계판이 없고 모래시계만 있는 것이 그 뜻이다'),

]

# ★ P1-8#3(브라시노스테로이드 → 종자 발아)은 폐기가 아니라 배열이다 —
#    브라시노 후크(부목)는 s20p01b 에, 발아(끌로 씨앗을 깬다)는 s20p01a 에 그려져 있다.
DROP = {}


def main():
    for pid, ids in DEMOTE.items():
        if demote(pid, set(ids)): return 1
    for pid, d in DROP.items():
        if demote(pid, set(d)): return 1
    if link(LINK): return 1
    print('\n판정 — 내림 %d장 · 폐기 %d장' % (sum(len(v) for v in DEMOTE.values()),
                                             sum(len(v) for v in DROP.values())))
    return 0


if __name__ == '__main__':
    sys.exit(main())
