#!/usr/bin/env python3
"""d05p02 「거리와 재조합빈도」 — d05 두 번째 도해.

왜 이 자리인가
  d05p01이 「배치가 배우자를 정한다」까지 갔고, 그 끝에서 부모형이 많고 재조합형이 적다는
  수의 불균형이 남았다. 그 불균형을 숫자로 바꾸는 것이 이 패널이다.

세 띠
  A 검정교배 자손 네 무리 — 부모형 410·405, 재조합형 90·95 → 185/1000 = 18.5 %
  B 초파리 지도 자        — 눈금이 곧 위치다. 거리는 빼고, 인접 구간은 더한다
  C 지도거리 vs 관측 RF   — 짧으면 자와 거의 같고, 멀어지면 벌어지다 50 %에서 천장에 붙는다

★ 자는 더하기로 늘어나는데 관측은 50 %에서 멈춘다 — 그 어긋남이 이 단원의 전부다.

⚠ 그린 것만 건다
  E0-56~68(다른 수치의 교차율 계산)은 걸지 않았다 — 그림에 있는 것은 410/405/90/95 한 벌뿐이다.
  계산법은 A띠 상자에 있으니 본문에서 그 한 줄로 푸는 법만 적었다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')
SVGF = os.path.join(ROOT, 'tools', '_d05p02.svg')

BR = ('★ <b>자는 더하기로 늘어나는데 관측은 50 %에서 멈춘다</b> — 그 어긋남이 이 단원의 전부다. '
      '검정교배에서 <b>많은 둘이 부모형, 적은 둘이 재조합형</b>이고, '
      '<b>재조합형 ÷ 전체 × 100</b>이 재조합빈도다. <b>1 % = 1 cM</b>이라 그 값이 곧 거리다. '
      '가까우면 자와 관측이 거의 같고, 멀어지면 관측이 자보다 작아지다가 <b>50 %를 못 넘는다</b>.')

BX = """<p><b>왜 부모형이 많은가.</b> 앞 패널에서 보았듯 배우자는 <b>막대를 통째로</b> 물려받는다. 그러니 막대에 원래 적혀 있던 조합이 그대로 나오는 것이 기본이고, 교차가 일어난 자리에서만 새 조합이 생긴다. 교차는 염색체 전체에서 드문드문 일어나므로 <b>부모형이 많고 재조합형이 적다</b>. 검정교배에서 네 무리 중 <b>가장 많은 둘이 부모형, 가장 적은 둘이 재조합형</b>인 것은 이 때문이다.</p><p><b>★ 계산의 첫걸음은 나누는 것이다.</b> 교차율 문제에서 가장 먼저 할 일은 계산이 아니라 <b>네 무리를 부모형과 재조합형으로 가르는 것</b>이다. 수만 보고 아무 둘이나 더하면 틀린다. 가른 뒤에는 한 줄이다 — <b>재조합형 수 ÷ 전체 수 × 100</b>. 그림의 예는 부모형 410과 405, 재조합형 90과 95이므로 (90+95) ÷ 1000 × 100 = <b>18.5 %</b>다. 다른 숫자가 나와도 하는 일은 똑같다.</p><p><b>1 % = 1 cM.</b> 재조합빈도 1 %를 지도거리 <b>1 map unit(centimorgan, cM)</b>으로 정한다. 그래서 18.5 %는 그대로 18.5 cM이다. 빈도가 거리의 <b>단위 그 자체</b>라는 것이 이 단원의 약속이고, 「재조합형 비율이 곧 상대적 거리의 근사값」이라는 말이 그 뜻이다.</p><p><b>자 읽는 법은 둘뿐이다.</b> ① <b>빼기</b> — 두 자리 사이 거리는 지도상 위치의 차다(67.0 − 48.5 = 18.5). ② <b>더하기</b> — 인접 구간을 이어 붙이면 멀리 떨어진 두 유전자 거리도 나온다(9.0 + 9.5 = 18.5). 두 방법의 답이 같다는 것이 그림의 아래 두 줄이다. 지도 자에는 왼쪽부터 짧은 더듬이(0), 고동색 눈(16.5), 검은 몸(48.5), 주홍색 눈(57.5), 흔적 날개(67.0), 아래로 휜 날개(75.5), 갈색 눈(104.5)이 찍혀 있다.</p><p><b>★ 그런데 자와 관측은 어긋난다.</b> 아래 띠의 점선은 「자 눈금이 그대로 관측될 때」이고, 실선은 <b>실제 관측</b>이다. 가까운 거리에서는 둘이 거의 겹친다 — 구간이 짧으면 교차가 많아야 한 번, 그래서 재는 대로 나온다. 그런데 멀어질수록 한 구간 안에서 <b>교차가 두 번</b> 일어나 원래대로 돌아가는 일이 생긴다. 두 번 일어난 교차는 겉으로 <b>부모형</b>으로 보여 재조합형에 세어지지 않는다. 그래서 관측값이 자보다 작아진다.</p><p><b>천장이 50 %인 이유.</b> 이 어긋남이 쌓이면 관측 재조합빈도는 <b>50 %에서 멈춘다</b>. 완전히 독립인 두 유전자를 검정교배하면 네 무리가 1:1:1:1이라 재조합형이 정확히 절반, 곧 50 %다. 연관이 아무리 느슨해도 그보다 더 섞일 수는 없다. 그래서 <b>재조합빈도의 이론적 최대값은 50 %</b>이고, 50 %에 가까우면 <b>독립유전처럼 행동한다</b>고 읽는다. 지도거리가 50 cM을 넘어가도 관측은 약 50 %에서 포화한다 — 그 너머의 거리는 한 번에 못 재고 <b>중간 유전자를 징검다리로 삼아 더해야</b> 알 수 있다.</p><p><b>0 %는 두 가지로 읽는다.</b> 아주 가깝게 연관되어 교차가 사실상 안 일어나거나, 자손 수가 적어 <b>교차를 관찰하지 못한</b> 경우다. 0이라고 해서 같은 자리라는 뜻은 아니다.</p><p><b>기출이 꼬는 자리.</b> ① 「가장 많은 두 무리가 재조합형이다」 → <b>거짓</b>(부모형이다). ② 「재조합빈도가 70 %인 두 유전자」 → <b>있을 수 없다</b>(최대 50 %). ③ 「재조합빈도 0 %면 같은 유전자다」 → <b>거짓</b>(아주 가깝거나 못 본 것이다). ④ 「지도거리 60 cM이면 관측 재조합빈도도 60 %다」 → <b>거짓</b>(약 50 %에서 포화한다).</p>"""

ROWS = [
    # ── A 띠
    ('A띠 — 막대 넷 중 앞의 둘이 높고(410·405) 뒤의 둘이 낮다(90·95)',
     '검정교배에서 <b>가장 많이 나오는 두 표현형이 부모형</b>, <b>가장 적게 나오는 두 표현형이 '
     '재조합형</b>이다',
     ['E0-26', 'E0-27']),

    ('앞의 둘이 높은 까닭 — 배우자는 막대를 통째로 물려받는다',
     '연관유전자들은 <b>부모 조합 그대로</b> 함께 배우자로 가기 쉽다. 교차는 드문드문 일어나므로 '
     '새 조합이 적다',
     ['E0-25']),

    ('막대 높이의 격차 자체가 판정이다 — 앞 둘이 압도적이냐, 뒤 둘과 비슷하냐',
     '부모형이 <b>압도적으로 많으면</b> 두 유전자가 매우 가깝고, 재조합형이 <b>부모형과 비슷하게 '
     '많으면</b> 멀거나 독립분리처럼 행동한다',
     ['E0-73', 'E0-74']),

    ('오른쪽 상자 — 재조합형 수 ÷ 전체 수 × 100',
     '이 값이 <b>교차율(재조합빈도)</b>이다',
     ['E0-28']),

    ('그 나눗셈을 하려면 먼저 네 무리를 두 편으로 갈라야 한다',
     '교차율 계산에서 <b>가장 먼저 할 일은 부모형과 재조합형을 구분하는 것</b>이다. '
     '수만 보고 아무 둘이나 더하면 틀린다',
     ['E0-29']),

    ('상자 안의 셈 — (90 + 95) ÷ 1000 × 100 = 18.5',
     '부모형 410·405, 재조합형 90·95이면 교차율은 <b>18.5 %</b>다',
     ['E0-63']),

    ('상자 맨 아랫줄 — 18.5 %가 곧 18.5 cM이다',
     '재조합빈도 <b>1 %가 지도거리 1 cM</b>이다. 빈도가 거리의 단위 그 자체다',
     ['E0-14']),

    # ── B 띠
    ('B띠 — 자 하나 위에 유전자 일곱이 저마다 정해진 자리에 찍혀 있다',
     '<b>유전자 지도</b>는 특정 염색체 위 유전자들의 <b>상대적 순서와 거리</b>를 나타낸다',
     ['E0-12']),

    ('자의 눈금 단위',
     '<b>map unit</b> 또는 <b>centimorgan(cM)</b>',
     ['E0-13']),

    ('눈금에 붙은 이름 — 0 짧은 더듬이 · 16.5 고동색 눈 · 48.5 검은 몸 · 57.5 주홍색 눈 · '
     '67.0 흔적 날개 · 75.5 아래로 휜 날개 · 104.5 갈색 눈',
     '왼쪽 끝이 <b>짧은 더듬이(0)</b>, 오른쪽 끝이 <b>갈색 눈(104.5)</b>이다',
     ['E0-30', 'E0-31', 'E0-32', 'E0-33', 'E0-34', 'E0-35', 'E0-36', 'E0-37', 'E0-38']),

    ('아래 붉은 두 구간 — 눈금을 빼면 거리가 나온다',
     '두 유전자 사이 거리는 <b>지도상 위치의 차</b>다. 48.5와 57.5 사이가 9.0, '
     '57.5와 67.0 사이가 9.5인 식이다',
     ['E0-54', 'E0-39', 'E0-40', 'E0-41', 'E0-42', 'E0-43', 'E0-44', 'E0-45',
      'E0-46', 'E0-49', 'E0-50', 'E0-51', 'E0-52', 'E0-53']),

    ('그 아래 청록 구간 — 67.0 − 48.5 = 18.5 = 9.0 + 9.5',
     '<b>인접 구간의 합</b>으로도 멀리 떨어진 두 유전자 거리를 구할 수 있다. 빼기와 더하기의 '
     '답이 같다',
     ['E0-55', 'E0-70', 'E0-71', 'E0-72']),

    # ── C 띠
    ('C띠 왼쪽 — 거리가 짧은 동안에는 실선이 점선(자 눈금)과 거의 겹친다',
     '재조합빈도가 <b>낮으면 가깝다</b>. 구간이 짧으면 재는 대로 관측된다',
     ['E0-11', 'E0-47']),

    ('오른쪽으로 갈수록 실선이 점선 아래로 벌어진다',
     '재조합빈도가 <b>높으면 멀다</b>. 다만 멀어질수록 한 구간에서 교차가 두 번 일어나 겉으로는 '
     '부모형이 되고, 그만큼 관측값이 자보다 작아진다',
     ['E0-10', 'E0-48']),

    ('★ 실선이 붉은 점선에 붙고 더는 오르지 않는다',
     '재조합빈도의 <b>이론적 최대값은 50 %</b>다. 독립이면 네 무리가 1:1:1:1이라 재조합형이 '
     '정확히 절반이고, 그보다 더 섞일 수는 없다',
     ['E0-15']),

    ('천장에 붙은 구간 — 지도거리가 더 늘어도 관측은 그대로다',
     '50 %에 가까우면 <b>독립유전처럼 행동하거나 매우 멀다</b>고 읽는다. 지도거리가 50 cM을 '
     '넘어도 관측은 약 50 %에서 <b>포화</b>한다',
     ['E0-16', 'E0-17', 'E0-69']),

    ('곡선의 시작점 — 원점',
     '재조합빈도가 <b>0 %</b>면 아주 가깝게 연관되었거나 <b>교차를 관찰하지 못한</b> 경우다. '
     '같은 자리라는 뜻은 아니다',
     ['E0-18']),

    ('두 띠를 겹쳐 보면 자와 관측이 한 그림이다',
     '<b>재조합형 비율이 곧 상대적 유전자 거리의 근사값</b>이다 — 가까운 구간에서만 그렇고, '
     '멀어지면 어긋난다',
     ['E0-75']),
]


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'d05p02'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    svg = open(SVGF, encoding='utf-8').read().strip()
    assert '`' not in svg and '\\' not in svg, 'SVG에 백틱·역슬래시'
    assert svg.startswith('<svg') and svg.endswith('</svg>')

    i = src.index("{id:'d05p01'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'d05p02',t:'거리와 재조합빈도',svg:`" + svg + "`,br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    k = out.index("{id:'d05p02'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('d05p02') == ids.index('d05p01') + 1, '위치 오류'
    cards = re.findall(r'"(E0-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복: ' + str(
        [c for c in cards if cards.count(c) > 1])
    print('패널 %d개 · d05p02 행 %d · 카드 %d장 · SVG %d바이트'
          % (len(ids), len(ROWS), len(cards), len(svg)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
