#!/usr/bin/env python3
"""s37p02 「두 광주리와 옆 탁자」 — 진정/이질염색질 · 아세틸화 · 세균과 사람의 염색체.

왜 이 자리인가
  s37p01이 「어떻게 접는가」를 맡았으니 이 패널은 「얼마나 접혔느냐가 무엇을 바꾸는가」를 맡는다.
  기출이 꼬는 자리는 셋이다.
    ① 느슨하고 밝게 염색되는 쪽이 **진정**염색질인가 **이질**염색질인가 (이름 뒤바꾸기)
    ② 아세틸화가 느슨하게 하는가 응축시키는가 (방향 뒤바꾸기)
    ③ 원핵에는 히스톤이 없다 (진핵세포에만 있다)

무엇을 그렸나 — 광주리 둘은 크기도 모양도 같다. 다른 것은 셋뿐이다
  틈    왼쪽은 성겨 팔이 어깨까지 들어감 / 오른쪽은 빈틈없어 손이 막힘 → 접근성 · 발현
  색    왼쪽은 연한 하늘색 / 오른쪽은 진한 남색                        → 밝게 / 진하게 염색
  깃발  왼쪽은 감긴 자리마다 꽂힘 / 오른쪽은 뽑혀 바닥에 흩어짐        → 아세틸화 / 탈아세틸화

  옆 탁자  실패가 없는 맨 실 한 타래 / 실패를 줄줄이 꿴 실 한 토막  → 원핵에는 히스톤이 없다

  ★ 깃발이 꽂힌 쪽이 성기다 — **아세틸화 = 느슨 = 켜짐**이 그림에 붙어 있다.

⚠ 그리지 않은 것은 걸지 않았다 — 간기 염색질의 영토(E1-114), G1의 DNA 최소(E1-115),
  그리고 세균 원형 / 사람 선형(E1-116 계열)은 소품이 없어 다음 패널로 남긴다.
  ★ 특히 E1-116: 탁자 위 고리가 끝이 열린 채로 그려져 「원형」이 성립하지 않는다. 걸지 않았다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('광주리 둘이 같은 크기 같은 모양이고 다른 것은 셋뿐이다 — <b>틈·색·깃발</b>. '
      '왼쪽은 성겨서 <b>팔이 어깨까지 들어가고</b> 연하게 칠해졌고 깃발이 <b>꽂혀</b> 있다. '
      '오른쪽은 빈틈이 없어 <b>손이 막히고</b> 진하며 깃발이 <b>뽑혀 바닥에</b> 있다.')

BX = """<p><b>접힌 정도가 곧 읽히는 정도다.</b> 앞 패널이 「어떻게 접는가」였다면 이 패널은 「얼마나 접혔느냐가 무엇을 바꾸는가」다. 광주리 둘은 <b>크기도 모양도 같다</b> — 다른 것은 <b>틈·색·깃발</b> 셋뿐이고, 그 셋이 서로 붙어 다닌다.</p><p><b>왼쪽 — 진정염색질(euchromatin).</b> 실이 <b>성기게</b> 감겨 틈이 넓고, 그 틈으로 사람이 <b>팔을 어깨까지</b> 밀어 넣어 안의 실을 잡는다. <b>덜 응축</b>되어 <b>접근이 되고</b> 따라서 <b>발현이 잘 된다</b>. 색이 <b>연한 것</b>은 <b>밝게 염색</b>된다는 뜻이다.</p><p><b>오른쪽 — 이질염색질(heterochromatin).</b> 같은 실인데 <b>빈틈없이</b> 감겨 손바닥이 표면에서 막힌다. <b>더 응축</b>되어 <b>접근성이 낮고</b> 발현이 억제된다. 색이 <b>진한 것</b>이 <b>진하게 염색</b>된다는 뜻이다. 「느슨하고 밝게 염색되는 쪽은?」이라고 물으면 답은 <b>진정</b>염색질이다 — 이름을 뒤바꾼 선지가 가장 흔하다.</p><p><b>깃발이 방향을 알려 준다.</b> 감긴 자리마다 꽂힌 <b>흰 깃발</b>이 <b>아세틸기</b>다. 깃발이 <b>꽂힌 쪽이 성기다</b> — <b>히스톤 아세틸화 = 느슨해짐 = 전사 촉진</b>. 오른쪽은 깃발이 <b>뽑혀 바닥에</b> 흩어졌고 그만큼 빽빽하다 — <b>탈아세틸화 = 응축 = 전사 억제</b>. 아세틸기가 히스톤의 양전하를 가려 DNA를 덜 끌어당기게 만들기 때문이다.</p><p><b>옆 탁자 — 누가 실패를 쓰는가.</b> 왼쪽 것은 <b>실패가 하나도 없는</b> 맨 실 한 타래이고, 오른쪽 것은 <b>실패를 줄줄이 꿴</b> 실 한 토막이다. <b>히스톤은 진핵세포에만 있고 원핵생물에는 없다</b> — 실패가 있느냐 없느냐가 그 차이다. (세균 염색체가 <b>원형</b>이고 사람이 <b>선형</b>이라는 것, 그리고 둘 다 <b>DNA와 단백질이 함께 있고 슈퍼코일로 꼬인다</b>는 것은 이 그림에 소품이 없어 다음 패널로 남겼다.)</p><p><b>기출이 꼬는 자리.</b> ① 「이질염색질은 밝게 염색되고 발현이 잘 된다」 → <b>거짓</b>(진정염색질이다). ② 「탈아세틸화가 전사를 촉진한다」 → <b>거짓</b>(억제한다). ③ 「세균에도 히스톤이 있다」 → <b>거짓</b>(진핵세포에만 있다).</p>"""

ROWS = [
    ('광주리 둘이 같은 크기 같은 모양 — 다른 것은 틈·색·깃발 셋뿐이다',
     '<b>염색질</b>은 같은 것인데 <b>얼마나 접혔느냐</b>만 다르다. 그 차이가 곧 '
     '<b>읽히느냐 안 읽히느냐</b>다',
     None),
    ('왼쪽 — 실이 성기게 감겨 틈이 넓고, 사람이 팔을 어깨까지 밀어 넣어 안의 실을 잡는다',
     '<b>진정염색질(euchromatin)</b> — <b>덜 응축</b>되어 접근이 되고 <b>발현이 잘 된다</b>',
     ['E1-109']),
    ('왼쪽 광주리는 색이 연하다',
     '<b>밝게 염색</b>된다. 「느슨하고 밝게 염색되는 쪽은?」의 답이 <b>진정</b>염색질이다 — '
     '이름을 뒤바꾼 선지가 가장 흔하다',
     ['E1-111']),
    ('오른쪽 — 같은 실이 빈틈없이 감겨 손바닥이 표면에서 막히고, 색이 진하다',
     '<b>이질염색질(heterochromatin)</b> — <b>더 응축</b>되어 <b>접근성이 낮고</b> '
     '<b>진하게 염색</b>된다',
     ['E1-110']),
    ('왼쪽은 감긴 자리마다 흰 깃발이 꽂혀 있다',
     '<b>히스톤 아세틸화</b> — 깃발이 꽂힌 쪽이 <b>성기다</b>. 곧 <b>느슨해지고 전사가 촉진</b>된다. '
     '아세틸기가 히스톤의 양전하를 가려 DNA를 덜 끌어당기게 만들기 때문이다',
     ['E1-112#1']),
    ('오른쪽은 깃발이 하나도 없고, 뽑힌 깃발들이 바닥에 흩어져 있다',
     '<b>탈아세틸화</b> — 깃발이 빠진 만큼 <b>응축되고 전사가 억제</b>된다',
     ['E1-112#2', 'E1-112']),
    ('옆 탁자 — 왼쪽 것은 실패가 하나도 없는 맨 실 한 타래, 오른쪽 것은 실패를 줄줄이 꿴 실 한 토막',
     '<b>히스톤은 진핵세포에만 있다</b> — <b>원핵생물에는 없다</b>. 실패가 있느냐 없느냐가 '
     '그 차이다',
     ['E1-106']),
]

PC = []


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s37p02'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR

    i = src.index("{id:'s37p01'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s37p02',t:'두 광주리와 옆 탁자',br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n   '.join(rows) + ']}')
    out = src[:e + 1] + ',\n  ' + panel + src[e + 1:]

    k = out.index("{id:'s37p02'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('s37p02') == ids.index('s37p01') + 1, '위치 오류'
    cards = re.findall(r'"([A-Z][\w-]*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복: ' + str(cards)
    print('패널 %d개 · s37p02 행 %d · 카드 %d장' % (len(ids), len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
