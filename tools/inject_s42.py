#!/usr/bin/env python3
"""s42 「생활사 세 집」 신설 — 유성생식 생활사 3유형 (D 게이트).

★ 왜 새 장면이 필요했나
  이 카드들은 d01p01(감수분열 3열 대조표)에 pc로 뭉뚱그려 걸려 있었다.
  그 표에는 생활사가 한 칸도 없다. 2026-08-20 사용자가 잡았다 —
  「이거랑 연결이 안되는거같은데?」. 떼고 나니 갈 곳이 없어 새로 그렸다.

★ 상징은 덱의 기존 것을 그대로 쓴다 (규칙 ④ 하나의 상징은 하나의 사실)
    자루 두 개 = 2n · 자루 한 개 = n · 깔때기 = 감수분열   ← 전부 s36p02와 같다
  셋을 가르는 축은 하나다 — **누가 마당을 채우는가**.

★ 실측 (2600×1418)
  왼쪽 마당  자루 둘짜리 8 · 자루 하나짜리는 문간 상자 안 2뿐
  가운데 마당 자루 하나짜리 12 · 자루 둘짜리 1(깔때기 안) · 깔때기 아래 1짜리 2
  오른쪽 마당 위층 자루 둘짜리 4 · 아래층 자루 하나짜리 6 · 사다리에 2명
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('세 마당을 가르는 축은 하나다 — <b>누가 마당을 채우는가</b>. '
      '자루 <b>두 개</b>가 2n, <b>한 개</b>가 n이고 깔때기가 감수분열이다(s36p02와 같은 표식). '
      '어른들이 채우면 동물, 작은 이들이 채우면 균류, 두 층이 사다리로 오가면 식물이다.')

BX = """<p><b>★ 셋을 가르는 것은 「감수분열이 언제 오는가」 하나다.</b> 유성생식은 어디서나 같은 두 사건을 거친다 — <b>수정</b>으로 2n이 되고 <b>감수분열</b>로 n이 된다. 갈리는 것은 그 사이에 <b>어느 쪽이 다세포로 오래 사는가</b>이고, 그림에서는 <b>어느 쪽이 마당을 채우는가</b>로 나타난다.</p><p><b>왼쪽 — 2배체 우세형(동물).</b> 마당이 자루 둘짜리로 가득하다. 사람이든 개든 우리가 보는 몸은 전부 2n이고, 반수체는 <b>정자·난자뿐</b>이라 문간 상자 안 둘로 족하다. 감수분열은 <b>배우자를 만들 때 딱 한 번</b> 오고, 그 산물이 바로 배우자다 — 그래서 동물에는 「반수체 다세포 단계」가 아예 없다.</p><p><b>가운데 — 반수체 우세형(대부분 균류·일부 원생생물·조류).</b> 정반대다. 마당이 자루 하나짜리로 가득하고 자루 둘짜리는 <b>딱 하나</b>인데 그마저 곧바로 깔때기로 들어간다. <b>접합자만 2n</b>이고 그 상태가 오래가지 못한다 — 수정 <b>직후</b>에 감수분열이 온다. 2n으로 사는 시간이 사실상 없다는 것이 이 유형의 전부다.</p><p><b>오른쪽 — 세대교번형(식물·일부 조류).</b> 여기만 마당이 <b>두 층</b>이다. 위층(2n 포자체)과 아래층(n 배우체)에 <b>같은 수</b>가 살고, 사다리 하나로 <b>양방향</b> 오간다. 둘 다 다세포로 제 몫의 삶을 산다는 뜻이고, 그래서 이름이 「교<b>번</b>」이다. 자세한 한 바퀴는 s36p02 「한 바퀴 도는 마당」에 있다.</p><p><b>기출이 꼬는 자리.</b> ① 「동물도 반수체 다세포 단계가 있다」 → <b>거짓</b>(배우자뿐이다). ② 「균류는 감수분열을 안 한다」 → <b>거짓</b>(접합자 직후에 한다). ③ 「식물의 배우자는 감수분열로 만든다」 → <b>거짓</b>(배우체가 <b>체세포분열</b>로 만든다 — s36p02의 도구 없는 첫째 칸이 그 답이다). ④ 「세대교번은 2n과 n이 번갈아 나오는 것이니 동물도 세대교번이다」 → <b>거짓</b>(둘 다 <b>다세포</b>여야 한다).</p>"""

ROWS = [
    ('지붕에 개가 앉은 왼쪽 집 — 마당이 <b>자루 두 개</b>를 멘 어른들로 가득하고, '
     '자루 한 개짜리는 문간 상자 안 <b>둘</b>뿐이다',
     '<b>2배체 우세형(동물)</b> — 성체가 <b>2n</b>이고 반수체는 <b>배우자뿐</b>이다. '
     '감수분열이 배우자를 <b>직접</b> 만든다', ['D0-171#1', 'N0-4']),
    ('지붕에 버섯이 난 가운데 집 — 마당이 <b>자루 한 개</b>짜리로 가득하고, '
     '자루 두 개짜리는 <b>딱 하나</b>인데 그마저 깔때기로 들어가 아래에서 한 개짜리 둘이 나온다',
     '<b>반수체 우세형(대부분 균류·일부 원생생물·조류)</b> — 성체가 <b>n</b>이고 '
     '<b>접합자만 2n</b>인데 곧바로 감수분열한다. 2n으로 사는 시간이 사실상 없다',
     ['D0-171#2']),
    ('지붕에 화분 나무가 선 오른쪽 집 — 마당이 <b>위아래 두 층</b>이고 '
     '위층은 자루 두 개짜리, 아래층은 자루 한 개짜리로 <b>수가 같다</b>. '
     '사다리 하나에 오르는 이와 내려오는 이가 함께 있다',
     '<b>세대교번형(식물·일부 조류)</b> — <b>2n 포자체</b>와 <b>n 배우체</b>가 교대한다. '
     '둘 다 <b>다세포</b>로 제 몫의 삶을 사는 것이 요점이라 「교번」이다. '
     '한 바퀴 전체는 s36p02에 있다', ['D0-171#3']),
    ('★ 세 마당을 나란히 두면 — 자루 둘짜리와 한 개짜리 중 <b>누가 마당을 채우는가</b>로 갈린다',
     '유성생식 생활사의 <b>3유형</b>. 어디서나 수정으로 2n이 되고 감수분열로 n이 되는 것은 같고, '
     '갈리는 것은 <b>어느 쪽이 다세포로 오래 사는가</b> 하나다', ['D0-171']),
    ('가운데 마당의 깔때기 — s36p02 셋째 칸의 그 깔때기와 같은 기계다',
     '감수분열이 <b>언제</b> 오는지가 세 유형을 가른다. 동물은 배우자 만들 때, '
     '균류는 접합자 <b>직후</b>, 식물은 포자체가 다 자란 뒤다', []),
    ('어느 마당에서나 자루가 <b>둘에서 하나로</b> 줄어드는 자리는 깔때기뿐이고, '
     '<b>하나에서 둘로</b> 늘어나는 자리는 짝을 만나는 자리뿐이다',
     '감수분열이 없으면 세대마다 염색체 수가 <b>계속 늘어난다</b>. '
     '수정이 두 반수체 세트를 합쳐 <b>2배체를 회복</b>하고, 감수분열이 그것을 되돌린다',
     ['D0-169', 'D0-170']),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s42'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    assert os.path.exists(os.path.join(ROOT, 'img', 's42p01.webp')), '그림이 없다'

    i = src.index("{id:'s41'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s42p01',t:'생활사 세 집',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    scene = ("{id:'s42',t:'생활사 세 집',gate:'D',unit:'13 감수분열과 유성생식 — 생활사',"
             "panels:[\n   " + panel + "\n  ]}")
    out = src[:e + 1] + ',\n ' + scene + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    blk = out[out.index("{id:'s42p01'"):]
    blk = blk[:close_brace(blk, 0) + 1]
    cards = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복'
    print('장면 s42 신설 · 행 %d · 카드 %d장 (%s)'
          % (len(ROWS), len(cards), ' '.join(sorted(cards))))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
