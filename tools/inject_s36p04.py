#!/usr/bin/env python3
"""s36p04 「두 밭」 — 단자엽 vs 진정쌍자엽.

왜 이 자리인가
  기본서의 「시험에서 자주 꼬는 지점」에 그대로 있다 —
  단자엽=평행맥·수염뿌리·3수성 vs 쌍자엽=그물맥·곧은뿌리·4·5수성.
  덱에는 자엽(떡잎) 수만 s34p04 종자 창고에 그려져 있고 나머지 넷은 그림이 없었다.
  네 형질 모두 **셀 수 있고 서로 반대**라 한 장에 1:1로 들어간다.

무엇을 그렸나 — 밭 둘이 나란히, 같은 크기 같은 화풍. 다른 곳은 넷뿐이다
  잎맥    왼쪽 곧은 줄이 나란히 / 오른쪽 갈라져 그물          → 평행맥 / 그물맥
  뿌리    왼쪽 굵은 것 없이 실뭉치 / 오른쪽 굵은 것 하나가 곧게 → 수염뿌리 / 곧은뿌리
  줄기 단면 왼쪽 점이 흩어짐 / 오른쪽 점이 한 고리로           → 산재 / 고리 배열
  꽃잎    왼쪽 셋 / 오른쪽 다섯                                → 3의 배수 / 4·5의 배수

  ★ 자엽 수(1개 / 2개)는 s34p04 종자 창고에 이미 있다 — 여기서 또 걸지 않는다.

⚠ 그리지 않은 것은 걸지 않았다 — 화분 구멍 수(단자엽 1 / 쌍자엽 3)는 소품이 없다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('밭 둘이 같은 크기 같은 화풍이고 <b>다른 곳은 넷뿐</b>이다 — <b>잎맥·뿌리·줄기 단면·꽃잎 수</b>. '
      '왼쪽이 <b>단자엽</b>, 오른쪽이 <b>진정쌍자엽</b>이다. 넷이 전부 정반대라 하나만 알면 나머지가 따라온다.')

BX = """<p><b>네 군데만 보면 된다.</b> 두 밭의 풀은 크기도 화풍도 같다. 다른 것은 <b>잎맥·뿌리·줄기 단면·꽃잎 수</b> 넷뿐이고, 그 넷이 그대로 기출의 선지가 된다. 왼쪽이 <b>단자엽</b>, 오른쪽이 <b>진정쌍자엽</b>이다.</p><p><b>잎맥.</b> 왼쪽 잎의 줄은 밑에서 끝까지 <b>나란히</b> 가고 서로 만나지 않는다 — <b>평행맥</b>. 오른쪽 잎의 줄은 갈라지고 서로 이어져 <b>그물</b>을 이룬다 — <b>그물맥</b>. 벼·옥수수·난초의 잎을 떠올리면 왼쪽이고, 참나무·장미의 잎이면 오른쪽이다.</p><p><b>뿌리.</b> 왼쪽 땅속에는 <b>굵은 것이 하나도 없다</b>. 굵기가 고른 가는 실이 뭉텅이로 퍼진다 — <b>수염뿌리</b>이고, <b>주근이 없다</b>는 것이 요점이다. 오른쪽은 굵은 뿌리 <b>하나</b>가 곧게 내려가고 거기서 잔뿌리가 난다 — <b>곧은뿌리</b>(주근).</p><p><b>줄기 단면.</b> 잘라 세워 둔 토막의 자른 면을 본다. 왼쪽은 점이 면 전체에 <b>흩어져</b> 있고 가운데에도 있다 — 관속의 <b>산재</b>. 오른쪽은 점이 가장자리를 따라 <b>한 고리</b>로 늘어서고 가운데는 비었다 — <b>고리 배열</b>. 「흩어졌나 줄을 섰나」가 그대로 답이다.</p><p><b>꽃 기관 수.</b> 왼쪽 꽃잎은 <b>셋</b>, 오른쪽은 <b>다섯</b>이다. 단자엽은 꽃 기관이 <b>3의 배수</b>, 진정쌍자엽은 <b>4 또는 5의 배수</b>다. 세어서 3으로 나뉘면 왼쪽, 4·5로 나뉘면 오른쪽이다.</p><p><b>여기 없는 두 가지.</b> ① <b>자엽(떡잎) 수</b>는 단자엽 1개·진정쌍자엽 2개인데, 그 그림은 <b>씨앗 공방 종자 창고</b>에 이미 있다 — 얇은 떡 한 장에 배젖 자루가 남은 쪽이 단자엽, 두꺼운 떡 두 장이 배젖을 흡수한 쪽이 쌍자엽이었다. ② 화분 구멍 수(단자엽 1개 / 진정쌍자엽 3개)는 이 그림에 소품이 없다.</p><p><b>왜 「진정」쌍자엽인가.</b> 전통적으로 쌍자엽으로 묶던 무리가 <b>측계통</b>이라, 그중 한 분기군만 <b>진정쌍자엽(eudicot)</b>으로 따로 부른다. 「쌍자엽식물은 하나의 분기군이다」는 <b>오답</b>이다.</p>"""

ROWS = [
    ('밭 둘이 나란히, 같은 크기 같은 화풍 — 다른 곳은 넷뿐이다',
     '왼쪽이 <b>단자엽</b>, 오른쪽이 <b>진정쌍자엽</b>이다. 네 형질이 전부 정반대라 '
     '<b>하나만 알면 나머지가 따라온다</b>',
     ['X-PL-13']),
    ('왼쪽 잎 — 줄이 밑에서 끝까지 나란히 가고 서로 만나지 않는다',
     '<b>평행맥</b>. 벼·옥수수·난초의 잎이다',
     ['M1-24#1']),
    ('오른쪽 잎 — 줄이 갈라지고 서로 이어져 그물을 이룬다',
     '<b>그물맥</b>. 참나무·장미의 잎이다',
     ['M1-24#2', 'M1-24']),
    ('왼쪽 땅속 — 굵은 것이 하나도 없고 굵기가 고른 가는 실이 뭉텅이로 퍼진다',
     '<b>수염뿌리</b>. 요점은 <b>주근이 없다</b>는 것이다',
     ['M1-26#1']),
    ('오른쪽 땅속 — 굵은 뿌리 하나가 곧게 내려가고 거기서 잔뿌리가 난다',
     '<b>곧은뿌리</b>(주근)',
     ['M1-26#2', 'M1-26']),
    ('왼쪽 줄기 토막의 자른 면 — 점이 면 전체에 흩어져 있고 가운데에도 있다',
     '관속의 <b>산재</b>',
     ['M1-27#1']),
    ('오른쪽 줄기 토막의 자른 면 — 점이 가장자리를 따라 한 고리로 늘어서고 가운데는 비었다',
     '관속의 <b>고리 배열</b>. 「흩어졌나 줄을 섰나」가 그대로 답이다',
     ['M1-27#2', 'M1-27']),
    ('왼쪽 꽃잎은 셋, 오른쪽 꽃잎은 다섯',
     '꽃 기관 수 — 단자엽은 <b>3의 배수</b>, 진정쌍자엽은 <b>4·5의 배수</b>다',
     ['M1-28', 'M1-28#1', 'M1-28#2']),
    ('이 그림에 없는 것 — 자엽(떡잎) 수',
     '단자엽 <b>1개</b> / 진정쌍자엽 <b>2개</b>. 그 그림은 <b>씨앗 공방 종자 창고</b>에 이미 있다 — '
     '얇은 떡 한 장에 배젖 자루가 남은 쪽이 단자엽, 두꺼운 떡 두 장이 배젖을 흡수한 쪽이 쌍자엽이었다',
     None),
]

PC = []


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s36p04'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR

    i = src.index("{id:'s36p03'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s36p04',t:'두 밭',br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n   '.join(rows) + ']}')
    out = src[:e + 1] + ',\n  ' + panel + src[e + 1:]

    k = out.index("{id:'s36p04'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('s36p04') == ids.index('s36p03') + 1, '위치 오류'
    cards = re.findall(r'"([A-Z][\w-]*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복: ' + str(cards)
    print('패널 %d개 · s36p04 행 %d · 카드 %d장' % (len(ids), len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
