# -*- coding: utf-8 -*-
"""pc 3~4장짜리 판 18개 — 60장 남짓.

★ 여기서도 ⑤ 배열이 다섯 — 「둘·셋을 비교하라」는 카드가 옆 판에 반쪽을 두고 있다
  N0-21#1 선구동물 예 — 연체·환형은 s04p03, 절지는 s04p04
  S-AN-8  순환계로 문 나누기 — 선충 없음(s04p02) · 연체 개방/환형 폐쇄(s04p03)
  X-PL-2  8핵 7세포 vs 2세포 — 배낭 s34p02 · 화분립 s34p01
  S-PL-2  암배우체 vs 수배우체 — 같은 두 판
★ ② 중복 제거도 하나 — G1-39(비타민 B12 = 코발라민)는 s12p04 의 「코발트빛 코」 행에
  **이미 제대로 걸려 있는데** s12p01 의 pc 에도 얹혀 있었다. pc 는 이런 것도 안 보여 준다.

③ 폐기 셋 — 목록의 한 항목이 덱에 아예 없다
  I0-33  파지 방어 셋 = 수용체 돌연변이 · 제한효소 · CRISPR
         — 「수용체 단백질」 돌연변이가 덱 전체 0건이다. 나머지 둘만 그려져 있다
  A0-63  소포체 2종 = 활면 / 조면 — 「활면」이 덱 전체 0건이다. s14p01 은 「왼쪽 포장소」 하나로만 그린다
  G1-40  비타민 B12 의 기능 = 핵산·적혈구 생성 — s12p04 는 흡수(봉투와 문)만 그리고 기능은 안 그린다
"""
import sys, os, re
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from pc_sweep import sweep

def go(pid, **kw):
    t = open(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', 'sketchy.html'),
             encoding='utf-8').read()
    i = t.index("{id:'%s'" % pid)
    if re.search(r"pc:\[", t[i:i + 500]): sweep(pid, **kw)

go('s01p04', DEMOTE=[('["어깨에 멘 크레인"',            ['I0-45']),
                     ('["네 칸짜리 판 두 개 = 여덟 칸"', ['I0-17']),
                     ('["짖는 개"',                     ['I0-45#2'])])

go('s03p02', DEMOTE=[('["붉은 밀랍 도장을 찍는 사무원', ['I0-33#2']),
                     ('["벽면 초상화 게시판"',          ['I0-34#1'])],
             DROP=['I0-33'])

go('s04p02', DEMOTE=[('["위쪽만 판석, 아래는 맨흙"',        ['N0-14']),
                     ('["머리 위로 껍질을 벗김"',            ['N0-25#3']),
                     ('["양 끝이 마개로 막힌 파이프"',       ['X-AN-13'])])

go('s04p03', DEMOTE=[('["문어만 벽의 배관에', ['X-AN-12', 'S-AN-8']),
                     ('["대리석 바닥"',       ['N0-21#1'])],
             MOVE=[('s04p04', '["팔·다리 관절마다 박힌 검은 쇠 경첩"', ['N0-21#1']),
                   ('s04p02', '["양 끝이 마개로 막힌 파이프"',          ['S-AN-8'])])

go('s05p03', DEMOTE=[('["종잇장처럼 얇은 조각 하나', ['M2-11', 'M2-13#3']),
                     ('["가장 넓은 조각',            ['M2-12']),
                     ('["중간 크기의 청록 조각',      ['M2-13'])])

go('s09p03', DEMOTE=[('["천에 덮여 안 쓰는 배양기"', ['I1-39']),
                     ('["벽 쌓기가 멈춘 자리"',      ['X-BT-23'])],
             DROP=['I1-37#1'])

go('s10p02', DEMOTE=[('["청록 돔(위층)과 청록 상자(아래층)', ['A0-57', 'A0-57#2']),
                     ('["아래 상자 옆면 — 열쇠구멍이 넷인',  ['F0-89'])])

go('s12p01', DEMOTE=[('["하반신이 붉은 벽돌인 인부"', ['G1-92']),
                     ('["벽돌 인부만 팔에 낀 봉투',   ['G1-214'])],
             # ② 중복 제거 — G1-39 는 s12p04 의 「코발트빛 코」 행에 **이미** 걸려 있는데
             #    s12p01 의 pc 에도 얹혀 있었다. 여기서만 떼면 s12p04 링크는 그대로 남는다.
             DROP=['G1-39', 'G1-40'])

go('s13p01', DEMOTE=[('["아기를 안고 아치를 통과"',  ['IM-083']),
                     ('["이끼 낀 초소의 쌍둥이 둘"', ['IM-042']),
                     ('["성냥으로 술통의 도화선에',  ['IM-046'])])

go('s14p01', DEMOTE=[('["가운데 쌍박공 건물', ['A0-74']),
                     ('["오른쪽 폐기 구덩이"', ['A0-77'])],
             DROP=['A0-63'])

go('s21p01', DEMOTE=[('["조상과 후손 전원을 감싼 올가미"', ['S-EV-1']),
                     ('["깃털 단 하나만 올가미 밖',        ['K2-14', 'K2-14#1']),
                     ('["조상이 따로인 둘을 묶고 같은 모자"', ['K2-14#2'])])

go('s23p02', DEMOTE=[('["왼쪽 — 슬롯이 문에 직접 달려', ['B0-220', 'B0-220#1']),
                     ('["오른쪽 — 뒤가 막힌 판',        ['B0-220#2'])])

go('s23p03', DEMOTE=[('["2×2 격자 틀 자체', ['G0-201']),
                     ('["우상 — 크랭크로 진자를 빠르게"', ['G0-201#1']),
                     ('["우하 — 접힌 관을 벌리개로 펼침"', ['G0-201#2'])])

go('s29p01', DEMOTE=[('["헬멧 + 척추 보호대"',        ['N2-1#2']),
                     ('["경첩 달린 턱 보호대"',        ['S-AN-12']),
                     ('["깃털 여행자의 옆길이 되돌아옴"', ['X-AN-20'])])

go('s29p02', DEMOTE=[('["망치를 흡수하는 물 쿠션"',   ['N2-18', 'N2-19']),
                     ('["채워지고 닫히는 쓰레기통"',   ['N2-21']),
                     ('["관으로 양분을 보내는 창고"',  ['N2-22'])])

go('s34p02', DEMOTE=[('["선반의 <b>대포 넷</b>', ['P0-4']),
                     ('["큰 바구니 하나에 나란히 앉은 둘"', ['P0-5', 'X-PL-2', 'S-PL-2'])],
             MOVE=[('s34p01', '알갱이 속 — 몸은 셋인데', ['X-PL-2', 'S-PL-2'])])
