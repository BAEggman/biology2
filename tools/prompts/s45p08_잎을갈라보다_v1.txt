Draw another picture in exactly the same style as the one you just made - warm cream paper, thin hand-inked storybook outlines, soft flat watercolour washes, on bare cream ground, no text of any kind. Square image. Keep it simple and uncluttered.

The picture has a LEFT HALF and a RIGHT HALF, side by side, with bare cream ground between them.

THE LEFT HALF - a leaf shown CUT ACROSS, drawn as a long flat slab lying horizontally, seen edge on, so you can see inside it. Two things are inside the slab and nothing else:
- the middle band of the slab is PACKED FULL of small GREEN LOZENGES, all the same shape, crowded together;
- running lengthwise through that middle band is a BUNDLE OF THIN PIPES tied together, entering the slab from the LEFT edge, with a few clear water droplets travelling along inside the pipes.

THE RIGHT HALF - ONE of those green lozenges drawn very LARGE, as if magnified, standing alone. It is built like this:
- its shell is TWO SEPARATE WALLS, one inside the other, with a clear empty gap between them, so you plainly count two walls; the OUTER wall is drawn slightly thicker than the inner one;
- inside, standing on the floor, are three STACKS of flat round pouches piled one on top of another like stacks of plates, each stack many plates high;
- the whole floor around the stacks is a plain flat MAT, and lying scattered on that mat are a few small hand TOOLS - a spanner, a pair of tongs, a small hammer;
- ONE pouch at the front of the nearest stack is CUT OPEN so that its hollow inside is visible as an empty cavity;
- one PALE BUBBLE is leaving that cut-open pouch, passing out through both walls of the lozenge, and drifting away off the right edge of the picture.

Nothing else is in the picture. No people, no sun, no sky.

WHAT MUST NOT HAPPEN: the lozenge on the right drawn with only ONE wall, or with the two walls touching so the gap cannot be seen; the outer wall drawn thinner than the inner one; the stacks drawn as single pouches instead of tall piles of many; the mat missing, or the tools placed inside a pouch instead of on the mat; no pouch cut open, or the cut pouch drawn solid instead of hollow; the pale bubble starting anywhere other than the cut-open pouch, or stopping inside the lozenge instead of passing out through both walls; the pipes drawn as a single pipe instead of a tied bundle; the green lozenges in the leaf drawn in a single row instead of packed crowded; any writing, letters, numbers, labels or arrows appearing anywhere.
