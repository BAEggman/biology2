#!/usr/bin/env python3
"""s39p02 「두 줄과 거품」 — 복제 원점·복제 기포·복제분기점.

왜 이 자리인가
  s11 공사장은 **분기점 안쪽**(헬리케이스·프라이머·선도/지연)을 이미 다 그렸는데,
  **분기점 바깥**, 곧 「어디서 몇 군데서 시작하나」는 그림이 없었다.
  4차 훑기로 프라이머·5′→3′·분기점 6장을 s11에 걸고, 남은 이 축을 여기서 그린다.

  기출이 여기서 묻는 것은 둘이다.
    ① 기포 하나에 분기점이 **몇 개**이고 어디로 가나 → **2개, 서로 반대 방향**
    ② 세균과 진핵의 차이 → 세균 **원점 1개**, 진핵 **여러 개**

무엇을 그렸나 — 같은 줄 둘, 다른 것은 「몇 군데서 풀리는가」뿐이다
  위 줄   가운데 한 군데만 풀려 거품 하나. 거품 양 끝에 쐐기 둘이 서로 반대로 밀려 나간다
          → 세균: 복제 원점 1개 · 양방향 · 기포 하나에 분기점 2개
  아래 줄 같은 줄인데 다섯 군데가 한꺼번에 풀려 거품이 다섯, 쐐기는 열
          이웃한 거품끼리 거의 맞닿았다
          → 진핵: 원점이 여러 개 · 그래서 큰 DNA도 짧은 시간에 끝난다

  ★ 쐐기가 곧 분기점이다. 거품 하나에 언제나 둘이고, 둘은 반대로 간다.

⚠ 그리지 않은 것은 걸지 않았다 — 복제가 S기에 일어난다(E1-44)는 세포주기 소품이 없다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('줄 둘이 같은 줄이고 다른 것은 <b>몇 군데서 풀리는가</b>뿐이다 — 위는 <b>한 군데</b>(세균), '
      '아래는 <b>다섯 군데</b>(진핵). ★ <b>쐐기가 곧 분기점</b>이라 거품 하나에 언제나 <b>둘</b>이고 '
      '둘은 <b>서로 반대로</b> 간다.')

BX = """<p><b>어디서 시작하는가.</b> 복제는 줄 아무 데서나 시작하지 않는다. <b>복제 원점(origin of replication)</b>이라는 특정 염기서열에서 시작한다. 그림에서 줄이 풀리기 시작한 자리가 그 원점이다.</p><p><b>풀린 자리가 거품이 된다.</b> 원점에서 두 가닥이 갈라져 양쪽으로 벌어지면 가운데가 빈 납작한 타원이 생긴다 — <b>복제 기포(replication bubble)</b>다. 그리고 기포의 <b>양 끝</b>, 곧 두 가닥이 다시 하나로 합쳐지는 자리가 Y자 모양이 되는데 그것이 <b>복제분기점(replication fork)</b>이다.</p><p><b>★ 쐐기가 곧 분기점이다.</b> 기포 하나에 쐐기가 <b>둘</b>이고, 둘은 기포 가운데에서 <b>서로 반대 방향</b>으로 밀려 나간다. 그래서 기포는 양쪽으로 자란다 — 이것이 <b>양방향 복제</b>다. 「기포 하나에 분기점이 몇 개냐」는 문항의 답이 이 쐐기 둘이다.</p><p><b>위 줄 — 세균.</b> 원형 염색체에 복제 원점이 <b>보통 하나</b>다. 그래서 거품도 하나뿐이고, 그 하나가 양방향으로 자라 줄 전체를 끝낸다.</p><p><b>아래 줄 — 진핵.</b> 같은 줄인데 <b>여러 군데가 한꺼번에</b> 열린다. 진핵의 선형 염색체는 <b>복제 원점을 많이</b> 쓴다. 이웃한 거품들이 자라 서로 맞닿으면 이어져서 한 가닥이 된다. 원점이 많을수록 <b>큰 DNA를 짧은 시간에</b> 복제할 수 있다 — 사람 염색체 하나가 세균 것보다 훨씬 큰데도 몇 시간 안에 끝나는 이유다. 세균과 진핵 복제의 <b>핵심 차이</b>가 바로 이 개수다.</p><p><b>기출이 꼬는 자리.</b> ① 「복제 기포 하나에 분기점은 하나다」 → <b>거짓</b>(둘, 서로 반대로 간다). ② 「세균은 복제 원점이 여러 개다」 → <b>거짓</b>(보통 하나). ③ 「진핵은 원점이 하나라 복제가 오래 걸린다」 → <b>거짓</b>(원점이 많아 오히려 빠르다). ④ 「복제는 한 방향으로만 진행된다」 → <b>거짓</b>(원점에서 양방향이다).</p>"""

ROWS = [
    ('줄 둘이 같은 줄이다 — 다른 것은 몇 군데서 풀리는가뿐',
     '복제는 아무 데서나 시작하지 않는다. <b>복제 원점(origin of replication)</b>이라는 '
     '특정 염기서열에서 시작한다',
     ['E1-46']),
    ('풀린 자리에서 두 가닥이 벌어져 가운데가 빈 납작한 타원이 생겼다',
     '<b>복제 기포(replication bubble)</b>다',
     ['E1-51']),
    ('기포의 양 끝, 두 가닥이 다시 하나로 합쳐지는 자리에 박힌 쐐기',
     '그 자리가 Y자 모양의 <b>복제분기점(replication fork)</b>이다 — 쐐기가 곧 분기점이다',
     None),
    ('쐐기는 기포마다 둘이고, 둘이 가운데에서 서로 반대 방향으로 밀려 나간다',
     '기포 하나에 분기점은 <b>2개</b>이고 <b>서로 반대 방향</b>으로 이동한다. 그래서 기포가 '
     '양쪽으로 자란다 — <b>양방향 복제</b>다',
     ['E1-52']),
    ('위 줄 — 가운데 한 군데만 풀려 거품이 하나뿐이다',
     '<b>세균</b>의 원형 염색체는 복제 원점이 <b>보통 1개</b>이고 거기서 <b>양방향</b>으로 복제한다',
     ['E1-47']),
    ('아래 줄 — 같은 줄인데 다섯 군데가 한꺼번에 풀려 거품이 다섯이다',
     '<b>진핵</b>의 선형 염색체는 <b>복제 원점을 여러 개</b> 쓴다',
     ['E1-48']),
    ('아래 줄은 이웃한 거품끼리 이미 거의 맞닿았다',
     '원점이 많을수록 <b>큰 DNA를 짧은 시간 안에</b> 복제할 수 있다. 거품들이 만나면 이어져 '
     '한 가닥이 된다',
     ['E1-49']),
    ('두 줄의 차이는 굵기도 길이도 아니고 오직 거품의 개수다',
     '진핵과 세균 복제의 <b>핵심 차이</b>가 바로 이 <b>원점의 개수</b>다 — 진핵 여러 개, 세균 한 개',
     ['E1-53']),
]

PC = []


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s39p02'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR

    i = src.index("{id:'s39p01'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s39p02',t:'두 줄과 거품',br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n   '.join(rows) + ']}')
    out = src[:e + 1] + ',\n  ' + panel + src[e + 1:]

    k = out.index("{id:'s39p02'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('s39p02') == ids.index('s39p01') + 1, '위치 오류'
    cards = re.findall(r'"([A-Z][\w-]*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복: ' + str(cards)
    print('패널 %d개 · s39p02 행 %d · 카드 %d장' % (len(ids), len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
