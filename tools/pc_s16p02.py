# -*- coding: utf-8 -*-
"""s16p02(전화국 · 2차 전달자 — 고리 창구) pc 훑기 — 21장.

★ 이 판의 pc 는 절반이 「배열이어야 할 것」이었다
  2차 전달자 이야기는 세 방에 나뉘어 있다 — 고리(s16p02) · 판과 자갈(s16p02b) · 시각(s15p02).
  「가장 많이 쓰는 둘은?」「다섯을 다 들면?」 같은 카드는 급소가 여러 방에 있으니
  한 방에만 걸면 그린 것의 일부를 버린다.

③ 폐기 둘 — 덱 전체에 0건인 것을 답으로 요구한다
  B0-204  cAMP=아데닐산 / cGMP=구아닐산 고리화효소 — 「구아닐산」이 덱 전체 0건이다
  S-SG-12 종결 3층(GTP 가수분해 / PDE / 탈인산화효소) — 「탈인산화효소」가 덱 전체 0건이다
"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from pc_sweep import sweep

M = '["왼쪽 기계 — <b>곧은 막대</b>'      # 1행 아데닐산 고리화효소
A = '["<b>A자로 벌어진 접이 사다리</b>'   # 2행 PKA
C = '["문간의 정장 남자에서 시작해'       # 3행 증폭
P = '["오른쪽 — <b>펜치</b>'              # 4행 PDE

sweep('s16p02',
  DEMOTE=[
    (M, ['B0-100', 'B0-101', 'X-SG-4', 'B0-113#4', 'B0-204#1', 'X-SG-5',
         'B0-113', 'Q0-9', 'B0-95', 'B0-115', 'B0-191', 'B0-96']),
    (A, ['B0-113#6', 'X-SG-6', 'B0-83']),
    (C, ['B0-136']),
    (P, ['X-SG-2']),
  ],
  MOVE=[
    # ⑤ 이사 — 이 판에 아예 없는 것
    ('s16p01', '["토큰을 뒤집으며', ['B0-113#3']),
    ('s16p01', '["둘이 나란히 서서', ['B0-87']),
    # ⑤ 배열 — 급소가 여러 방에 나뉘어 있다
    ('s16p01',  '["토큰을 뒤집으며',                    ['B0-113', 'Q0-9']),
    ('s16p02b', '["금고 열쇠구멍에서 쏟아지는 작은 자갈"', ['B0-83']),
    ('s15p02',  '["텅 빈 천장 통"',                      ['B0-96']),
    # ⚠ B0-95·B0-115·B0-191·B0-96 은 s16p02b 의 pc 에 **이미** 얹혀 있다.
    #    여기서 또 옮기면 중복이다 — s16p02b 를 훑을 때 그쪽 행으로 내리면
    #    그때 자연스럽게 배열이 된다. pc 가 두 판에 겹쳐 얹힌 것도 pc 의 흔한 모습이다.
  ],
  DROP=['B0-204', 'S-SG-12'])
