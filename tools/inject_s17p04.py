#!/usr/bin/env python3
"""s17p04 「우물 셋 — 무엇이 없으면 안 되는가」 — 수질 삼투기울기와 ADH.

★ 왜 새 판을 만드나
  s17p03의 pc에 매달려 있던 14장 가운데 절반이 **수질 삼투기울기** 카드였는데,
  기울기는 덱 어디에도 **그려진 적이 없다.** 운하 물빛은 처음부터 끝까지 같은 색이다.
  s17p03을 고치면 잘 되어 있는 후크(로마 투구·알·베·V자 이음쇠)를 잃을 위험이 있어
  기울기 전용 판을 따로 세운다.

★ 이 판의 문법 — 셋을 같은 모양으로 세우고 **하나씩만 뺀다**
  1  소금 껍질 O · V자 이음쇠 O   → 통이 바닥만 젖음   (둘 다 있으면 농축된다)
  2  소금 껍질 O · 이음쇠 없음     → 통이 넘침          (ADH가 없다)
  3  소금 껍질 X(자물쇠) · 이음쇠 O → 통이 넘침          (기울기가 무너졌다)
  「둘 중 하나만 없어도 안 된다」가 세 통의 물높이로 한눈에 보인다.

★ 후크 — 전부 이 씬에서 쓰던 것을 그대로 쓴다
  V자 이음쇠 = ADH의 V2 수용체 (s17p03에서 확립)
  금색 자물쇠 = 약물·독물 전용 (s17p02 푸로세미드에서 확립) → 루프 이뇨제
  흰 소금     = Na (s17p02·s17p03에서 확립)
  아래로 갈수록 두꺼워지는 껍질 = 깊을수록 진한 수질 간질. 뜻이 곧 그림이다
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('우물 셋이 똑같이 생겼고 다른 것은 <b>둘</b>뿐이다 — 벽에 <b>소금 껍질</b>이 있는가, '
      '가운데 관에 <b>V자 이음쇠</b>가 꽂혔는가. '
      '★ 둘 다 있는 첫째 우물만 아래 통이 <b>바닥만 젖고</b>, 하나라도 빠지면 <b>통이 넘친다</b>. '
      'V자 이음쇠가 <b>ADH</b>이고 소금 껍질이 <b>수질 삼투기울기</b>다.')

BX = """<p><b>★ 농축은 둘이 갖춰져야 된다.</b> 집합관에서 물을 끌어내려면 두 가지가 동시에 있어야 한다 — 관 벽에 물길이 열려야 하고(<b>ADH</b>), 밖이 안보다 짜야 한다(<b>수질 삼투기울기</b>). 그림은 그 둘을 <b>V자 이음쇠</b>와 <b>벽의 소금 껍질</b>로 갈라 놓고, 셋 중 하나씩만 빼서 결과를 통의 물높이로 보여 준다. 첫째 우물만 통이 바닥만 젖었고 나머지 둘은 넘친다.</p><p><b>기울기는 왜 아래로 갈수록 두꺼운가.</b> 수질은 깊이 들어갈수록 간질이 진해진다. 겉질 가까이는 혈장과 비슷하고 속수질 끝은 몇 배로 짜다. 그래서 관이 아래로 내려가는 동안 <b>내내</b> 물이 빠져나갈 힘이 걸린다. 벽의 흰 껍질이 위는 얇고 아래는 두껍게 그려진 것이 그 그대로다.</p><p><b>그 소금은 어디서 오나.</b> 우물 옆구리에 붙은 기계가 흰 알갱이를 벽에 뿌린다. <b>굵은 상행각</b>의 NaCl 재흡수다. 이 분절은 물에 불투과라서 염만 밖으로 퍼내는데, 그것이 수질 간질을 고삼투로 만드는 <b>핵심 단계</b>다. 셋째 우물은 그 기계에 <b>금색 자물쇠</b>가 걸려 있어 알갱이가 안 나오고, 그래서 벽이 맨 돌이다 — <b>루프 이뇨제</b>가 하는 일이 이것이다. 덱에서 금색은 언제나 약물·독물이다.</p><p><b>두 우물이 왜 같은 결과인가.</b> 둘째는 ADH가 없어서(이음쇠가 없다), 셋째는 기울기가 없어서(껍질이 없다) 물이 안 빠진다. 원인은 다른데 결과는 똑같이 <b>묽고 양이 많은 소변</b>이다. 시험은 이 둘을 바꿔 놓고 원인을 묻는다 — <b>이음쇠를 보는가 벽을 보는가</b>로 갈린다. 그리고 둘째 우물이 보여 주듯 <b>ADH가 없어도 기울기 자체는 그대로 있다</b>. 기울기를 만드는 것은 ADH가 아니라 헨레고리다.</p><p><b>기출이 꼬는 자리.</b> ① 「ADH가 수질 삼투기울기를 만든다」 → <b>거짓</b>. 만드는 것은 굵은 상행각이고 ADH는 그 기울기를 <b>쓰는</b> 쪽이다. ② 「루프 이뇨제는 집합관에 작용한다」 → <b>거짓</b>. 굵은 상행각의 NKCC2를 막아 <b>기울기를 무너뜨려</b> 간접적으로 농축을 방해한다. ③ 「ADH가 없으면 기울기도 사라진다」 → <b>거짓</b>(둘째 우물). ④ 「물 투과성이 ADH로 조절되는 곳은 여러 분절이다」 → <b>거짓</b>. <b>집합관</b>이다.</p>"""

ROWS = [
    ('우물 셋이 <b>같은 크기·같은 모양</b>이고, 가운데를 <b>흰 관</b>이 위에서 아래까지 관통해 '
     '아래 통으로 쏟아진다 · <b>V자 이음쇠</b>는 이 관에만 꽂힌다',
     '가운데 관이 <b>집합관</b>이고 V자 이음쇠가 <b>ADH</b>(V2 수용체)다. '
     '물 투과성이 ADH로 조절되는 분절은 <b>집합관</b>이다 — 다른 분절의 물 투과성은 ADH와 무관하다',
     ['X-RN-14']),

    ('첫째·둘째 우물 벽에 <b>흰 소금 껍질</b>이 붙어 있는데 <b>위는 얇고 아래로 갈수록 두껍다</b> · '
     '옆구리 기계가 흰 알갱이를 벽에 뿌린다',
     '<b>수질 삼투기울기</b>. 깊이 들어갈수록 간질이 짜진다. '
     '뿌리는 기계가 <b>굵은 상행각의 NaCl 재흡수</b>이고, 이것이 '
     '<b>수질 간질을 고삼투성으로 만드는 핵심 단계</b>다',
     ['H1-90']),

    ('★ 첫째 우물 — <b>소금 껍질도 있고 이음쇠도 넷</b> · 이음쇠마다 물이 옆으로 빨려 나가고 '
     '아래 통은 <b>바닥만 젖었다</b>',
     '최종 소변 농축을 직접 정하는 것은 <b>둘</b>이다 — <b>ADH</b>와 <b>수질 삼투기울기</b>. '
     '둘이 갖춰지면 관 안의 물이 벽 쪽으로 끌려 나가 소변이 진해지고 양이 준다',
     ['H1-140', 'H1-170']),

    ('★ 둘째 우물 — 소금 껍질은 <b>첫째와 똑같은데</b> 관 옆구리가 <b>매끈해 이음쇠가 하나도 없다</b> · '
     '통이 <b>넘친다</b>',
     '기울기는 있는데 <b>ADH가 없으면</b> 소변은 <b>묽고 양이 많다</b>. '
     '★ 껍질이 그대로 있는 것이 요점이다 — <b>ADH가 없어도 수질 기울기는 존재한다</b>. '
     '기울기를 만드는 것은 ADH가 아니라 헨레고리다',
     ['H1-172', 'H1-171']),

    ('★ 셋째 우물 — 옆구리 기계에 <b>금색 자물쇠</b>가 걸려 알갱이가 안 나오고 벽이 <b>맨 돌</b>이다 · '
     '이음쇠는 첫째와 똑같이 넷인데 <b>몇 방울만</b> 나오고 통이 <b>넘친다</b>',
     '<b>루프 이뇨제</b>(금색은 덱에서 약물 전용)가 굵은 상행각을 막아 '
     '<b>수질 NaCl 기울기를 약화</b>시킨다. ADH가 있어도 <b>물을 끌어낼 삼투 구동력이 없어</b> '
     '농축이 안 되고, 그래서 소변량이 는다',
     ['H1-173', 'H1-174', 'H1-93']),

    ('★ 셋을 나란히 보면 — <b>통이 빈 것은 첫째 하나뿐</b>이다',
     '<b>둘 중 하나만 없어도 농축이 안 된다.</b> 그래서 <b>굵은 상행각이 정상이어야</b> '
     '집합관에서 ADH가 제 일을 한다 — 기울기가 유지돼야 ADH에 의한 물 재흡수가 가능하기 때문이다. '
     '둘째와 셋째는 원인이 다른데 결과가 같다',
     ['H1-103']),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s17p04'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    assert os.path.exists(os.path.join(ROOT, 'img', 's17p04.webp')), '그림이 없다'

    i = src.index("{id:'s17p03'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s17p04',t:'우물 셋 — 무엇이 없으면 안 되는가',br:'" + BR
             + "',bx:`" + BX + "`,f:[" + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    blk = out[out.index("{id:'s17p04'"):]
    blk = blk[:close_brace(blk, 0) + 1]
    cards = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복'
    print('s17p04 신설 · 행 %d · 카드 %d장' % (len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
