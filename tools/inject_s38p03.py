#!/usr/bin/env python3
"""s38p03 「작업대 위의 저울 둘」 — 샤가프 법칙·왓슨크릭의 근거·디옥시리보스 2′.

왜 이 자리인가
  s38p01(사다리 한 바퀴)이 이중나선의 **생김새**를 그렸다면, 여기는 그 생김새를
  **무엇을 보고 알아냈나**와 **계산이 어디서 나오나**다.
  s38p02(메셀슨-스탈)가 「그래서 어떻게 불어나나」로 넘어가기 전의 마지막 구조 문항이다.

  기출이 여기서 묻는 것은 셋이다.
    ① 샤가프 계산 (C가 40%면 T는? / (G+C)/(A+T)=1.8이면 A는?)
    ② 왓슨·크릭이 무엇을 근거로 삼았나 → 프랭클린의 X선 사진 + 샤가프
    ③ DNA의 당은 무엇이고 리보스와 어디가 다른가 → 디옥시리보스, 2번 탄소

무엇을 그렸나 — ★ 저울 둘은 언제나 수평이고, 서로 담긴 개수가 다르다
  왼쪽 저울  양 접시에 갈고리 둘짜리 조각이 둘씩 — 수평   (A = T)
  오른쪽 저울 양 접시에 갈고리 셋짜리 조각이 넷씩 — 수평  (G = C)
  두 저울에 담긴 개수가 다르다                            (A+T ≠ G+C, 종마다 다르다)
  ★ 갈고리 둘 = A-T, 갈고리 셋 = G-C — s38p01의 갈고리 개수를 그대로 잇는다.

  가운데   검은 카드 한 장에 흐릿한 X자 십자         (프랭클린의 X선 회절 사진)
  왼쪽 사람 오각판 — 꼭짓점마다 고리, 넷 다 있다     (리보스)
  가운데 사람 오각판 — 칸 조각 바로 다음 자리만 비었다 (디옥시리보스, 2번 탄소)

⚠ 그린 것만 건다 — 「종마다 조성이 다르다」는 두 저울의 개수 차이로만 보인다.
   구체적 숫자(40%·1.8·168)는 글자를 안 쓰므로 본문에만 있다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ <b>저울 둘은 둘 다 수평인데, 담긴 개수는 서로 다르다</b> — 그것이 샤가프의 전부다. '
      '<b>A=T, G=C</b>는 언제나 맞고 <b>A+T와 G+C는 종마다 다르다</b>. '
      '그리고 <b>고리 하나가 빠진 판</b>이 디옥시리보스다 — 빠진 자리가 <b>2번</b>이다.')

BX = """<p><b>저울이 곧 샤가프다.</b> 어떤 DNA를 가져와 염기를 세어 보아도 <b>A의 양 = T의 양</b>, <b>G의 양 = C의 양</b>이다. 그림의 두 저울이 둘 다 <b>수평</b>인 것이 이것이다. 이유는 s38p01에서 이미 그렸다 — 칸마다 A는 T와, G는 C와만 짝을 짓기 때문이다. 짝을 지으니 개수가 같아질 수밖에 없다.</p><p><b>★ 그런데 두 저울에 담긴 개수는 서로 다르다.</b> 왼쪽 저울은 접시마다 둘, 오른쪽은 접시마다 넷이다. <b>A+T와 G+C의 비는 종마다 다르다</b>는 뜻이다. 「A=T이므로 A+T=G+C」로 착각하게 만드는 것이 이 단원 최다 함정이고, 두 저울의 높이가 아니라 <b>개수</b>가 다르다는 것이 그 반례다.</p><p><b>갈고리 개수가 어느 저울인지 말해 준다.</b> 갈고리 <b>둘</b>짜리 조각이 담긴 것이 <b>A-T</b> 저울, <b>셋</b>짜리가 담긴 것이 <b>G-C</b> 저울이다. s38p01에서 A-T는 수소결합 2개, G-C는 3개로 그렸고 여기서 그 표시를 그대로 쓴다.</p><p><b>계산은 두 줄이면 끝난다.</b> 전체를 100으로 두고 <b>A=T</b>, <b>G=C</b>만 쓰면 된다. ① 「C가 40%이면 T는?」 → C=40이니 G=40, 둘이 80. 남은 20이 A+T이고 A=T이므로 <b>T=10%</b>다. ② 「(G+C)/(A+T)=1.8이고 총 168염기이면 A는 몇 개?」 → G+C와 A+T의 비가 1.8:1이니 합 2.8이 168이고 A+T는 168÷2.8=60. A=T이므로 <b>A=30개</b>다. 어느 문제든 「짝끼리 같다」 한 줄에서 나온다.</p><p><b>가운데 검은 카드가 프랭클린의 사진이다.</b> 로절린드 프랭클린이 찍은 <b>X선 회절 사진</b>에서 흐릿한 <b>X자 십자</b> 무늬가 나선 구조를 가리켰고, 거기서 나선의 <b>지름과 한 바퀴 길이</b>까지 읽어 낼 수 있었다. 왓슨과 크릭이 모델을 세울 때 쓴 근거는 <b>이 사진과 샤가프의 법칙 둘</b>이고, 그림에서 작업대에 놓인 것도 딱 그 둘뿐이다.</p><p><b>오각판은 당이다.</b> s38p01의 기둥에서 오각형 판이 디옥시리보스였다. 여기서 그 판을 확대해 둘을 견준다. <b>리보스</b>는 꼭짓점마다 고리(-OH)가 다 있고, <b>디옥시리보스</b>는 <b>한 자리만 비어</b> 있다. 비어 있는 그 자리가 <b>2번 탄소</b>이고, 거기 있어야 할 -OH가 <b>-H</b>로 바뀐 것이 「디옥시(deoxy, 산소가 하나 빠졌다)」다. 판 꼭대기에 붙은 칸 조각이 염기가 붙는 <b>1번</b>이므로 <b>그 바로 다음 자리가 2번</b>이다 — 세는 법이 그림에 들어 있다.</p><p><b>왜 하필 2번이 중요한가.</b> RNA는 그 자리에 -OH가 있어 스스로를 끊기 쉽고, 그래서 <b>RNA가 DNA보다 불안정</b>하다. 유전정보를 오래 보관하는 쪽이 DNA인 이유가 이 고리 하나다.</p><p><b>기출이 꼬는 자리.</b> ① 「A=T이므로 A+T=G+C이다」 → <b>거짓</b>(종마다 다르다). ② 「샤가프 법칙은 두 가닥 사이가 아니라 한 가닥 안에서 성립한다」 → <b>거짓</b>(이중가닥 DNA 전체에서 성립하며 한 가닥만 보면 A=T가 깨진다). ③ 「DNA의 당은 리보스다」 → <b>거짓</b>(디옥시리보스). ④ 「디옥시는 3번 탄소의 -OH가 없다는 뜻이다」 → <b>거짓</b>(2번이다. 3′-OH는 살아 있어야 중합효소가 이어 붙인다).</p>"""

ROWS = [
    ('저울 둘이 나란히 서 있고 둘 다 저울대가 완전히 수평이다',
     '<b>샤가프 법칙</b> — 어떤 DNA에서도 <b>A의 양 = T의 양</b>, <b>G의 양 = C의 양</b>이다. '
     '칸마다 짝이 정해져 있으니 개수가 같아질 수밖에 없다',
     ['E1-24']),

    ('왼쪽 저울 — 양 접시의 조각에 갈고리가 둘씩이다',
     '갈고리 둘은 <b>A-T</b>다(s38p01에서 A-T는 수소결합 2개로 그렸다). 이 저울이 수평인 것이 A=T다',
     None),

    ('오른쪽 저울 — 양 접시의 조각에 갈고리가 셋씩이다',
     '갈고리 셋은 <b>G-C</b>다(수소결합 3개). 이 저울이 수평인 것이 G=C다',
     None),

    ('★ 두 저울이 담고 있는 개수가 서로 다르다 — 왼쪽은 접시마다 둘, 오른쪽은 넷이다',
     '<b>A+T와 G+C의 비는 종마다 다르다</b>. 「A=T니까 A+T=G+C겠지」가 이 단원 최다 함정이고, '
     '개수가 다른 두 저울이 그 반례다',
     ['E1-31', 'E1-32']),

    ('작업대 가운데에 놓인 검은 카드 한 장 — 흐릿한 X자 십자가 찍혀 있다',
     '로절린드 <b>프랭클린의 X선 회절 사진</b>. X자 무늬가 나선 구조를 가리켰고 지름과 '
     '한 바퀴 길이까지 읽어 낼 수 있었다',
     None),

    ('작업대에 놓인 것은 사진 한 장과 저울, 그 둘뿐이다',
     '왓슨과 크릭이 이중나선 모델의 근거로 삼은 것은 <b>프랭클린의 X선 사진</b>과 '
     '<b>샤가프의 법칙</b> 둘이다',
     ['E1-25']),

    ('왼쪽 사람이 든 오각판 — 꼭대기 칸 조각을 뺀 네 꼭짓점에 고리가 하나씩, 넷 다 있다',
     '<b>리보스</b>. 고리가 -OH다',
     None),

    ('가운데 사람이 든 오각판 — 칸 조각 바로 다음 자리 하나만 맨숭맨숭하고 고리가 셋뿐이다',
     'DNA를 이루는 당은 리보스가 아니라 <b>디옥시리보스</b>다 — 고리 하나가 빠진 쪽이 그것이다',
     ['E1-16']),

    ('비어 있는 자리는 칸 조각이 붙은 꼭짓점 바로 다음이다',
     '칸 조각(염기)이 붙는 자리가 <b>1번 탄소</b>이므로 바로 다음이 <b>2번 탄소</b>다. '
     '거기 있어야 할 <b>-OH가 -H로</b> 바뀐 것이 「디옥시」다. RNA는 그 자리에 -OH가 있어 '
     '더 불안정하다',
     ['E1-17']),
]

PC = []


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s38p03'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR

    i = src.index("{id:'s38p02'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s38p03',t:'작업대 위의 저울 둘',br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n   '.join(rows) + ']}')
    out = src[:e + 1] + ',\n  ' + panel + src[e + 1:]

    k = out.index("{id:'s38p03'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('s38p03') == ids.index('s38p02') + 1, '위치 오류'
    cards = re.findall(r'"([A-Z][\w-]*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복: ' + str(cards)
    print('패널 %d개 · s38p03 행 %d · 카드 %d장' % (len(ids), len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
