#!/usr/bin/env python3
"""s40 「가문 현판 벽」 신설 — 가계도 (D 게이트). D1 13장.

★ 왜 이 단원이 Sketchy에 맞나
  가계도 기호는 **전부 자의적인 약속**이다 — 사각형이 남성인 데 이유가 없다.
  논리로 못 따라가니 연상으로 붙잡아야 하고, 그게 Sketchy가 값을 내는 자리다.
  (사용자 판정으로 접은 세포막 단원이 그 반대편이다 — 거긴 논리로 이어진다)

★ 소품 — 덱 전체 grep으로 골랐다
    액자(s04p05) · 밧줄(13건) · 초상화(s03p02) 전부 이미 쓰임 → **현판**(0건)으로.
    s21p02 분기도는 **천장에 매단 모빌 + 사람 인형**이라 매체도 토큰도 다르다.

★ 실측 (2752×1536, 검은 화소 침식 후 셈)
    왼쪽 판  1줄 0개 · 2줄 1개 · 3줄 1개
    오른쪽 판 1줄 2개 · 2줄 4개 · 3줄 3개  → 세 줄 모두 있음
  요구대로면 왼쪽 판 2줄은 0개였어야 하는데 하나가 들어왔다.
  카드를 죽이지는 않는다 — 「맨 윗줄이 깨끗하다」와 「오른쪽은 세 줄 모두」의 대비가
  살아 있어 D1-165·166은 그대로 성립한다. 한 판 더 돌리는 값보다 위험이 커서 받는다.

★ 걸지 않은 것 — 그림에 없다
    D1-159·160·161 낭포성 섬유증·테이삭스·PKU — 병명이 그림에 없다
    D1-158 헌팅턴 · D1-154 근친결혼 · D1-162 겸상적혈구 — 소품이 없다
  이 넷은 다음 판에 「작은 이름표 현판」과 「같은 갈래에서 나온 둘을 잇는 가로 막대」를
  더하는 편집으로 넣는다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ <b>모양이 성별, 색이 발현</b>이다 — 네모는 남성, 동그라미는 여성, '
      '검게 칠한 것이 발현자다. 가로 막대가 부부, 거기서 내려오는 세로 막대가 자식이다. '
      '판정은 <b>검은 현판이 매 세대에 있느냐</b> 하나로 갈린다 — '
      '왼쪽 판처럼 <b>윗세대가 깨끗한데 아래에서 나오면 열성</b>, '
      '오른쪽 판처럼 <b>세 줄이 안 끊기면 우성</b>이다. '
      '귀퉁이에 점이 찍힌 정상 현판이 <b>보인자</b>다.')

BX = """<p><b>★ 여기는 외울 수밖에 없는 자리다.</b> 사각형이 남성이고 원이 여성인 데에는 아무 이유가 없다. 음영이 발현자라는 것도 약속일 뿐이다. 이렇게 <b>자의적인 약속</b>은 논리로 따라갈 수가 없어서 그림에 걸어 두는 것이 유일한 방법이다. 벽에 걸린 현판 두 판이 그 약속 전부를 담고 있다 — 모양이 성별, 색이 발현, 가로 막대가 부부, 세로 막대가 자식.</p><p><b>왼쪽 판을 읽는 법 — 열성.</b> 맨 윗줄에는 검은 현판이 <b>하나도 없다</b>. 그런데 아래 줄에서 검은 현판이 나온다. 정상인 부모에게서 환자가 나왔다는 뜻이고, 그러려면 그 병은 <b>열성 동형접합</b>이라야 나타나는 것이어야 한다. 그리고 그 부모는 둘 다 겉으로 멀쩡한 <b>보인자</b>다 — 귀퉁이에 작은 점이 찍힌 현판이 그것이다. 정상 대립유전자가 하나만 있어도 충분해서 겉으로는 표가 안 난다.</p><p><b>오른쪽 판을 읽는 법 — 우성.</b> 세 줄 <b>모두</b>에 검은 현판이 있다. 줄이 끊기지 않는다. 우성 유전병은 <b>이형접합만으로도 발현</b>하니 숨을 데가 없고, 그래서 환자 자녀가 있으면 <b>부모 중 최소 한 명은 환자</b>다. 오른쪽 판에 귀퉁이 점이 하나도 없는 것이 그 뜻이다 — 우성병에는 숨은 보인자가 없다.</p><p><b>★ 두 판을 나란히 두는 것이 요령이다.</b> 가계도 문제는 「이 병이 우성인가 열성인가」를 먼저 정해야 풀린다. 판정은 한 줄이다 — <b>세대마다 나오면 우성, 건너뛰면 열성</b>. 붓을 든 사람이 현판을 칠하는 것은 <b>겉으로 나타난 것</b>만 칠한다는 뜻이다. 가계도는 표현형을 적는 것이지 유전자형을 적는 것이 아니다.</p><p><b>기출이 꼬는 자리.</b> ① 「정상 부모에게서 환자가 나왔으니 돌연변이다」 → <b>거짓</b>(부모가 보인자면 된다). ② 「환자가 여자니까 X연관이 아니다」 → <b>거짓</b>(상염색체면 성별과 무관하다). ③ 「우성병은 반드시 부모가 둘 다 환자다」 → <b>거짓</b>(한 명이면 충분하다). ④ 「보인자는 증상이 약하게 나타난다」 → <b>거짓</b>(완전 우성이면 겉으로 완전히 정상이다).</p>"""

ROWS = [
    ('벽 가운데에 짝 없이 홀로 걸린 네모 현판', '가계도에서 <b>사각형은 남성</b>이다', ['D1-146']),
    ('그 옆에 짝 없이 홀로 걸린 둥근 현판', '가계도에서 <b>원은 여성</b>이다', ['D1-147']),
    ('검게 옻칠한 현판', '그 형질 또는 질환의 <b>발현자</b>다. 색이 곧 「나타났다」이고, '
     '모양은 성별이라 둘은 서로 간섭하지 않는다', ['D1-148']),
    ('두 현판을 잇는 <b>가로</b> 막대', '<b>부부</b>다', ['D1-149']),
    ('그 가로 막대 가운데에서 아래로 내려가는 <b>세로</b> 막대', '<b>부모-자식</b> 관계다. '
     '가로는 짝, 세로는 대물림 — 방향 하나로 갈린다', ['D1-150']),
    ('왼쪽 판 맨 아랫줄 — 검은 현판이 <b>정상 현판 둘</b> 아래에 달려 있다',
     '열성 유전병은 <b>열성 동형접합</b>이라야 나타난다. 그래서 정상 부모에게서 환자가 '
     '나오면 그 <b>부모는 둘 다 보인자</b>다 — 이것이 열성 가계도 문제의 첫 수다',
     ['D1-151', 'D1-163']),
    ('왼쪽 판 — 귀퉁이에 작은 검은 점이 찍힌 <b>정상</b> 현판',
     '<b>보인자(carrier)</b>. 이형접합인데도 겉으로 멀쩡한 것은 정상 대립유전자 <b>하나로 '
     '충분</b>하기 때문이다. 칠하지 않고 점만 찍은 것이 그 뜻이다', ['D1-152', 'D1-153']),
    ('★ 왼쪽 판은 맨 윗줄에 검은 현판이 <b>하나도 없다</b> — 오른쪽 판은 맨 윗줄부터 둘이다',
     '상염색체 <b>열성</b>은 매 세대에 나타나지 않고 <b>세대를 건너뛸 수 있다</b>. '
     '보인자를 거쳐 조용히 전해지기 때문이다', ['D1-165']),
    ('★ 오른쪽 판 — 세 줄 <b>모두</b>에 검은 현판이 있어 줄이 끊기지 않는다',
     '상염색체 <b>우성</b>은 대체로 <b>세대마다</b> 나타난다. 그래서 환자 자녀가 있으면 '
     '<b>부모 중 최소 한 명은 환자</b>다', ['D1-166', 'D1-164']),
    ('오른쪽 판에는 귀퉁이 점이 <b>하나도</b> 없다',
     '우성 유전병은 <b>이형접합만으로도 발현</b>한다 — 숨은 보인자가 없다는 뜻이다. '
     '왼쪽 판에만 점이 있는 것이 두 판을 가르는 또 하나의 표시다', ['D1-155']),
    ('왼쪽 판 앞 — 걸상에 올라서서 붓으로 현판 하나를 검게 칠하는 사람',
     '칠하는 것은 <b>겉으로 나타난 것</b>뿐이다. 가계도는 <b>표현형을 적는 표</b>이지 '
     '유전자형을 적는 표가 아니다 — 유전자형은 거기서 거꾸로 추론하는 것이다', []),
    ('오른쪽 판 앞 — 긴 막대로 검은 현판의 줄을 위아래로 짚는 사람',
     '우성 판은 <b>짚어 내려가도 줄이 끊기지 않는다</b>. 판정은 결국 이 한 동작이다 — '
     '세대마다 있으면 우성, 비면 열성', []),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\': j += 2; continue
            if c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s40'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    assert os.path.exists(os.path.join(ROOT, 'img', 's40p01.webp')), '그림이 없다'

    i = src.index("{id:'s39'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s40p01',t:'가문 현판 벽',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    scene = ("{id:'s40',t:'가문 현판 벽',gate:'D',unit:'15 멘델 유전 — 가계도와 유전병',"
             "panels:[\n   " + panel + "\n  ]}")
    out = src[:e + 1] + ',\n {' .replace(' {', '') + scene + src[e + 1:]

    k = out.index("{id:'s40p01'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    cards = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복'
    print('장면 s40 신설 · 패널 %d개 · s40p01 행 %d · 카드 %d장'
          % (len(ids), len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
