#!/usr/bin/env python3
"""s20p06 「같은 밤인데 하나는 피고 하나는 오므린다」 — 장일식물과 플로리겐.

★ 새로 그리기 전에 덱을 먼저 뒤졌다
  s20p02(시계탑)가 「무엇을 재는가」를 이미 그리고 있었다 — 모래시계(연속된 밤 길이),
  손전등(광중단), 피는 줄과 앙상한 줄(단일식물). 그래서 그쪽에 걸 수 있는 넷은
  **그림을 새로 그리지 않고** 걸었다(P1-24·P1-25·P1-30#1·P1-29#1).

  이 판이 맡는 것은 s20p02 에 **없는 것**이다 —
  ① 같은 광중단에 **장일식물은 오히려 핀다**는 반대 반응
  ② **플로리겐** — 잎이 재고 신호가 꼭대기로 올라간다

★ 문법 — 셋째 줄과 넷째 줄이 **똑같은 띠**다
  검은 부분 길이도 같고 섬광 자리도 같은데 하나는 오므리고 하나는 폈다.
  갈린 것은 **어느 식물이냐**뿐이다 — 그것이 이 판의 전부다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ 셋째 줄과 넷째 줄이 <b>똑같은 띠</b>다 — 검은 부분 길이도 같고 섬광 자리도 같다. '
      '그런데 하나는 오므리고 하나는 폈다. 갈린 것은 <b>어느 식물이냐</b>뿐이다. '
      '옆 판(시계탑)이 「무엇을 재는가」라면, 이 판은 <b>잰 결과가 왜 갈리는가</b>다.')

BX = """<p><b>이름은 낮으로 부르는데 정하는 것은 밤이다.</b> 그래서 늘 헷갈린다. <b>단일식물</b>은 낮이 짧을 때 피니 곧 <b>밤이 길어야</b> 핀다 — 다른 이름이 <b>장야식물</b>이고 이쪽이 실제 조건이다. <b>장일식물</b>은 낮이 길 때 피니 <b>밤이 짧아야</b> 한다 — <b>단야식물</b>이다. 문제를 만나면 먼저 <b>밤 이름으로 바꿔 읽는다</b>.</p><p><b>★ 광중단이 왜 정반대로 작용하는가.</b> 긴 밤 한가운데에 섬광을 주면 밤이 <b>두 토막</b>으로 갈린다. 총량은 그대로인데 <b>연속</b>이 끊긴 것이다. 단일식물(장야)에게는 「충분히 긴 밤」이 사라진 셈이라 <b>못 핀다</b>. 장일식물(단야)에게는 「너무 긴 밤」이 없어진 셈이라 <b>오히려 핀다</b>. 같은 처리, 정반대 결과 — 갈리는 것은 <b>무엇을 필요로 하는가</b>다.</p><p><b>같은 띠를 두 번 그린 까닭.</b> 셋째 줄과 넷째 줄은 밤 길이도 섬광 자리도 똑같다. 조건이 같은데 결과가 다르면 원인은 <b>조건이 아니라 식물</b>이다 — 실험을 읽는 방식 그대로다. 시험에서 「같은 광주기를 준 두 식물」이 나오면 이 그림을 떠올리면 된다.</p><p><b>★ 재는 곳과 피는 곳이 다르다.</b> 밤 길이를 재는 것은 <b>잎</b>이다. 그런데 꽃은 줄기 <b>꼭대기</b>에서 핀다. 그래서 잎에서 만들어진 신호가 위로 올라가야 하는데, 그 신호가 <b>플로리겐</b>(FT 단백질)이다. 그림에서 아래 잎에서 뽑아낸 쪽지가 꼭대기 꽃으로 올라가는 것이 그것이다. 잎만 가리고 실험하면 개화가 안 되는 까닭이 이것이다.</p><p><b>예로 외울 것.</b> 단일식물(장야) — <b>국화 · 포인세티아 · 콩</b>, 늦여름·가을·겨울에 핀다. 장일식물(단야) — <b>시금치 · 무 · 상추</b>, 늦봄·초여름에 핀다. 계절과 묶어 두면 헷갈리지 않는다.</p><p><b>기출이 꼬는 자리.</b> ① 「단일식물은 낮이 짧으면 핀다」 → 맞지만 <b>반쪽</b>이다. 정하는 것은 연속된 밤 길이다. ② 「광중단은 모든 식물의 개화를 막는다」 → <b>거짓</b>. 장일식물은 오히려 핀다. ③ 「밤의 총량이 같으면 결과도 같다」 → <b>거짓</b>. <b>연속</b>이 조건이다. ④ 「개화 신호는 꼭대기에서 만들어진다」 → <b>거짓</b>. <b>잎</b>에서 만들어져 올라간다.</p>"""

ROWS = [
    ('줄마다 <b>낮은 옅고 밤은 검다</b> · 오른쪽 끝의 화초가 피거나 오므린다',
     '다른 것은 <b>검은 부분의 길이</b>와 <b>끊겼는가</b>뿐이다. '
     '무엇을 재는지는 옆 판(시계탑의 모래시계)이 그렸고, 이 판은 <b>잰 결과</b>를 그린다', []),

    ('첫 줄 — 검은 부분이 <b>길다</b> · 국화가 <b>활짝 폈다</b>',
     '<b>단일식물</b>(장야식물)은 <b>긴 밤</b>이라야 핀다 — '
     '국화 · 포인세티아 · 콩이 그 예다', ['P1-25']),

    ('둘째 줄 — 검은 부분이 <b>짧다</b> · 같은 국화가 <b>오므렸다</b>',
     '밤이 짧으면 못 핀다. ★ 그래서 단일식물의 다른 이름이 <b>장야식물</b>이고, '
     '이쪽이 <b>실제 조건</b>이다 — 이름은 낮으로 부르는데 정하는 것은 밤이다', ['P1-30#1']),

    ('★ 셋째 줄 — 첫 줄과 <b>똑같이 긴데</b> 한가운데 <b>흰 섬광 하나</b>로 두 토막 났다 · '
     '국화가 <b>오므렸다</b>',
     '<b>광중단</b>. 밤의 <b>총량은 같은데 연속이 끊겼다</b> — 그래서 못 핀다. '
     '「연속된 밤」이 조건이라는 증거가 이 한 줄이다', ['P1-29#1']),

    ('★ 넷째 줄 — 셋째 줄과 <b>같은 띠</b>인데 넓은잎 화초가 <b>활짝 폈다</b>',
     '<b>장일식물</b>은 <b>짧은 밤</b>이라야 피니, 긴 밤이 두 토막 난 것이 <b>오히려 도움</b>이 된다 — '
     '그래서 다른 이름이 <b>단야식물</b>이다. 시금치 · 무 · 상추가 그 예다',
     ['P1-26', 'P1-29#2', 'P1-30#2']),

    ('★ 셋째와 넷째가 <b>같은 밤</b>인데 결과가 <b>정반대</b>다',
     '조건이 같은데 결과가 다르면 원인은 <b>식물</b>이다. '
     '<b>단일 = 장야</b>(긴 밤이 필요) · <b>장일 = 단야</b>(짧은 밤이 필요) — '
     '광중단은 앞엣것을 <b>막고</b> 뒤엣것을 <b>돕는다</b>',
     ['P1-29', 'P1-30']),

    ('★ 첫 줄 — <b>아래 잎</b>께에서 손이 작은 <b>편지</b>를 꺼내 <b>꼭대기 꽃</b>으로 올려 보낸다',
     '<b>플로리겐</b>(FT 단백질). ★ <b>재는 곳과 피는 곳이 다르다</b> — '
     '밤을 재는 것은 <b>잎</b>이고 꽃은 <b>꼭대기</b>에서 핀다. '
     '그래서 신호가 위로 올라가야 한다', ['P1-32']),

    ('섬광이 <b>무슨 빛</b>이었는지는 이 그림에 없다',
     '★ 광중단은 <b>적색광</b>이 효과를 내고 곧이어 <b>근적외선</b>을 주면 <b>취소</b>된다 — '
     '<b>피토크롬</b>이 매개한다. 그 토글은 옆 판(양면 스위치)에 있다', []),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s20p06'" not in src, '이미 있다'
    assert os.path.exists(os.path.join(ROOT, 'img', 's20p06.webp')), '그림이 없다'

    i = src.index("{id:'s20p05'")
    e = close_brace(src, i)
    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')
    panel = ("{id:'s20p06',t:'같은 밤인데 하나는 피고 하나는 오므린다',br:'" + BR
             + "',bx:`" + BX + "`,f:[" + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[a-z]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    b2 = out[out.index("{id:'s20p06'"):]
    b2 = b2[:close_brace(b2, 0) + 1]
    cs = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', b2)
    assert len(cs) == len(set(cs)), '카드 중복: ' + str(cs)
    print('s20p06 신설 · 행 %d · 카드 %d장' % (len(ROWS), len(cs)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
