#!/usr/bin/env python3
"""s20p01c 「갓 쓴 아비가 넷을 누른다」 — ABA 전용 판 (s20p01b 뒤에 붙인다).

★ 왜 세우나 — 이름이 그림에 없었다
  s20p01b(멈추게 하는 셋)의 「잎의 셔터」는 ABA가 *하는 일*(닫는다)은 나르지만
  *이름*은 못 나른다. 음성 후크 감사가 그 한 행에서 5장을 위반으로 세고 있었다.
  게다가 ABA가 하는 일 넷 중 셋(생장 억제·종자 휴면·건조 내성)은 덱에 그림이 아예 없어
  P1-6#1·#3·#4가 통째로 미연결이었다. 합쳐 8장이다.

★ 후크 — 갓 쓴 늙은 아비 = 「아바」 (형태 ①)
  ⚠ 낙엽은 쓰지 않았다. abscisic = 「떨어뜨리는」이라 뜻은 맞지만
     실제 잎 탈리는 에틸렌이 하고, 그 낙엽은 이미 옆 판 s20p01b에 있다 — 심볼 충돌이다.
  ⚠ 자물쇠도 쓰지 않았다. 「금색 자물쇠 = 약물·독물 전용」이 예약돼 있다.
     휴면은 자물쇠가 아니라 「흙에 묻고 손바닥으로 누른다」로 그렸다 —
     옆 판 s20p01a의 「끌로 씨앗 껍질을 깨자 싹이 남」과 정확히 반대 동작이다.

★ 소품 다섯 (규칙 10, 6개 상한 안)
  갓 쓴 늙은 아비 · 잎에 박힌 셔터 · 흙에 묻혀 눌린 씨앗 ·
  납작하게 웅크린 모종 셋 · 갈라진 땅 위에서 빳빳한 잎

★ 통일 동작이 곧 답이다 — 넷이 다 「누르는」 쪽이다.
  딱 하나 건조 내성만 「막는다」가 아니라 「버틴다」로 뒤집힌다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ 온실에 <b>갓 쓴 늙은 아비</b> 하나가 서서 <b>닿는 것마다 멈춘다</b> — '
      '셔터를 끌어내려 닫고, 흙더미를 손바닥으로 눌러 씨앗을 재우고, 모종을 자라다 말게 한다. '
      '★ 그런데 <b>갈라진 땅 위의 화분 하나만은 파랗게 빳빳하다</b> — 멈추는 것이 곧 버티는 힘이다.')

BX = """<p><b>이름부터 잡는다 — 갓 쓴 아비가 「아바」다.</b> 옆 판(멈추게 하는 셋)에는 이 호르몬이 <b>하는 일</b>만 그려져 있었다. 셔터를 내리는 그림은 「닫는다」를 가르치지만 「<b>ABA</b>」라는 이름은 못 가르친다. 그래서 이 판을 따로 세웠다 — 갓 쓴 늙은 아비 하나가 온실을 돌며 닿는 것마다 멈춘다.</p><p><b>하는 일이 넷이고, 넷이 한 방향이다.</b> ① <b>생장 억제</b> — 모종이 키를 못 키우고 납작하게 웅크린다. ② <b>기공 닫기</b> — 잎에 박힌 셔터를 끌어내린다(건조 스트레스). ③ <b>종자 휴면</b> — 씨앗을 흙에 묻고 손바닥으로 눌러 재운다. ④ <b>건조 내성</b> — 땅이 갈라져도 그 화분만 파랗게 버틴다. ★ 앞의 셋은 「<b>막는다</b>」인데 넷째만 「<b>버틴다</b>」다. 이 하나가 뒤집혀 나오므로 방향을 따로 외운다.</p><p><b>휴면과 발아는 정확히 반대 동작으로 그렸다.</b> 옆 판(자라게 하는 셋)에서는 <b>끌로 씨앗 껍질을 깨자 싹이 났다</b> — 그 일을 하는 것이 <b>지베렐린</b>이고 <b>브라시노스테로이드</b>도 거든다. 여기서는 아비가 그 씨앗을 <b>도로 묻고 눌러</b> 싹을 막는다. 「휴면을 거는 쪽과 푸는 쪽」을 묻는 문제는 이 두 그림을 나란히 놓으면 끝난다.</p><p><b>기공은 이 판이 아니라 옆의 두 판이 자세히 그린다.</b> 셔터를 끌어내리는 그림은 <b>멈추게 하는 셋</b>에, 공변세포 한 쌍이 홀쭉해져 붙는 그림은 <b>두 콩팥이 벌리는 틈</b>에 있다. 이 판의 셔터는 「<b>이 아비가 그 일을 한다</b>」를 잇는 자리다 — 갈라진 땅도 그 판의 오른쪽과 같은 신호다.</p><p><b>어디서 만들어지나.</b> 주로 <b>잎</b>에서, 물이 모자랄 때 늘어난다. 뿌리에서도 만들어져 물관을 타고 잎으로 올라간다는 것이 밝혀져 있는데, 어느 쪽이든 <b>「마르면 늘어난다」</b>가 핵심이다. 늘어난 ABA가 공변세포에서 <b>K⁺를 내보내고</b> 물이 따라 나가 세포가 쭈그러들며 기공이 닫힌다. 여는 것은 반대로 K⁺ 유입이다 — 「어느 이온이 어느 방향으로」가 뒤집혀 나오므로 방향까지 확인한다.</p><p><b>기출이 꼬는 자리.</b> ① 「ABA가 기공을 연다」 → <b>거짓</b>. 닫는다. ② 「ABA가 종자 발아를 촉진한다」 → <b>거짓</b>. 휴면을 <b>건다</b>. 푸는 것은 지베렐린·브라시노스테로이드다. ③ 「ABA는 건조에 약하게 만든다」 → <b>거짓</b>. <b>건조 내성</b>을 준다 — 멈추는 것이 곧 버티는 방법이다. ④ 「낙엽을 지게 하는 것이 ABA다」 → 이름(abscisic, 떨어뜨리는)만 보면 그럴듯하지만 <b>실제 잎 탈리는 에틸렌</b>이다. 그래서 이 그림에는 낙엽이 한 장도 없다.</p>"""

ROWS = [
    ('★ 온실 한가운데 <b>갓 쓴 늙은 아비</b> 하나 — <b>원통 관에 챙 넓은 검은 갓</b>, 흰 두루마기, 흰 수염이다',
     '<b>ABA</b>(앱시스산). ★ <b>아바</b> — 갓 쓴 아비가 그 이름이다. '
     '하는 일이 넷인데 넷이 <b>한 방향</b>이다 — 멈추고 · 닫고 · 재우고 · 버틴다',
     ['P1-6']),

    ('아비의 왼손이 줄을 당겨 <b>잎에 박힌 셔터</b>를 끌어내려 <b>닫았다</b>',
     '<b>기공을 닫는다</b> — 마르면 공변세포에서 물을 빼내 쭈그러뜨린다. '
     '★ <b>여는 것이 아니라 닫는다</b> — 이 호르몬 최다 함정이다. '
     '셔터를 끌어내리는 그림은 옆 판(멈추게 하는 셋)에도 있다',
     ['P1-6#2', 'O1-22', 'O1-22#2']),

    ('★ 앞쪽 — 아비가 <b>흙더미에 오른손 손바닥을 얹어 꾹 눌렀다</b> · '
     '흙더미 앞이 잘려 있어 속이 보이는데 <b>갈색 씨앗 하나가 닫힌 채</b> 묻혀 있고 싹은 하나도 없다',
     '<b>종자 휴면</b>을 건다. ★ 옆 판(자라게 하는 셋)에서는 <b>끌로 씨앗 껍질을 깼다</b> — '
     '<b>깨는 것은 지베렐린·브라시노스테로이드</b>이고 <b>묻어 재우는 것이 이 아비</b>다',
     ['P1-6#3', 'X-PL-27']),

    ('오른쪽 화분 셋 — 모종이 <b>자라다 만 채 작고</b>, 오른쪽으로 갈수록 <b>잎이 옆으로 눕는다</b> · 바로 옆 큰 화분과 견주면 키가 넷에 하나도 안 된다',
     '<b>생장을 억제한다</b>. 옆 판에서 자라게 하는 셋(옥신·지베렐린·시토키닌)이 미는 것을 '
     '이쪽이 되민다',
     ['P1-6#1']),

    ('★ 맨 오른쪽 — 바닥이 <b>쩍쩍 갈라진 판</b>으로 쪼개졌는데 그 위에 선 화분의 잎만 <b>짙푸르고 빳빳하게</b> 곧추섰다',
     '<b>건조 내성</b>을 준다. ★ 갈라진 땅은 옆 판(두 콩팥이 벌리는 틈)의 오른쪽과 '
     '<b>같은 신호</b>다 — 마르면 이 아비가 나선다',
     ['P1-6#4']),

    ('★ 아비가 손댄 것은 다 <b>멈췄는데</b> 갈라진 땅 위의 것만 <b>버티고 서 있다</b>',
     '그래서 이 호르몬 문제는 방향만 맞히면 거의 다 풀린다. '
     '★ 딱 하나 <b>건조 내성</b>만 뒤집힌다 — 넷 중 이것만 「막는다」가 아니라 「버틴다」다',
     []),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s20p01c'" not in src, '이미 있다'
    assert os.path.exists(os.path.join(ROOT, 'img', 's20p01c.webp')), '그림이 없다'

    i = src.index("{id:'s20p01b'")
    e = close_brace(src, i)
    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')
    panel = ("{id:'s20p01c',t:'갓 쓴 아비가 넷을 누른다',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[a-z]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    b2 = out[out.index("{id:'s20p01c'"):]
    b2 = b2[:close_brace(b2, 0) + 1]
    cs = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', b2)
    assert len(cs) == len(set(cs)), '카드 중복: ' + str(cs)
    print('s20p01c 신설 · 행 %d · 카드 %d장' % (len(ROWS), len(cs)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
