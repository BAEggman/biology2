#!/usr/bin/env python3
"""s20p05 「끝이 본다」 — 굴광성 고전 실험 (s20p04 뒤에 붙인다).

★ 이 판이 메우는 구멍
  s20p04 가 굴광성의 **기작**(옥신이 그늘 쪽으로)을 그렸다면, 이 판은 그것을
  **어떻게 알아냈는가**를 그린다. 기출에 실험 해석이 단골로 나오는데 덱에 없었다.

★ 문법 — 다섯이 같은 빛을 받는데 갈린 것은 하나씩뿐이다
  ① 그대로            → 굽는다   (대조)
  ② 끝에 검은 고깔     → 안 굽는다
  ③ 줄기에만 검은 띠   → 굽는다   ★ ②와 ③을 나란히 보면 결론이 하나다 — 보는 것은 끝
  ④ 사이에 맑은 젤리   → 굽는다
  ⑤ 사이에 회색 돌판   → 안 굽는다 ★ 통하는 것은 빛이 아니라 물질이다

  이것이 s21p02b·s17p04 와 같은 「있다/없다」 문법이다 — Gemini 가 신설에서는 지킨다.

★ 후크
  파란 유리알 = 포토트로핀 — 끝에 박혀 **청색광**을 받는 자리. 빛줄기도 푸르게 그렸다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ 다섯이 <b>같은 빛</b>을 받는데 셋만 굽었다. 갈린 것은 <b>가린 자리</b>와 '
      '<b>사이에 끼운 것</b>뿐이다 — 끝을 가리면 안 굽고 아래를 가려도 굽으니 '
      '<b>보는 것은 끝</b>이고, 젤리는 통하는데 돌판은 안 통하니 '
      '내려가는 것은 빛이 아니라 <b>물질</b>이다.')

BX = """<p><b>실험 문제는 「무엇 하나만 달랐는가」로 읽는다.</b> 다섯 자엽초가 같은 창에서 같은 빛을 받는다. 크기도 화분도 같다. 다른 것은 딱 하나씩이다 — 그러니 결과가 갈리면 그 하나가 원인이다. 이 판은 그 읽는 법 자체를 그린 것이다.</p><p><b>다윈 부자(1880) — 보는 것은 끝이다.</b> 끝에 <b>검은 고깔</b>을 씌우면 굽지 않는다. 그런데 끝은 드러내고 <b>줄기 가운데에만</b> 띠를 두르면 <b>여전히 굽는다</b>. 둘을 나란히 놓으면 결론이 하나뿐이다 — 빛을 감지하는 자리는 <b>끝</b>이지 굽는 자리가 아니다. ★ <b>감지하는 곳과 굽는 곳이 다르다</b>는 것이 이 실험의 값이고, 그래서 「신호가 내려간다」는 다음 물음이 생긴다.</p><p><b>보이센-옌센(1913) — 내려가는 것은 물질이다.</b> 끝을 잘라 다시 얹되 사이에 무엇을 끼우느냐로 갈린다. <b>맑은 젤리</b>를 끼우면 굽고, <b>돌판</b>을 끼우면 굽지 않는다. 젤리는 빛도 통하고 물질도 통하지만 돌판은 둘 다 막는다 — 그런데 <b>끝이 이미 빛을 받은 뒤</b>이므로 남은 차이는 <b>물질이 지나갈 수 있는가</b>뿐이다. 그래서 끝에서 만들어진 <b>화학 신호</b>가 아래로 내려간다는 것이 밝혀졌다. 그 물질이 나중에 <b>옥신</b>으로 밝혀진다.</p><p><b>왜 푸른 빛인가.</b> 굴광성을 일으키는 것은 <b>청색광</b>이고, 그것을 받는 것이 <b>포토트로핀</b>이다(photo = 빛, tropin = 굽음 — 이름이 곧 기능이다). 그림에서 첫째 자엽초의 <b>끝에 박힌 파란 유리알</b>이 그 자리다. 같은 청색광 수용체 무리에 <b>크립토크롬</b>이 하나 더 있는데, 그쪽은 굽히는 것이 아니라 <b>하배축이 길어지는 것을 막는다</b>.</p><p><b>기출이 꼬는 자리.</b> ① 「빛을 감지하는 곳과 굽는 곳이 같다」 → <b>거짓</b>. 끝이 감지하고 아래가 굽는다. ② 「끝을 잘라도 굽는다」 → <b>거짓</b>. 잘라 버리면 안 굽는다(다시 얹어야 굽는다). ③ 「돌판을 끼워도 굽는 것은 빛이 막혀서다」 → <b>거짓</b>. 끝은 이미 빛을 받았고, 막힌 것은 <b>물질</b>이다. ④ 「굴광성은 적색광이 일으킨다」 → <b>거짓</b>. 청색광이다(적색·근적외는 파이토크롬 쪽이고 옆 판에 있다).</p>"""

ROWS = [
    ('창이 하나뿐이라 빛이 <b>옆에서만</b> 든다 · 그 빛줄기가 <b>푸르다</b>',
     '굴광성을 일으키는 것은 <b>청색광</b>이다. 다섯이 <b>같은 빛</b>을 받는데 결과가 갈린다 — '
     '그러니 갈린 까닭은 빛이 아니다', []),

    ('★ 첫째 — 아무것도 안 했다 · 끝에 <b>파란 유리알</b>이 박혔다 → <b>굽는다</b>',
     '대조군이다. ★ 빛을 받는 자리가 <b>끝</b>이고, 그 <b>청색광</b>을 받는 것이 '
     '<b>포토트로핀</b>이다 — photo(빛) + tropin(굽음), 이름이 곧 기능이다',
     ['P1-18#2']),

    ('★ 둘째 — 끝에 <b>검은 고깔</b>을 씌웠다 → <b>꼿꼿하다</b>',
     '끝을 가리면 <b>안 굽는다</b>. 끝을 아예 잘라 버려도 마찬가지다', []),

    ('★ 셋째 — 끝은 드러내고 <b>줄기 가운데에만 검은 띠</b>를 둘렀다 → <b>굽는다</b>',
     '아래를 가려도 <b>굽는다</b>. ★ 둘째와 셋째를 나란히 보면 결론이 하나뿐이다 — '
     '<b>빛을 보는 것은 끝</b>이지 굽는 자리가 아니다. '
     '감지하는 곳과 굽는 곳이 <b>다르다</b>',
     ['P1-9']),

    ('넷째 — 끝을 잘라 <b>다시 얹고</b> 사이에 <b>맑은 젤리</b>를 끼웠다 → <b>굽는다</b>',
     '잘라 버리면 안 굽지만 <b>다시 얹으면</b> 굽는다 — 끝이 있어야 한다', []),

    ('★ 다섯째 — 같은데 사이에 <b>회색 돌판</b>을 끼웠다 → <b>꼿꼿하다</b>',
     '젤리는 통하고 돌판은 안 통한다. 끝은 <b>이미 빛을 받은 뒤</b>이므로 남은 차이는 '
     '<b>물질이 지나갈 수 있는가</b>뿐이다 — 내려가는 것은 빛이 아니라 '
     '<b>화학 신호</b>다. 그것이 나중에 <b>옥신</b>으로 밝혀졌다',
     ['P1-10']),

    ('★ 다섯이 <b>같은 빛</b>을 받는데 <b>셋만</b> 굽었다',
     '갈린 것은 <b>가린 자리</b>와 <b>끼운 것</b>뿐이다. '
     '실험 문제는 언제나 이렇게 읽는다 — <b>무엇 하나만 달랐는가</b>', []),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s20p05'" not in src, '이미 있다'
    assert os.path.exists(os.path.join(ROOT, 'img', 's20p05.webp')), '그림이 없다'

    i = src.index("{id:'s20p04'")
    e = close_brace(src, i)
    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')
    panel = ("{id:'s20p05',t:'끝이 본다',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[a-z]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    b2 = out[out.index("{id:'s20p05'"):]
    b2 = b2[:close_brace(b2, 0) + 1]
    cs = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', b2)
    assert len(cs) == len(set(cs)), '카드 중복: ' + str(cs)
    print('s20p05 신설 · 행 %d · 카드 %d장' % (len(ROWS), len(cs)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
