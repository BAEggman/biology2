#!/usr/bin/env python3
"""s39 「거친 집과 매끄러운 집」 신규 장면 + s39p01 「네 집과 쥐」 — 그리피스·에이버리.

왜 이 자리인가
  E1-0~11이 통째로 미연결이고 전부 「유전물질이 무엇인가」를 밝힌 실험들이다.
  그리피스 문항은 언제나 **네 조합 중 어느 것에서 쥐가 죽었나**를 묻고,
  에이버리 문항은 **무엇을 없앴을 때 형질전환이 멈췄나**를 묻는다.

★ 이름이 곧 형태다
  R형 = rough = **거친 벽** = 비병원성 / S형 = smooth = **매끄러운 벽** = 병원성
  이 하나만 붙여 두면 R·S를 뒤집어 놓은 선지가 통하지 않는다.

무엇을 그렸나 — 집 넷이 한 줄, 발치마다 쥐 한 마리
  첫째  매끄러운 집 · 쥐가 누움              → 살아있는 S형 → 죽는다
  둘째  거친 집 · 쥐가 섬                    → 살아있는 R형 → 산다
  셋째  불타 무너진 잔해(매끄러운 조각) · 쥐가 섬 → 열처리로 죽인 S형 → 산다
  넷째  잔해 + 거친 집이 나란히, 잔해에서 나온 실이 거친 벽을 타고 올라
        벽 위쪽 절반이 이미 매끄럽게 변했다 · 쥐가 누움
                                             → 죽은 S형 + 산 R형 → 죽는다 · **형질전환**
  오른쪽 아래 가위가 그 실을 싹둑 잘랐고 그 곁의 쥐는 서 있다
                                             → DNA를 없애면 형질전환이 안 일어난다(에이버리)

  ★ 실이 곧 DNA다. 실이 건너가면 벽이 바뀌고, 실을 자르면 아무 일도 안 일어난다.

⚠ 그리지 않은 것은 걸지 않았다 — 20세기 초에 단백질이 유력했던 이유(E1-0),
  허시-체이스(E1-7~11)는 다음 패널로 남긴다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

SCENE_T = '거친 집과 매끄러운 집'
GATE = 'E'
UNIT = '16 핵산과 유전 — 유전물질을 찾은 실험'

BR = ('★ <b>이름이 곧 형태다</b> — R형은 <b>rough</b>, 거친 벽이고 <b>비병원성</b>. '
      'S형은 <b>smooth</b>, 매끄러운 벽이고 <b>병원성</b>이다. 그리고 <b>실이 곧 DNA</b>다 — '
      '실이 건너가면 거친 벽이 매끄럽게 바뀌고(<b>형질전환</b>), 실을 자르면 아무 일도 안 일어난다.')

BX = """<p><b>이름이 곧 형태다.</b> <b>R형은 rough</b>, 거친 벽이다 — 집락이 거칠고 <b>병원성이 없다</b>. <b>S형은 smooth</b>, 매끄러운 벽이다 — 집락이 매끄럽고 <b>병원성이 있다</b>. 이 하나만 붙여 두면 R과 S를 뒤집어 놓은 선지는 통하지 않는다. (S형이 매끄러운 것은 다당류 껍질을 쓰고 있어서이고, 그 껍질이 곧 병원성이다.)</p><p><b>네 조합이 그대로 문항이다.</b> ① 매끄러운 집 → 쥐가 <b>죽는다</b>. ② 거친 집 → 쥐가 <b>산다</b>. ③ 불타 무너진 잔해(열처리로 <b>죽인 S형</b>) → 쥐가 <b>산다</b>. ④ 잔해 + 거친 집을 함께 주면 → 쥐가 <b>죽는다</b>. ①~③만 보면 당연한데 ④가 이상하다 — 죽은 것과 병 없는 것을 합쳤는데 쥐가 죽었다.</p><p><b>그리고 살아 있는 S형이 나온다.</b> 그림에서 거친 벽의 <b>위쪽 절반이 이미 매끄럽게 변해</b> 있다. 죽은 S형에서 나온 무언가가 살아 있는 R형에 들어가 <b>R을 S로 바꿔 놓은</b> 것이다. 이것이 <b>형질전환(transformation)</b> — 외부 DNA가 세포 안으로 들어가 형질이 바뀌는 현상이다. 그리피스는 그 「무언가」를 <b>형질전환 물질</b>이라고만 불렀다.</p><p><b>그 무언가가 무엇인지는 에이버리가 답했다.</b> 오른쪽 아래에서 <b>가위가 실을 잘랐고</b>, 그러자 곁의 쥐는 <b>서 있다</b>. 에이버리·매클라우드·매카티는 죽은 S형의 추출물에서 단백질·RNA·DNA를 각각 없애 보았는데, <b>DNA 분해효소를 처리했을 때만 형질전환이 멈췄다</b>. 곧 형질전환 물질은 <b>단백질이 아니라 DNA</b>다.</p><p><b>실이 곧 DNA다.</b> 이 그림에서 실은 잔해에서 나와 벽을 타고 올라가 벽의 성질을 바꾼다. 실이 건너가면 형질이 바뀌고, 실을 자르면 아무 일도 일어나지 않는다 — 두 실험이 한 소품으로 이어진다.</p><p><b>기출이 꼬는 자리.</b> ① 「R형이 병원성이다」 → <b>거짓</b>(거칠고 병원성이 없다). ② 「열처리로 죽인 S형만 주입하면 쥐가 죽는다」 → <b>거짓</b>(산다). ③ 「형질전환은 파지가 옮긴다」 → <b>거짓</b>(그것은 <b>형질도입</b>이다. 형질전환은 벌거벗은 외부 DNA를 직접 받아들이는 것). ④ 「에이버리는 단백질을 없앴을 때 형질전환이 멈추는 것을 보았다」 → <b>거짓</b>(DNA를 없앴을 때다).</p>"""

ROWS = [
    ('벽이 두 가지뿐이다 — 우툴두툴 거친 벽과 반들반들 매끄러운 벽',
     '<b>R형 = rough = 거친 집락 = 비병원성</b> / <b>S형 = smooth = 매끄러운 집락 = 병원성</b>. '
     '이름이 곧 형태다',
     ['E1-2', 'E1-2#1', 'E1-2#2']),
    ('첫째 — 매끄러운 집 앞의 쥐가 누워 있다',
     '살아있는 <b>S형</b>을 주입하면 쥐가 <b>죽는다</b>',
     None),
    ('둘째 — 거친 집 앞의 쥐가 서 있다',
     '살아있는 <b>R형</b>을 주입하면 쥐가 <b>산다</b>',
     None),
    ('셋째 — 불타 무너진 잔해(무너진 조각이 매끄러운 것들이다) 앞의 쥐가 서 있다',
     '<b>열처리로 죽인 S형</b>만 주입하면 쥐가 <b>산다</b>',
     None),
    ('넷째 — 잔해와 거친 집을 나란히 두었더니 쥐가 누워 있다',
     '<b>죽은 S형 + 살아있는 R형</b>을 함께 주입하면 쥐가 <b>죽는다</b>. 앞의 셋만 보면 '
     '일어날 수 없는 일이라 이 칸이 실험의 전부다',
     ['E1-3', 'E1-1']),
    ('잔해에서 나온 실이 거친 벽을 타고 오르고, 벽 위쪽 절반이 이미 매끄럽게 변했다',
     '<b>형질전환</b> — 외부 DNA가 세포 안으로 들어가 <b>형질이 바뀌는</b> 현상이다. '
     '그래서 죽은 쥐에서 <b>살아 있는 S형</b>이 검출된다',
     ['E1-4']),
    ('오른쪽 아래 — 가위가 그 실을 싹둑 잘랐고, 곁의 쥐는 서 있다',
     '<b>에이버리·매클라우드·매카티</b>. 여러 성분을 하나씩 없애 보았는데 '
     '<b>DNA 분해효소를 처리했을 때만 형질전환이 멈췄다</b>',
     ['E1-6']),
    ('자른 것이 실이지 벽돌이나 기와가 아니다',
     '형질전환 물질은 <b>단백질이 아니라 DNA</b>다',
     ['E1-5']),
]

PC = []


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s39'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR and "'" not in SCENE_T

    i = src.index("{id:'s38'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s39p01',t:'네 집과 쥐',br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n   '.join(rows) + ']}')
    scene = ("{id:'s39',t:'" + SCENE_T + "',gate:'" + GATE + "',unit:'" + UNIT
             + "',panels:[\n  " + panel + '\n ]}')
    out = src[:e + 1] + ',\n ' + scene + src[e + 1:]

    k = out.index("{id:'s39p01'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    sids = re.findall(r"\{id:'(s\d+)',t:", out)
    assert len(sids) == len(set(sids)), '장면 id 중복'
    assert sids.index('s39') == sids.index('s38') + 1, '장면 위치 오류'
    cards = re.findall(r'"([A-Z][\w-]*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복: ' + str(cards)
    print('장면 %d개 · 패널 %d개 · s39p01 행 %d · 카드 %d장'
          % (len(sids), len(ids), len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
