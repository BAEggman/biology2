#!/usr/bin/env python3
"""s11p01b 「이름이 붙는 자리」 — 대장균 복제 개시 넷의 이름 (s11p01 뒤에 붙인다).

★ 이 판이 메우는 구멍
  s11p01(분기점)은 넷이 **무슨 일을 하는지**는 완벽히 그린다 —
  분필로 동그라미(복제기점 인식) · 지퍼 끌기(헬리케이스) · 쐐기(붙잡기) · 프라이팬(프라이메이스).
  그런데 **이름**을 나르는 것이 하나도 없어서 음성 후크 감사에 s11p01 이 11장으로 최다였다.
  DnaA·DnaB·DnaG 는 앞의 「Dna」가 같고 **갈리는 것은 뒤의 한 글자뿐**이라,
  그 한 글자만 물건으로 잡으면 된다.

★ 후크 — 한국어로 읽은 글자 소리
  에이스 카드 = A (에이)        · 빗자루(비) = B (비)        · 지팡이 = G (지)
  ★ 가운데 낀 SSB 만 Dna 가 아니다 — **사람이 둘(SS)** 이고 **쐐기로 붙잡는(B)** 것이 이름이다.

★ 왜 편집이 아니라 신설인가
  s11p01 은 소품이 열이 넘는 큰 그림이고, 세 사람에게 각각 다른 물건을 나중에 쥐여 주는 것은
  Gemini 가 가장 못 하는 일이다(2026-08-19 s21p02 편집 실패). 이름만 다루는 작은 판을
  따로 세우고, 카드는 **두 판에 배열로** 건다 — 앱이 「일하는 그림」과 「이름 그림」을
  차례로 함께 보여 준다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ 셋은 앞에 <b>Dna</b>가 똑같이 붙고 <b>갈리는 것은 뒤의 한 글자</b>뿐이다. '
      '그래서 물건도 한 글자만 나른다 — <b>에이스</b>가 A, <b>비(빗자루)</b>가 B, '
      '<b>지팡이</b>가 G다. 가운데 낀 <b>SSB</b>만 Dna가 아닌데, '
      '<b>사람이 둘</b>인 것이 그대로 이름이다.')

BX = """<p><b>이 판은 일이 아니라 이름만 다룬다.</b> 넷이 무슨 일을 하는지는 앞 판(분기점)이 이미 그렸다 — 분필로 동그라미를 그리고, 지퍼를 끌고, 쐐기를 박고, 프라이팬에서 못을 꺼낸다. 여기서는 그 넷이 <b>대장균에서 뭐라고 불리는가</b>만 잡는다.</p><p><b>이름의 대부분은 이미 뜻이다.</b> DnaA·DnaB·DnaG는 앞의 <b>Dna</b>가 똑같다 — 후크가 필요 없다. 갈리는 것은 <b>뒤의 한 글자</b>뿐이라, 물건도 한 글자만 나르면 된다. 카드의 <b>에이스</b>가 A, 어깨에 멘 <b>비</b>가 B, 짚은 <b>지팡이</b>가 G다. 셋 다 한국어로 글자를 읽은 소리다.</p><p><b>★ 가운데 하나만 이름이 다르다.</b> SSB는 Dna로 시작하지 않는다. 그래서 그림에서도 혼자 <b>둘</b>이다 — 사람이 둘인 것이 <b>단일가닥 둘</b>(SS)이고, 둘이 함께 쐐기를 잡은 것이 <b>붙잡는다</b>(B)이다. 이름이 다르니 생김새도 다르게 세운 것이고, 「넷 중 하나만 Dna가 아니다」가 시험이 노리는 자리다.</p><p><b>★ 순서는 외우는 것이 아니라 일의 앞뒤다.</b> 자리를 <b>알아보고</b>(DnaA가 복제기점을 찾는다) → <b>벌리고</b>(DnaB가 헬리케이스로 푼다) → 벌어진 것을 <b>붙잡고</b>(SSB가 다시 안 붙게 잡는다) → 그제야 <b>못을 놓을 자리</b>가 생긴다(DnaG가 프라이머를 놓는다). 앞의 일이 끝나야 뒤의 일이 가능하므로 순서가 뒤집힐 수 없다.</p><p><b>왜 DnaA가 첫 번째인가.</b> 복제기점(oriC)은 <b>AT가 많은</b> 자리다. AT는 수소결합이 <b>둘</b>뿐이라(GC는 셋) 약해서 거기부터 벌어진다. DnaA는 그 자리를 알아보고 <b>여기서부터 열라</b>고 표시할 뿐, 스스로 풀지는 않는다. 푸는 것은 DnaB다 — 「DnaA가 이중나선을 푼다」로 쓰면 틀린다.</p><p><b>기출이 꼬는 자리.</b> ① 「DnaB가 복제기점을 인식한다」 → <b>거짓</b>. 인식은 DnaA다. ② 「SSB도 Dna 단백질이다」 → <b>거짓</b>. 넷 중 이것만 다르다. ③ 「DnaG가 DNA 프라이머를 만든다」 → <b>거짓</b>. <b>RNA</b> 프라이머다. ④ 「SSB가 가닥을 잘라 벌린다」 → <b>거짓</b>. 벌리는 것은 DnaB이고 SSB는 벌어진 것을 <b>붙잡기만</b> 한다.</p>"""

ROWS = [
    ('맨 왼쪽 — <b>닫힌 지퍼</b>가 아직 그대로다 · 다섯이 그 앞에 <b>한 줄로</b> 섰다',
     '대장균 복제 <b>개시</b>의 차례다. <b>왼쪽부터가 순서</b>이고, 앞의 일이 끝나야 '
     '뒤의 일이 가능하므로 뒤집힐 수 없다',
     ['E1-119']),

    ('★ 첫째 — 지퍼에 손을 대고 <b>에이스 카드</b>를 높이 들었다',
     '<b>DnaA</b>. 「<b>에이스</b>」가 곧 <b>A</b>다. 복제기점을 알아보고 '
     '<b>여기서부터 열라고 표시</b>할 뿐, 스스로 풀지는 않는다',
     ['E1-119#1']),

    ('★ 둘째 — 어깨에 <b>큰 빗자루(비)</b>를 멨다',
     '<b>DnaB</b>. 「<b>비</b>」가 곧 <b>B</b>다. 이 사람이 <b>헬리케이스</b>로 '
     '지퍼를 끌어 두 가닥을 벌린다 — 푸는 것은 첫째가 아니라 이 사람이다',
     ['E1-119#2']),

    ('★ 가운데 자리만 <b>둘이 함께</b> — 나란히 붙어 서서 긴 <b>나무 쐐기 하나</b>를 같이 들었다',
     '<b>SSB</b>. 넷 중 <b>이것만 Dna가 아니다</b>. 사람이 <b>둘(SS)</b>인 것이 갈라진 '
     '<b>단일가닥 둘</b>이고, 쐐기로 <b>붙잡는</b>(B) 것이 이름이다 — 벌리지 않고 붙잡기만 한다',
     ['E1-119#3']),

    ('★ 넷째 — <b>지팡이</b>를 짚었다',
     '<b>DnaG</b>. 「<b>지</b>」가 곧 <b>G</b>다. <b>프라이메이스</b>로 못을 놓아 시작점을 만든다 — '
     '놓는 것은 DNA가 아니라 <b>RNA</b> 프라이머다',
     ['E1-119#4']),

    ('★ 셋은 앞에 <b>Dna</b>가 똑같이 붙는다 — 갈리는 것은 <b>뒤의 한 글자</b>뿐이다',
     '그래서 물건도 한 글자만 나른다 — <b>에이스(A) · 비(B) · 지팡이(G)</b>. '
     '가운데 낀 SSB만 이름이 다르고, 그래서 혼자 <b>둘</b>로 서 있다', []),

    ('★ 순서에는 까닭이 있다 — 아직 <b>지퍼가 닫혀 있다</b>',
     '자리를 <b>알아보고</b>(A) → <b>벌리고</b>(B) → 벌어진 것을 <b>붙잡고</b>(SSB) → '
     '그제야 못을 놓을 자리가 생긴다(G). 외우지 말고 <b>일의 앞뒤</b>로 풀면 된다', []),
]

# 앞 판의 br 에 「이름은 옆 판이 나른다」 한 줄을 보탠다
BR_ADD = (' ★ 이 사람들의 <b>이름</b>은 옆 판이 나른다 — <b>에이스 카드</b>가 DnaA, '
          '<b>빗자루(비)</b>가 DnaB, <b>지팡이</b>가 DnaG이고, 쐐기를 든 <b>두 사람</b>이 SSB다.')


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s11p01b'" not in src, '이미 있다'
    assert os.path.exists(os.path.join(ROOT, 'img', 's11p01b.webp')), '그림이 없다'

    i = src.index("{id:'s11p01'")
    e = close_brace(src, i)
    blk = src[i:e + 1]

    # ① 앞 판 br 보강 (음성 감사가 패널 글 전체를 본다 — 이름 후크를 여기서도 잡아 준다)
    m = re.search(r"br:'(.*?)',bx", blk, re.S)
    assert m and BR_ADD not in blk
    blk = blk[:m.end(1)] + BR_ADD + blk[m.end(1):]
    src = src[:i] + blk + src[e + 1:]
    e = i + len(blk) - 1

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')
    panel = ("{id:'s11p01b',t:'이름이 붙는 자리',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    b2 = out[out.index("{id:'s11p01b'"):]
    b2 = b2[:close_brace(b2, 0) + 1]
    cs = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', b2)
    assert len(cs) == len(set(cs)), '카드 중복: ' + str(cs)
    print('s11p01b 신설 · 행 %d · 카드 %d장(전부 배열) · s11p01 br 보강' % (len(ROWS), len(cs)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
