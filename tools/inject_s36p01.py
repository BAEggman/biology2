#!/usr/bin/env python3
"""s36 「물가에서 언덕까지」 신규 장면 + s36p01 「누가 업고 있나」.

왜 이 자리인가
  게이트 M(식물 다양성) 207장 중 걸린 것이 41장뿐이고, 그 미연결의 대부분이
  ch29 육상식물(55장) · ch30 종자식물(64장) 두 대역에 몰려 있다.
  ch30은 s34 「씨앗 공방」에 이미 그림이 있는 것을 3차 훑기로 20장 걸었고,
  남은 것은 **그림이 아예 없는 축**이다. 그 축의 첫째가 이것이다.

  기본서의 「시험에서 자주 꼬는 지점」이 이 패널의 설계도다 —
    · 선태식물 = 배우체 우세  vs  관속식물 = 포자체 우세
    · 종자식물 배우체는 미세·포자체 의존 (포자체 우세의 심화)
    · 정자가 헤엄치려면 물이 필요(선태·양치) ↔ 종자식물은 화분관
    · 관속조직 유무가 관속/비관속을 가른다

  즉 기출은 표를 통째로 묻지 않는다. **「누가 크냐」와 「누가 누구에게 붙어 있냐」**를 묻는다.
  그래서 그림의 본체는 표가 아니라 **세 집의 자세 — 업기 / 따로 서기 / 주머니에 넣기**다.

무엇을 그렸나
  자루 개수  = 배수성. 한 개면 n, 두 개면 2n.
               (s34p04에서 「자루가 셋인 것」이 배젖 3n이었다 — 그 자루를 그대로 쓴다)
  키         = 우세 세대. 큰 쪽이 그 식물군의 주된 세대다.
  자세       = 의존 관계. 업힘(영양 의존) / 따로 섬(독립) / 주머니 안(미세·의존)
  물웅덩이   = 수정에 물이 필요하다. 연과 줄 = 화분과 화분관(물이 필요 없다)
  집 높이    = 관속조직. 납작한 오두막(비관속) / 대나무 기둥 위(관속)
  땅 겉의 실 = 헛뿌리 — 파고들지 않는다(부착용, 흡수 약함)

⚠ 그리지 않은 것은 걸지 않았다 — 세대교번 5단계·감수분열/체세포분열(M0-8~11),
  장정기·장란기(M0-16), 소엽/대엽(M0-21)은 소품이 없어 다음 패널로 남긴다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

SCENE_T = '물가에서 언덕까지'
GATE = 'M'
UNIT = '29·30 식물의 육상 진출과 종자식물'

BR = ('세 집이 왼쪽 물가에서 오른쪽 언덕 위로 나란히 섰다. <b>멘 자루가 한 개면 n</b>, '
      '두 개면 2n이고 <b>큰 쪽이 우세 세대</b>다. 자세가 답이다 — '
      '<b>업기</b>(선태) → <b>따로 서기</b>(양치) → <b>주머니에 넣기</b>(종자식물).')

BX = """<p><b>표를 외우지 말고 자세를 보라.</b> 이 단원에서 기출이 실제로 묻는 것은 「~식물의 우세 세대는?」 한 형태다. 세 집이 그 답을 자세로 들고 있다 — 왼쪽은 <b>업고</b> 있고, 가운데는 <b>따로 서</b> 있고, 오른쪽은 <b>주머니에 넣고</b> 있다.</p><p><b>자루가 곧 배수성이다.</b> 한 개면 n(배우체), 두 개면 2n(포자체). 이 표식은 새로 만든 것이 아니다 — 씨앗 공방 종자 창고에서 <b>자루가 셋이던 것이 배젖 3n</b>이었다. 그러니 여기서도 세는 것은 사람이 아니라 <b>자루</b>다. 그리고 <b>큰 쪽이 그 식물군의 주된 세대</b>다.</p><p><b>왼쪽 — 선태식물(우산이끼·이끼·뿔이끼).</b> 자루 한 개(n)를 멘 큰 사람이 자루 두 개(2n)를 멘 작은 사람을 <b>업고</b> 있다. 곧 <b>배우체 우세</b>이고 <b>포자체가 배우체에 영양을 의존</b>한다 — 관속식물과 정확히 반대다. 집이 납작한 것은 관속조직이 없어 키가 못 자라기 때문이고(보통 5 cm 미만), 땅 겉에만 붙은 실은 뿌리가 아니라 <b>헛뿌리</b>다(부착용, 흡수는 약하다).</p><p><b>가운데 — 비종자 관속식물(석송류·양치류).</b> 자루 두 개(2n)를 멘 큰 사람이 주인이니 <b>포자체 우세</b>다. 그런데 작은 배우체가 <b>업히지 않고 제 발로 서서 제 밥그릇을 들고</b> 있다 — 배우체가 작아졌지만 아직 <b>독립</b>해서 스스로 광합성하며 산다. 이 「따로 서기」가 왼쪽·오른쪽 어느 쪽과도 갈리는 자리다.</p><p><b>오른쪽 — 종자식물.</b> 큰 사람은 여전히 자루 두 개(2n)이니 포자체 우세는 같다. 달라진 것은 배우체다 — <b>콩알만 하게 줄어</b> 포자체의 <b>가슴주머니 안</b>에 들어앉았다. 곧 <b>배우체가 미세해지고 포자체 조직에 영양을 의존</b>한다. 모체 안에 있으니 자외선·건조에서도 보호받는다. 배우체 축소의 흐름은 여기서 끝난다.</p><p><b>물이 있느냐 없느냐.</b> 왼쪽·가운데 집 앞에는 웅덩이가 있고 그 안에 헤엄치는 사람이 하나씩 있다 — <b>선태·양치는 정자가 물 속을 헤엄쳐</b> 난자에게 가야 해서 <b>수정에 물이 필요</b>하다. 오른쪽 땅은 말라 갈라져 있고 대신 <b>연 하나가 바람에 떠</b> 있으며 그 <b>줄이 가슴주머니까지</b> 내려온다 — 화분이 바람·동물로 운반되고 <b>화분관</b>이 정자를 직접 배우체에게 보낸다. 그래서 <b>물이 필요 없다</b>. 「정자가 헤엄치는 쪽은 누구인가」는 이 두 웅덩이로 답한다.</p><p><b>기출이 꼬는 자리.</b> ① 「선태식물의 포자체는 독립생활을 한다」 → 업혀 있으니 <b>거짓</b>. ② 「관속식물의 배우체는 없다」 → 가운데에도 오른쪽에도 작은 사람이 <b>있다</b>. 작아진 것이지 없어진 것이 아니다. ③ 「종자식물의 배우체는 독립생활을 한다」 → 주머니 안이니 <b>거짓</b>(양치와 갈리는 자리다). ④ 「양치식물은 수정에 물이 필요 없다」 → 웅덩이가 있으니 <b>거짓</b>. 물이 필요 없어지는 것은 <b>화분이 생긴 뒤</b>다.</p><p><b>그림에서 뺀 것 (소품이 없어 걸지 않았다).</b> 세대교번 5단계와 배우자=체세포분열·포자=감수분열, 장정기·장란기, 소엽/대엽. 다음 패널에서 그린다.</p>"""

ROWS = [
    ('세 집이 왼쪽 물가에서 오른쪽 언덕 위로 나란히',
     '왼쪽이 <b>선태식물</b>, 가운데가 <b>비종자 관속식물</b>(석송류·양치류), 오른쪽이 '
     '<b>종자식물</b>이다. 오른쪽으로 갈수록 물에서 멀어진다 — 이 줄이 곧 육상 진출의 순서다',
     None),
    ('어깨에 멘 자루 — 한 개면 n, 두 개면 2n. 이 그림의 유일한 표식이다',
     '배우체는 <b>n</b>, 포자체는 <b>2n</b>이다. 씨앗 공방 종자 창고에서 자루가 셋이던 것이 '
     '배젖 <b>3n</b>이었다 — 같은 자루다',
     None),
    ('왼쪽 — 자루 한 개를 멘 큰 사람이 자루 두 개를 멘 작은 사람을 업고 있다',
     '선태식물은 <b>배우체(n)가 우세</b>하고 <b>포자체(2n)가 배우체에 영양을 의존</b>한다. '
     '업힌 쪽이 자루 두 개인 것이 곧 「포자체가 얹혀 있다」다',
     ['M0-14', 'M1-3#1']),
    ('가운데 — 자루 두 개를 멘 큰 사람과 자루 한 개를 멘 작은 사람이 닿지 않고 나란히 서고, '
     '작은 사람은 제 밥그릇을 들었다',
     '관속식물은 <b>포자체(2n)가 우세</b>하다. 그런데 비종자 관속식물의 배우체는 작아졌을 뿐 '
     '<b>독립</b>해서 스스로 광합성하며 산다 — 업히지도, 주머니에 들어가지도 않는다',
     ['M0-18', 'M1-3#2']),
    ('오른쪽 — 자루 두 개를 멘 큰 사람의 가슴주머니 안에 콩알만 한 사람이 들어앉았다',
     '종자식물은 포자체 우세에 더해 <b>배우체가 미세</b>해지고 <b>포자체 조직에 영양을 의존</b>한다. '
     '모체 포자낭 안이라 자외선·건조에서도 보호받는다',
     ['M1-2', 'M1-3#3']),
    ('업기 → 따로 서기 → 주머니에 넣기, 세 집이 한 줄로',
     '<b>배우체 축소</b>의 흐름 그 자체다. 「누가 크냐」와 「누가 누구에게 붙어 있냐」 둘을 '
     '한꺼번에 묻는 것이 이 단원 기출의 형태다',
     ['M1-3']),
    ('왼쪽·가운데 집 앞에는 웅덩이가 하나씩 있고 그 안에 헤엄치는 사람이 하나씩',
     '선태·양치는 <b>정자가 물 속을 헤엄쳐</b> 난자에게 가야 한다 — <b>수정에 물이 필요</b>하다',
     ['M0-17']),
    ('오른쪽 땅은 말라 갈라졌고, 하늘의 연에서 내려온 줄이 가슴주머니까지 닿는다',
     '종자식물은 <b>화분</b>이 바람·동물로 운반되고(연) <b>화분관</b>이 정자를 배우체에게 직접 '
     '전달한다(줄) — 그래서 <b>수정에 물이 필요 없다</b>',
     ['M1-9', 'X-PL-15']),
    ('왼쪽 집만 지붕이 땅에 닿을 만큼 납작하고, 집 아래 실이 땅 겉에만 붙어 있다',
     '땅에 붙은 실은 뿌리가 아니라 <b>헛뿌리</b>다 — 부착용이고 흡수 기능은 약하다',
     ['M0-15']),
    ('가운데·오른쪽 집만 속 빈 대나무 기둥 위에 서서 키가 크다',
     '<b>관속조직</b>(목부·사부)이 있으면 키가 커진다. <b>관속조직의 유무</b>가 관속식물과 '
     '비관속식물(선태식물)을 가르는 기준이다',
     ['M0-12']),
]

PC = []


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s36'" not in src, '이미 있다'
    assert "{id:'s36p01'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR and "'" not in SCENE_T

    i = src.index("{id:'s35'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s36p01',t:'누가 업고 있나',br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n   '.join(rows) + ']}')
    scene = ("{id:'s36',t:'" + SCENE_T + "',gate:'" + GATE + "',unit:'" + UNIT
             + "',panels:[\n  " + panel + '\n ]}')
    out = src[:e + 1] + ',\n ' + scene + src[e + 1:]

    k = out.index("{id:'s36p01'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    sids = re.findall(r"\{id:'(s\d+)',t:", out)
    assert len(sids) == len(set(sids)), '장면 id 중복'
    assert sids.index('s36') == sids.index('s35') + 1, '장면 위치 오류'
    cards = re.findall(r'"([A-Z][\w-]*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복: ' + str(cards)
    print('장면 %d개 · 패널 %d개 · s36p01 행 %d · 카드 %d장'
          % (len(sids), len(ids), len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
