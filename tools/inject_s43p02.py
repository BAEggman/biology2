#!/usr/bin/env python3
"""s43p02 「담장 안 손질대」 — 진핵 RNA 가공 (s43에 둘째 패널로 붙인다).

★ 이 판의 핵심은 엑손과 인트론을 **공간으로** 가르는 것이다
    청록 띠는 이어 붙여져 담장 구멍으로 **나간다**   → 엑손 (ex-)
    회색 토막은 올가미로 묶여 담장 **안** 통에 남는다 → 인트론 (in-)
  이름이 곧 행선지다. 시험이 둘을 바꿔 놓는 선지를 계속 내는데,
  지금까지 덱에는 그 구분을 나르는 소품이 하나도 없었다.

★ 나머지 후크
    캡 = 모자 (소리 그대로)          poly-A = 똑같은 술 여러 가닥 (poly=여럿)
    스플라이소좀 = 다섯이 팔짱 껴 한 덩어리 (snRNP 5종)
    라리아트 = 올가미 매듭 (lariat=올가미, 뜻 그대로)
    잘린 토막에 모자도 술도 없다 = 인트론이 빨리 분해되는 이유

★ 실측 (2600×1418, 작업대 위 가로 훑기)
    회색·청록·회색·청록·회색·청록 이 번갈아 있고, 오른쪽 이어 붙인 구간은
    흰 이음매로 나뉜 **청록만** 셋이다. 개수는 못 박지 않고 「번갈아」로 적는다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('담장 안에서 하는 손질은 <b>셋</b>이다 — 앞머리에 <b>모자</b>, 꼬리에 <b>술</b>, '
      '가운데를 <b>잘라 잇기</b>. '
      '★ 청록은 이어져 <b>나가고</b>(엑손), 회색은 올가미로 묶여 <b>안에 남는다</b>(인트론) — '
      '이름이 곧 행선지다.')

BX = """<p><b>★ 엑손과 인트론은 이름이 곧 행선지다.</b> 이 그림에서 청록 띠는 이어 붙여져 담장 구멍으로 <b>나가고</b>(ex-), 잘린 회색 토막은 통에 담겨 담장 <b>안에 남는다</b>(in-). 시험은 둘을 바꿔 놓은 선지를 계속 내는데, 어느 쪽이 최종 mRNA에 남느냐만 정하면 나머지는 따라온다. <b>남는 쪽이 청록, 나가는 쪽이 청록</b>이다.</p><p><b>손질은 셋이고 순서보다 자리로 외운다.</b> <b>앞머리</b>에 모자(5′ 캡), <b>꼬리</b>에 술(poly-A tail), <b>가운데</b>를 잘라 잇기(스플라이싱). 캡은 전사가 시작되고 얼마 안 돼 바로 붙고, 하는 일이 둘이다 — <b>분해를 막는 것</b>과 밖에서 읽는 쪽이 <b>먼저 잡는 손잡이</b>가 되는 것. 진핵 번역 개시에서 작은 소단위체가 맨 먼저 붙는 자리가 이 모자다. 꼬리의 술이 여러 가닥인 것이 <b>poly</b>이고, 안정성을 높이고 <b>핵 밖으로 나가는 것</b>과 번역을 돕는다.</p><p><b>가운데의 다섯은 왜 붙어 있나.</b> 스플라이싱을 하는 것은 한 사람이 아니라 <b>스플라이소좀</b>이라는 한 덩어리다. 단백질과 작은 RNA가 모인 복합체이고, 그 <b>snRNP가 다섯 종</b>이라 다섯이 팔짱을 끼고 있다. 잘려 나간 인트론이 <b>올가미 매듭</b> 모양인 것은 실제로 <b>라리아트(lariat)</b>라는 고리 구조를 만들기 때문이다 — 이름이 곧 올가미다.</p><p><b>왜 잘린 것은 금방 없어지나.</b> 통에 담긴 회색 토막들을 보면 <b>모자도 술도 없다.</b> 캡과 꼬리가 mRNA를 지켜 주는 것인데 인트론에는 둘 다 없으니 그대로 분해된다. 그리고 작업대 위 띠는 길고 구멍으로 나가는 띠는 짧다 — 실제 유전자가 암호화 구간보다 훨씬 긴 것이 <b>인트론이 많이 끼어 있기</b> 때문이다.</p><p><b>기출이 꼬는 자리.</b> ① 「인트론이 최종 mRNA에 남는다」 → <b>거짓</b>(남는 것은 엑손이다). ② 「스플라이싱은 캡·꼬리보다 먼저 끝난다」 → 순서를 묻는 선지는 조심한다. 캡은 전사 도중 일찍 붙는다. ③ 「원핵도 스플라이싱을 한다」 → <b>거짓</b>(세균에는 인트론이 없다. 다만 고세균에는 있다). ④ 「잘린 인트론은 재활용된다」 → <b>거짓</b>(보호가 없어 분해된다).</p>"""

ROWS = [
    ('작업대 왼쪽 끝 — 띠의 <b>앞머리</b>에 작은 <b>모자</b>를 씌우는 사람',
     '<b>5′ 캡</b>. 모자가 곧 이름이다. 하는 일이 둘이다 — <b>분해를 막고</b>, '
     '밖에서 읽는 쪽이 <b>먼저 잡는 손잡이</b>가 된다. 진핵 번역 개시에서 작은 소단위체가 '
     '맨 먼저 붙는 자리가 여기다',
     ['F0-56#1', 'F1-81#1', 'F0-57', 'F0-59', 'F1-82#1', 'F0-116']),
    ('작업대 오른쪽 끝 — 띠의 <b>꼬리</b>에 똑같은 <b>술을 여러 가닥</b> 매다는 사람',
     '<b>poly-A 꼬리</b>. 여러 가닥인 것이 <b>poly</b>다. 안정성을 높이고 '
     '<b>핵 밖으로 나가는 것</b>과 번역을 돕는다',
     ['F0-56#2', 'F1-81#2', 'F0-61', 'F0-63', 'F1-82#6']),
    ('가운데 — <b>다섯 사람이 팔짱을 껴 한 덩어리</b>가 되어 가위질한다',
     '<b>스플라이소좀</b>. 자르는 것은 한 사람이 아니라 <b>단백질과 작은 RNA가 모인 한 복합체</b>이고, '
     '그 <b>snRNP가 다섯 종</b>이라 다섯이 붙어 있다',
     ['F0-68', 'F0-69', 'F0-174']),
    ('띠에 <b>청록과 회색이 번갈아</b> 있고, 가위는 <b>회색만</b> 자른다',
     '청록이 <b>엑손</b>(최종 mRNA에 남아 번역되는 구간), 회색이 <b>인트론</b>(전사는 되지만 '
     '번역되지 않고 제거되는 구간)이다. 잘라 내고 남은 것을 잇는 것이 <b>스플라이싱</b>이다',
     ['F0-65', 'F0-66', 'F0-67', 'F0-56#3', 'F1-81#3']),
    ('★ 잘린 회색 토막은 <b>올가미 매듭</b>으로 묶여 담장 <b>안</b> 통에 남는다 · '
     '거기에는 <b>모자도 술도 없다</b>',
     '인트론이 빠질 때 <b>라리아트(lariat, 올가미)</b> 고리가 생긴다 — 이름이 곧 올가미다. '
     '잘린 인트론이 곧바로 분해되는 것은 <b>캡도 꼬리도 없어</b> 보호받지 못하기 때문이다',
     ['F0-70', 'F0-72']),
    ('★ 이어 붙인 것은 <b>청록만 한 줄</b>로 남아 담장 <b>구멍으로 나간다</b>',
     '★ <b>엑손은 나가고(ex-) 인트론은 안에 남는다(in-).</b> 이름이 곧 행선지다 — '
     '둘을 바꿔 놓은 선지가 이 단원 최다 오답인데, 이 한 줄이면 갈린다', []),
    ('작업대 위의 띠는 길고, 구멍으로 나가는 띠는 짧다',
     '실제 유전자가 암호화 구간보다 훨씬 긴 것은 <b>인트론이 많이 끼어 있기</b> 때문이다',
     ['F0-85']),
    ('★ 담장 안에서 하는 손질은 모두 <b>셋</b>이다 — 앞머리 모자 · 꼬리 술 · 가운데 잘라 잇기',
     '진핵 전사 후 <b>RNA 가공 3가지</b>: <b>5′ 캡</b> · <b>poly-A 꼬리</b> · <b>스플라이싱</b>. '
     '전부 담장 안에서 끝나고, 끝나야 나간다', ['F0-56', 'F1-81']),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s43p02'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    assert os.path.exists(os.path.join(ROOT, 'img', 's43p02.webp')), '그림이 없다'

    i = src.index("{id:'s43p01'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s43p02',t:'담장 안 손질대',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    blk = out[out.index("{id:'s43p02'"):]
    blk = blk[:close_brace(blk, 0) + 1]
    cards = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복'
    print('s43p02 신설 · 행 %d · 카드 %d장' % (len(ROWS), len(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
