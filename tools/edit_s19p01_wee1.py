#!/usr/bin/env python3
"""s19p01 — Wee1 후크 신설 (2026-08-20).

★ Wee1 = **「위원」 어깨띠**
  Wee1 은 순수 임의 문자열이라 소리로 잡는 수밖에 없다. 「위원」이 가장 가깝고,
  위원 어깨띠는 한 눈에 띄는 큰 소품이라 「후크가 작으면 후크가 아니다」에도 맞는다.
  자물쇠를 채우는 사람만 두른다 — Cdc25(자물쇠를 끊는 사람)는 안 두른다.
  ★ 첫 편집에서 Gemini 가 둘 다에게 띠를 둘러서 한 번 더 고쳤다.
    같은 소품이 두 사람에게 가면 후크가 죽는다(점검표 ④).
"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from hookedit import swap

SPEC = [
    ('s19p01', '를 채우는 사람',
     '★ <b>겨자색 어깨띠</b>를 비스듬히 두르고 레버에 <b>검은 쇠 자물쇠</b>를 채우는 사람',
     ' ★ <b>어깨띠</b>가 곧 이름이다 — <b>위원</b>(Wee1). '
     '띠를 두른 사람은 이 한 명뿐이고, 자물쇠를 끊는 쪽(Cdc25)은 두르지 않았다'),
]

if __name__ == '__main__':
    sys.exit(swap(SPEC))
