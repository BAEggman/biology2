#!/usr/bin/env python3
"""s44 「초롱 가게」 — 모건의 흰눈 초파리를 Sketchy로 (d05p06 도해를 대신한다).

사용자 요청: 「**d05p06 이것도 sketchy 화 할만해**」

★ d05p06은 도해로 남을 이유가 없었다
  도해를 쓰는 유일한 경우는 「수와 셈이 시험 축이고 생성기가 반복 실패할 때」인데,
  이 단원 카드 열다섯 장은 전부 **셈이 아니라 판정**이다 —
  「이 유전자형이면 눈색이 뭐냐」 · 「딸은 누구의 X를 받느냐」 · 「왜 수컷에만 나오느냐」.
  판정은 소품이 하는 일이다. 그래서 카드를 통째로 이리로 옮긴다.
  (d05p06 격자는 참고용으로 남긴다 — 같은 사실을 두 그림에 걸지 않는다)

★ 후크 — 전부 그림이 곧 뜻이다
  긴 횃대   = X 염색체 — 끝에 **고리**가 있어 초롱을 건다 (눈색 자리가 X에만 있다)
  몽당 횃대 = Y 염색체 — 짧고 끝이 뭉툭해 **고리가 아예 없다** (걸 자리가 없다)
  초롱      = 눈 — 겉이 **육각 격자**라 초파리 겹눈이다. 색이 곧 눈색
  빨간 초롱 = w+ 야생형 · 흰 초롱 = w 흰눈
  ★ 반접합  = **초롱이 하나뿐이다.** hemi(반)가 그대로 그림이다 — 가릴 짝이 없다

★ 계보를 선으로 안 그린 이유
  Gemini는 계보선을 못 그린다. 대신 **똑같이 생긴 초롱**으로 출처를 말하게 했다 —
  아들의 빨간 초롱은 어머니 것과 같고(아버지에게는 빨간 것이 없다),
  딸의 흰 초롱은 아버지 것과 같다(어머니에게는 흰 것이 없다).

★ 실측 (넷이 한 줄, 왼쪽부터)
  1 어머니  긴 횃대 둘 · 빨간 초롱 둘
  2 아버지  긴 횃대 하나에 흰 초롱 하나 + 몽당 횃대 하나(빈손)
  3 딸      긴 횃대 둘 · 빨간 초롱이 앞, 흰 초롱이 뒤에 가려짐
  4 아들    긴 횃대 하나에 빨간 초롱 하나 + 몽당 횃대 하나(빈손)
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ 초롱을 거는 <b>고리</b>가 <b>긴 횃대에만</b> 있다 — 눈색 자리가 X에만 있다는 뜻이다. '
      '몽당 횃대(Y)에는 고리가 없어 걸 자리가 아예 없다. '
      '그래서 <b>몽당 횃대를 가진 둘은 초롱이 하나뿐</b>이고, 가릴 짝이 없다. '
      '초롱 겉이 <b>육각 격자</b>인 것은 초파리 <b>겹눈</b>이라서다.')

BX = """<p><b>★ 고리가 한쪽 횃대에만 있다.</b> 긴 횃대 끝에는 초롱을 거는 쇠고리가 있고 몽당 횃대 끝은 밋밋하다. 이것이 <b>눈색 유전자가 X 염색체에만 있고 Y에는 없다</b>는 말 그대로다. 성염색체 두 개 중 하나에만 그 자리가 있다는 사실 하나에서 이 단원의 나머지가 전부 따라 나온다.</p><p><b>왜 수컷에만 흰눈이 몰리나 — 초롱이 하나뿐이라서다.</b> 긴 횃대를 둘 가진 쪽(암컷)은 초롱도 둘이라, 하나가 흰 것이어도 <b>빨간 것이 앞에 걸려 가린다</b>. 긴 횃대가 하나뿐인 쪽(수컷)은 초롱도 하나뿐이라 <b>가릴 짝이 없다</b> — 그 하나가 흰 것이면 그대로 흰눈이다. 이것이 <b>반접합(hemizygous)</b>이고, hemi가 「반」이라는 뜻인 것이 그림 그대로다. 한 유전자좌에 대립유전자가 하나뿐인 상태다.</p><p><b>초롱의 색은 누구에게서 왔나.</b> 계보를 선으로 잇지 않아도 알 수 있다. <b>아들의 빨간 초롱</b>은 어머니 것과 똑같이 생겼다 — 아버지에게는 빨간 초롱이 아예 없으니 어머니 것일 수밖에 없다. <b>딸의 흰 초롱</b>은 아버지 것과 똑같다 — 어머니에게는 흰 것이 없다. 즉 <b>아들의 X는 반드시 어머니 것</b>이고 <b>딸은 아버지의 X를 반드시 받는다</b>. 아버지가 아들에게 주는 것은 몽당 횃대(Y)뿐이다.</p><p><b>그래서 성을 정하는 쪽은 정자다.</b> 몽당 횃대를 가진 사람은 아버지와 아들 둘뿐이고 어머니와 딸에게는 하나도 없다. 어머니가 낼 수 있는 것은 <b>긴 횃대 한 가지</b>뿐이고, 아버지는 <b>긴 것이나 몽당 것 두 가지</b>를 낼 수 있다. 두 갈래를 가진 쪽이 결과를 가른다.</p><p><b>야생형이 곧 우성은 아니다.</b> 빨간 초롱이 <b>야생형</b>(w<sup>+</sup>)인 것은 자연계 초파리에 그 색이 흔하기 때문이지 우성이라서가 아니다. 여기서는 마침 야생형이 우성이라 둘이 겹쳤을 뿐이다. 이 구별을 묻는 선지가 실제로 나온다.</p><p><b>기출이 꼬는 자리.</b> ① 「아들은 아버지의 X를 받는다」 → <b>거짓</b>. 아버지가 아들에게 주는 것은 Y다. ② 「야생형은 곧 우성이다」 → <b>거짓</b>. ③ 「암컷도 X 하나만 흰눈이면 흰눈이 된다」 → <b>거짓</b>. 두 초롱이 다 희어야 한다. ④ 「흰눈이 수컷에만 나타나는 것은 눈색 유전자가 Y에 있기 때문이다」 → <b>거짓</b>. 정반대다 — <b>Y에 없어서</b>다.</p>"""

ROWS = [
    ('★ <b>긴 횃대</b> 끝에는 초롱을 거는 <b>쇠고리</b>가 있고, <b>몽당 횃대</b> 끝은 '
     '뭉툭해 <b>고리가 아예 없다</b>',
     '긴 횃대가 <b>X 염색체</b>, 몽당 횃대가 <b>Y 염색체</b>다. '
     '눈색 자리(고리)가 <b>X에만 있고 Y에는 없다</b> — 이 하나에서 나머지가 다 따라 나온다', []),

    ('초롱은 넷 다 똑같이 생겼다 — 겉이 <b>육각 격자</b>로 덮인 둥근 초롱이고 '
     '<b>색만</b> 다르다 (빨강 · 흰색)',
     '육각 격자가 초파리 <b>겹눈</b>이다. 초롱 색이 곧 <b>눈색</b>이고, '
     '초파리의 <b>야생형 눈색은 빨간눈</b>이다. '
     '빨간 초롱이 <b>w<sup>+</sup>(야생형 대립유전자)</b>, 흰 초롱이 <b>w(흰눈 대립유전자)</b>다',
     ['E0-20', 'E0-21', 'E0-22']),

    ('빨간 초롱을 <b>야생형</b>이라 부르는 것은 자연계에 그 색이 <b>흔해서</b>이지 '
     '앞에 걸려서가 아니다',
     '<b>야생형이 곧 우성이라는 뜻은 아니다.</b> 야생형은 자연계에 흔한 형질일 뿐이고, '
     '여기서는 마침 둘이 겹쳤다',
     ['E0-23', 'D1-119']),

    ('★ 셋째 사람(딸) — 긴 횃대 둘에 <b>빨간 초롱이 앞</b>, <b>흰 초롱이 뒤에 가려져</b> 있다',
     '<b>X<sup>w+</sup>X<sup>w</sup> 암컷은 빨간눈</b>이다. 초롱이 둘이라 하나가 희어도 '
     '빨간 것이 앞에 걸려 가린다 — 이것이 우성이 열성을 가린다는 말 그대로다',
     ['E0-118']),

    ('★ 둘째·넷째 사람은 <b>몽당 횃대</b>를 하나씩 들었고, 그래서 <b>초롱이 하나뿐</b>이다 · '
     '첫째·셋째는 긴 횃대 둘이라 초롱도 둘이다',
     '<b>반접합(hemizygous)</b>. 수컷은 한 유전자좌에 대립유전자가 <b>하나뿐</b>이라 '
     '<b>가릴 짝이 없다</b>. hemi가 「반」이라는 뜻인 것이 그림 그대로다',
     ['E0-149', 'D1-118']),

    ('둘째 사람(아버지)의 하나뿐인 초롱은 <b>흰색</b>이다 · '
     '넷째 사람(아들)의 하나뿐인 초롱은 <b>빨강</b>이다',
     '<b>X<sup>w</sup>Y 수컷은 흰눈</b>, <b>X<sup>w+</sup>Y 수컷은 빨간눈</b>이다. '
     '초롱 하나가 곧 눈색이라 가릴 것도 가려질 것도 없다',
     ['E0-116', 'E0-117']),

    ('★ 넷째(아들)의 <b>빨간 초롱</b>은 첫째(어머니) 것과 똑같이 생겼다 — '
     '둘째(아버지)에게는 빨간 초롱이 <b>하나도 없다</b>',
     '<b>아들의 X는 반드시 어머니 것</b>이다. 아버지가 아들에게 주는 것은 몽당 횃대(Y)뿐이다',
     ['E0-144']),

    ('★ 셋째(딸)의 <b>흰 초롱</b>은 둘째(아버지) 것과 똑같이 생겼다 — '
     '첫째(어머니)에게는 흰 초롱이 <b>하나도 없다</b>',
     '<b>딸은 아버지의 X를 반드시 받는다.</b> 아버지에게 긴 횃대는 하나뿐이라 '
     '딸에게 줄 것이 그것밖에 없다',
     ['E0-143']),

    ('★ 셋째와 넷째는 둘 다 <b>앞에 걸린 초롱이 빨강</b>이다',
     '흰눈 수컷 × 빨간눈 암컷의 <b>F1은 딸도 아들도 모두 빨간눈</b>이다. '
     '이 결과 자체가 <b>빨간눈이 우성</b>이라는 증거다',
     ['E0-24', 'E0-114']),

    ('몽당 횃대를 가진 것은 <b>둘째와 넷째뿐</b>이고, 첫째·셋째에게는 <b>하나도 없다</b>',
     '어머니가 낼 수 있는 것은 <b>긴 횃대 한 가지</b>(난자는 X만)뿐이고, '
     '아버지는 <b>긴 것이나 몽당 것 두 가지</b>(정자는 X 또는 Y)를 낸다. '
     '두 갈래를 가진 쪽이 결과를 가르므로 <b>성을 정하는 쪽은 정자</b>다',
     ['E0-145']),
]

MOVED = ['E0-20', 'E0-21', 'E0-22', 'E0-23', 'D1-119', 'E0-145', 'E0-24', 'E0-114',
         'E0-143', 'E0-144', 'E0-117', 'E0-118', 'E0-116', 'E0-149', 'D1-118']


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s44'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    assert os.path.exists(os.path.join(ROOT, 'img', 's44p01.webp')), '그림이 없다'

    # ① d05p06에서 옮길 카드를 뗀다 (같은 사실을 두 그림에 걸지 않는다)
    i = src.index("{id:'d05p06'")
    e = close_brace(src, i)
    blk = src[i:e + 1]
    for c in MOVED:
        n = len(re.findall(r'"%s"' % re.escape(c), blk))
        assert n == 1, '%s 가 d05p06에 %d번' % (c, n)
        blk = re.sub(r',"%s"|"%s",|"%s"' % ((re.escape(c),) * 3), '', blk, count=1)
    blk = blk.replace(',[]', '').replace('[],', '')
    src = src[:i] + blk + src[e + 1:]

    # ② 새 장면을 d05 앞에 넣는다
    j = src.index("{id:'d05',")
    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')
    panel = ("{id:'s44p01',t:'초롱 거는 횃대',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    scene = ("{id:'s44',t:'초롱 가게',gate:'E',unit:'15 감수분열과 성유전 — X 연관',panels:[\n "
             + panel + ']},\n ')
    out = src[:j] + scene + src[j:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    blk2 = out[out.index("{id:'s44p01'"):]
    blk2 = blk2[:close_brace(blk2, 0) + 1]
    cards = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', blk2)
    assert len(cards) == len(set(cards)), '카드 중복'
    print('s44p01 신설 · 행 %d · 카드 %d장 (d05p06에서 %d장 옮김)'
          % (len(ROWS), len(cards), len(MOVED)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
