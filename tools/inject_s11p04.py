#!/usr/bin/env python3
"""s11p04 「담장 안과 담장 밖」 신규 패널 — 진핵 DNA 중합효소 다섯.

왜 새 패널인가
  BM상 #367(2003)이 α·β·γ·δ·ε 다섯 짝을 통째로 묻는데, 지금까지 이 다섯은
  s11p02 의 bx 안에 「표」로만 있었다. 그림이 없으면 β(수선)·γ(미토콘드리아)를 몰라
  **정답을 몰라서가 아니라 오답을 못 지워서** 막힌다 — 그 문항의 진짜 함정이 거기다.
  s11p02 에 밀어 넣지 않은 이유는 여덟이 되어 서로 구별이 안 되기 때문이다(s10p02 의 교훈).

원핵(s11p02)과 자리를 그대로 맞췄다
  프라이팬 요리사 = 프라이메이스 자리 → pol α
  쇠망치 = 중합효소 자리(Pol III)     → pol δ · ε
  맨손·막다른 곁길 = Pol II 자리      → pol β
  담장 밖 굴뚝집의 끝없는 고리         → pol γ (미토콘드리아, 환형)

재질 코드도 이어진다 — 쇠못 = RNA 프라이머, 나무못 = 그 자리를 메운 DNA.
담장에 닿기 전 마지막 자락이 판자도 못도 없이 비어 있는 것이 말단 복제 문제이고,
고리에는 끝이 없어 그 자리가 생기지 않는다.

E1-90 은 s11p02 에 그림 근거 없이 걸려 있었다 — 이리로 옮긴다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BX = """<p><b>원핵 셋에 그대로 포갠다.</b> 자리가 같으면 도구도 같게 그렸다 — <b>프라이팬</b>은 프라이메이스 자리라 <b>pol α</b>, <b>쇠망치</b>는 주 복제 자리라 <b>pol δ·ε</b>, <b>맨손으로 막다른 곁길</b>은 Pol II 자리라 <b>pol β</b>다. 남은 하나 <b>pol γ</b>만 담장 밖에 있는데, 그것이 곧 「핵 밖」이라는 뜻이고 미토콘드리아다.</p><p><b>담장이 핵막이다.</b> 둥근 구멍은 핵공이다. 이 담장 하나가 원핵과 진핵을 가르는 전부이기도 하다 — 세균에는 이 담장이 없어 전사와 번역이 한자리에서 이어진다(그래서 오페론이 성립한다).</p><p><b>δ와 ε는 어느 가닥인지 그리지 않았다.</b> 교재마다 표기가 갈리는 자리라서다. 그림에서 둘은 <b>분기점에 나란히 서 있을 뿐 아직 갈래에 발을 들이지 않았다</b>. 기출도 그 다툼은 건드리지 않는다 — 뒤에 나오는 문항의 정답은 δ·ε의 가닥 배정과 무관하게 갈린다.</p><p><b>못의 재질이 계속 뜻을 나른다.</b> 요리사가 놓는 것은 <b>쇠못</b>(RNA 프라이머)이고, 판자를 붙들고 있는 것은 <b>나무못</b>(그 자리를 메운 DNA)이다. s11p01·s11p02와 같은 문법이다. 담장에 닿기 전 마지막 한 자락이 <b>판자도 못도 없이 비어 있고 그 위에 뽑힌 쇠못 하나</b>가 놓인 것이 <b>말단 복제 문제</b>다 — 마지막 프라이머를 뽑고 나면 그 자리를 시작할 못을 놓을 데가 없다. 굴뚝집의 고리에는 끝이 없어 그 빈자리가 아예 생기지 않는다.</p><div class='qx'><div class='qh'>여기서 갈리는 문제</div><p class='qlead'>이 문항은 <b>모르는 것을 찍게</b> 만든다. α·δ·ε 셋만 외우고 오면 처음 보는 <b>β</b>가 답처럼 보이는데, β는 옳은 짝이다. 정답을 아는 것보다 <b>오답을 지우는 것</b>이 먼저인 자리이고, 지우려면 다섯을 다 알아야 한다. 그리고 짝의 <b>앞이 아니라 뒤</b>를 보면 한 번에 끝난다.</p><details class='qitem'><summary><span class='qsrc'>BM상 #367 · 2003</span><span class='qq'>진핵생물 DNA 중합효소의 역할 짝지음 중 옳지 않은 것은?</span></summary><ol class='qc'><li>pol α - 지연사슬 합성(primer)</li><li>pol β - DNA 회복</li><li>pol γ - 미토콘드리아 DNA 복제</li><li>pol δ - 선도사슬 합성</li><li class='ans'>pol ε - Plasmid DNA 복제</li></ol><div class='qa'><b>정답 ⑤</b> — 원지 메모는 「pol ε는 선도가닥 복제·수선(진핵세포에 plasmid 일반적으로 없음)」이다. 답을 고르는 가장 빠른 길은 짝의 앞이 아니라 <b>뒤</b>를 보는 것이다. <b>플라스미드는 세균의 물건</b>이고 진핵세포에는 일반적으로 없다. 진핵 중합효소 목록에 세균 소품이 하나 섞여 있으니, ε가 무슨 일을 하는지 몰라도 ⑤는 지워진다.<br><b>① pol α</b>는 프라이메이스를 끼고 있어 <b>프라이머를 깔아 주는</b> 역할이라 지연가닥과 함께 나온다. 옳은 짝이고, 그림의 <b>프라이팬 요리사</b>가 그 사람이다 — s11p01에서 분기점의 프라이메이스가 들고 있던 바로 그 팬이다.<br><b>② pol β - DNA 회복</b>이 이 문항의 진짜 함정이다. α·δ·ε 셋만 외우고 온 학생은 β를 처음 보므로 「모르는 것이 답이겠지」로 여기를 찍는다. 그런데 β는 <b>수선(특히 염기절제 수선의 빈자리 메우기)</b> 담당으로 <b>옳은 짝</b>이다. 그림에서 그가 <b>본선을 벗어난 막다른 곁길에서 맨손으로 갈라진 판자를 갈아 끼우는</b> 것이 그 일이고, 원핵에서 <b>Pol II</b>가 서 있던 자리와 동작이 같다.<br><b>③ pol γ - 미토콘드리아 DNA 복제</b>도 옳은 짝이다. 핵 밖에도 복제할 DNA가 있고 그 전담이 γ다. 그림에서 <b>담장(핵막) 밖 굴뚝집</b>에 있고, 그 안의 띠가 <b>끝이 없는 고리</b>인 것은 미토콘드리아 DNA가 환형이기 때문이다.<br><b>④ pol δ - 선도사슬 합성</b>도 옳다. δ와 ε의 담당 가닥을 놓고 교재마다 표기가 갈리지만, 이 문항은 그 다툼을 건드리지 않는다 — ⑤가 플라스미드라는 <b>소속 자체</b>로 틀리기 때문에, 이 문항 안에서는 δ·ε의 가닥 배정을 따질 필요가 없다. 그림에서 둘이 분기점에 나란히 서 있기만 한 것이 그 뜻이다.</div></details><div class='qrule'>진핵은 <b>α=프라이머 · δ/ε=복제 · β=수선 · γ=미토콘드리아</b>. 원핵의 <b>프라이메이스·III·II</b>에 그대로 포개고, γ만 담장 밖에 따로 세운다. 짝의 <b>뒤</b>에 세균 소품(플라스미드)이 섞여 있으면 그것이 답이다.</div></div>"""

BR = ('원핵 셋(s11p02)에 그대로 포갠다 — <b>프라이팬은 α, 쇠망치 둘은 δ·ε, 맨손은 β</b>다. '
      '담장이 <b>핵막</b>이라 담장 밖 굴뚝집에서 <b>끝없는 고리</b> 위에 까는 사람이 γ(미토콘드리아)다.')

ROWS = [
    ('담장에 뚫린 둥근 구멍들',
     '담장이 <b>핵막</b>이고 구멍이 핵공이다. 담장 안에서 하는 일이 핵의 복제, 밖이 소기관의 복제다', None),
    ('담장 안, 프라이팬에서 벌겋게 달군 쇠못을 꺼내 놓는 요리사',
     '<b>pol α</b> — 프라이메이스를 끼고 있어 <b>프라이머를 깔아 준다</b>. s11p01 분기점의 프라이메이스와 '
     '같은 팬이고, 원핵의 프라이메이스 자리에 그대로 앉는다', None),
    ('분기점에 나란히 선 쇠망치 둘 — 하나는 삼각 모자, 하나는 둥근 모자',
     '<b>pol δ</b>(삼각형이 그리스 문자 Δ다)와 <b>pol ε</b>가 본선의 <b>주 복제</b>를 나눠 맡는다 — '
     '원핵의 Pol III 자리다. 어느 쪽이 선도이고 어느 쪽이 지연인지는 <b>교재마다 갈리므로</b> '
     '둘 다 아직 갈래에 발을 들이지 않았다', None),
    ('판자를 붙들고 있는 나무못 · 요리사가 놓는 쇠못',
     '<b>나무못은 DNA, 쇠못은 RNA 프라이머</b>. s11p01·s11p02와 같은 재질 문법이다 — '
     '쇠못 자리는 나중에 나무못으로 바뀐다', None),
    ('막다른 곁길에서 맨손으로 갈라진 판자를 갈아 끼우는 사람',
     '<b>pol β</b> — 수선 담당. s11p02에서 <b>Pol II</b>가 서 있던 바로 그 자리, 그 동작이다. '
     'α·δ·ε만 외우고 오면 이 사람을 몰라 오답을 못 지운다', None),
    ('담장 밖 굴뚝집, 그리고 끝이 없는 둥근 고리 위에 판자를 까는 사람',
     '<b>pol γ</b> — <b>미토콘드리아 DNA 복제</b>. 담장 밖인 것이 「핵 밖」이고, 띠가 고리인 것은 '
     '미토콘드리아 DNA가 환형이기 때문이다', None),
    ('담장에 닿기 전 마지막 한 자락이 판자도 못도 없이 비어 있다 · 그 위에 뽑힌 쇠못 하나',
     '<b>말단 복제 문제</b>. 마지막 프라이머(쇠못)를 뽑고 나면 그 자리를 시작할 못을 놓을 데가 없어 '
     '선형 DNA는 복제할수록 말단이 짧아진다', ['E1-90']),
    ('굴뚝집 고리에는 그런 빈 자락이 없다',
     '<b>환형 DNA에는 말단이 없어</b> 이 문제가 생기지 않는다. 세균의 염색체도 같은 이유로 겪지 않는다',
     ['E1-91']),
]


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s11p04'" not in src, '이미 있다'

    # 1) s11p02 의 pc 에서 E1-90 을 뺀다 (그림 근거가 여기로 옮겨 왔다)
    old_pc = '{id:\'s11p02\',pc:["E1-90","S-BT-11","E1-77"]'
    new_pc = '{id:\'s11p02\',pc:["S-BT-11","E1-77"]'
    assert src.count(old_pc) == 1, 's11p02 pc 앵커 %d회' % src.count(old_pc)
    src = src.replace(old_pc, new_pc)

    # 2) s11p03 뒤에 새 패널을 끼운다
    i = src.index("{id:'s11p03'")
    e = close_brace(src, i)

    assert '`' not in BX.replace('`', '') or True
    assert '`' not in BX, 'bx 에 백틱'
    assert "'" not in BR, 'br 에 작은따옴표'

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'s11p04',t:'담장 안과 담장 밖',br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n  '.join(rows) + ']}')

    out = src[:e + 1] + ',\n  ' + panel + src[e + 1:]

    # 3) 되읽어 확인
    k = out.index("{id:'s11p04'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('s11p04') == ids.index('s11p03') + 1, '위치가 s11p03 바로 뒤가 아니다'
    cards = re.findall(r'"(E1-\d+)"', blk)
    print('패널 %d개 · s11p04 행 %d · 카드 %s' % (len(ids), len(ROWS), ' '.join(cards)))
    open(SK, 'w', encoding='utf-8').write(out)
    print('sketchy.html %d → %d bytes' % (len(open(SK, encoding='utf-8').read()), len(out)))
    return 0


if __name__ == '__main__':
    sys.exit(main())
