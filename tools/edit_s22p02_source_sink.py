#!/usr/bin/env python3
"""s22p02 — 공급원을 잎에 붙이고 수용부를 셋으로 가른다 (2026-08-20).

pc 접지 1차에서 O1-24 계열 셋을 폐기했었다. 사유가
「야적 더미가 우듬지 잎에 매달려 있지 않고 받는 쪽도 자루뿐이라 뿌리·열매·덩이줄기가 안 갈린다」였다.
그림을 고쳤으므로 **O1-24#1(공급원=성숙한 잎)만** 되살린다.

⚠ O1-24 와 O1-24#2 는 그대로 둔다 — 정답이 「뿌리·열매·<b>어린 잎</b>·덩이줄기」인데
   그림에 그린 것은 셋뿐이고 <b>어린 잎</b>이 없다. 「절반만 그려졌으면 걸지 않는다」.
   넷째로 「아직 작은 잎사귀 한 장」을 더 그리면 그때 걸린다.
"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from hookedit import swap
from link_cards import link

SWAP = [
    ('s22p02', '야적 더미에서 관으로 밀어 넣으면',
     '★ 우듬지 <b>잎</b>에서 <b>나무 홈통</b>을 타고 자루가 미끄러져 내려와 더미가 되고, '
     '인부들이 그것을 관으로 밀어 넣으면 반대편으로 흘러나온다',
     ' ★ 자루가 <b>잎에서 내려온다</b>는 것이 요점이다 — 당을 만드는 곳이 곧 공급원이다'),
    ('s22p02', '수용부 쪽에만 있는, 목을 묶은 둥근 자루',
     '★ 반대편 끝 — <b>굵은 뿌리 다발</b> · <b>붉은 열매</b> · <b>울퉁불퉁한 덩이줄기</b> '
     '셋에 자루를 하나씩 비운다',
     ' 받는 쪽은 <b>당을 쓰거나 쌓아 두는</b> 곳이다. '
     '<b>어린 잎</b>도 같은 쪽인데 그림에는 안 그려져 있다'),
]

LINK = [('s22p02', '★ 우듬지 ', ['O1-24#1'],
         ' <b>공급원(source)</b>은 <b>성숙한 잎</b>이다 — 제 몫보다 많이 만들어 내보내는 쪽이다. '
         '아직 제 밥벌이를 못 하는 <b>어린 잎</b>은 반대로 받는 쪽이다')]


def main():
    if swap(SWAP): return 1
    return link(LINK)


if __name__ == '__main__':
    sys.exit(main())
