#!/usr/bin/env python3
"""pc 접지 감사 9차 — 8차에서 고아가 된 s09p01을 미연결 카드로 메운다.

I1-7(나노포어)을 떼자 s09p01(재조합 작업대)에 걸린 카드가 하나도 없어졌다.
그런데 이 판의 소품(가위·점착 말단·라이게이스)에 딱 맞는 **미연결** 카드가 넷 있었다.
빼기만 하는 감사가 아니라 **제자리를 찾아 주는** 감사여야 한다.
"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from link_cards import link

LINK = [
    ('s12p02a', '벽을 뜯어야 보이는', ['G1-95'],
     '★ 이 방은 <b>왼쪽이 앞</b>이다 — 브루너샘이 왼쪽 끝에만 있는 것이 그 표시다. '
     '순서는 <b>십이지장 → 공장 → 회장</b>이고, 회장은 오른쪽 끝(봉투를 낸 하나만 들어가는 문)이다'),
    ('s09p01', '큰 가위', ['I1-8'],
     '세균이 <b>외래 DNA(파지)를 막으려고</b> 가진 것을 사람이 빌려 쓰는 것이다 — '
     '아무 데나 자르는 것이 아니라 <b>정해진 서열</b>에서만 문다'),

    ('s03p02', '붉은 밀랍 도장', ['I1-9', 'X-BT-8'],
     '도장은 <b>메틸기</b>다. 붙여 두는 까닭은 하나뿐이다 — '
     '<b>제 가위에 제가 잘리는 것</b>을 피하려는 것이다'),

    ('s09p01', '뒤따라오며 납땜하는', ['S-BT-15'],
     '★ 클로닝 네 걸음 중 <b>앞의 둘</b>이 이 작업대다 — <b>제한효소로 자르고</b> '
     '<b>라이게이스로 봉합한다</b>. 남은 둘(숙주에 넣기 · 청백 선별)은 옆 판이다'),
    ('s09p02', '핀셋으로 골라내는', ['S-BT-15'],
     '★ 클로닝 네 걸음 중 <b>뒤의 둘</b>이 이 판이다 — 숙주에 넣어 키우고 '
     '<b>흰 군락만 골라내는 선별</b>이 마지막이다'),
]


def main():
    return 1 if link(LINK) else 0


if __name__ == '__main__':
    sys.exit(main())
