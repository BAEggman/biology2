# -*- coding: utf-8 -*-
"""pc 가 딱 한 장인 판 22개 — 한 번에 훑는다.

★ 판당 판정 하나라 훑기가 가장 싸다. 그런데 결과가 뜻밖이었다 —
   22장 중 **18장이 그냥 행으로 내려갔다.** 그림에 이미 또렷이 있는데
   pc 로 얹혀 있었을 뿐이다. 큰 판(41장·21장)일수록 폐기가 많고
   1장짜리는 거의 다 살아 있다 — 「어디 걸지 몰라 얹은 것」이 아니라
   「한 장이라 미뤄 둔 것」이었다.

③ 폐기 셋
  G1-15  수용성 비타민 = B군과 **비타민 C** — s12p04 는 B군만 그린다. C 는 bx 산문에만 있다
  T0-213 촉각 수용체 넷을 **깊이 순서**로 — s27p01 은 「얕은 둘 / 깊은 둘」 두 무리로만 가르고
         무리 안의 순서(메르켈↔마이스너, 루피니↔파치니)를 안 그린다. 넷을 한 줄로 세우면 살아난다
  B1-187 무기호흡 vs 발효 — 「무기호흡」이 덱 전체 0건이다. 발효 쪽만 그려져 있다

⑤ 배열 하나
  S-PL-15 개화 광주기 3원칙 — ①연속된 밤(s20p02 모래시계) ②광중단(s20p02 손전등)
          ③적색광↔근적외선 취소(**s20p03 양면 토글 스위치**). 셋째가 옆 판이라 배열이다
"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from pc_sweep import sweep

ONE = [
 ('s02p02', '["금색 자물쇠"',                      'I0-58#1'),
 ('s02p03', '["잔디 덮인 옛 자국"',                'X-BT-10'),
 ('s04p06', '["통 벽에 걸린 액자',                 'S-AN-13'),
 ('s08p02', '["먼저 떨어져 튀는 물방울"',          'G0-183#2'),
 ('s09p02', '["파란 군락"',                        'X-BT-9'),
 ('s11p02', '["두 도구가 서로 반대쪽을 문다',      'S-BT-11'),
 ('s12p03', '["붉은 고리 오른쪽에서 자름"',        'G1-103'),
 ('s13p02', '["깃대가 아예 없고 손에는 모래시계"', 'IM-098'),
 ('s14p03', '["아랫줄 — 이끼',                     'C1-80'),
 ('s15p01', '["물통에서 걸쇠로 이어진 관"',        'T0-408'),
 ('s15p02', '["지붕의 접시 안테나"',               'B0-221'),
 ('s19p02', '["파수꾼을 화로에 던짐"',             'C1-24'),
 ('s23p01', '["뒤늦게 열려 밖으로 쏟아냄"',        'T0-15'),
 ('s26p01', '["넷이 함께 오른쪽으로 밈"',          'H0-260'),
 ('s26p02', '["거의 빈 병을 흔든 뒤에야 호루라기"','H0-236'),
 ('s30p01', '["밀폐 헬멧으로 공중에서 끌어내림"',  'O2-18'),
 # ⚠ s34p05·s34p06 은 행을 작은따옴표로 썼다 — 바늘도 작은따옴표로 연다
 ('s34p05', chr(91)+chr(39)+'왼쪽 — 껍질이 열려',   'P0-27'),
  ('s34p06', chr(91)+chr(39)+'굵고 검은 밑둥',       'P0-20'),
]
def _has_pc(pid):
    import re
    t = open(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..', 'sketchy.html'),
             encoding='utf-8').read()
    i = t.index("{id:'%s'" % pid)
    return re.search(r"pc:\[", t[i:i + 500]) is not None

for pid, needle, cid in ONE:
    if _has_pc(pid): sweep(pid, DEMOTE=[(needle, [cid])])

# ⑤ 배열 — 셋째 원칙이 옆 판에 있다
if _has_pc('s20p02'): sweep('s20p02',
  DEMOTE=[('["시계판 · 바늘이 아예 없고 모래시계만"', ['S-PL-15'])],
  MOVE=[('s20p03', '["양면 토글 스위치"', ['S-PL-15'])])

# ③ 폐기
if _has_pc('s12p04'): sweep('s12p04', DROP=['G1-15'])
if _has_pc('s27p01'): sweep('s27p01', DROP=['T0-213'])
if _has_pc('s31p04'): sweep('s31p04', DROP=['B1-187'])
