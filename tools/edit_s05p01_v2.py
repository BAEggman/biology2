#!/usr/bin/env python3
"""s05p01 재생성본에 맞춰 소품 문구를 고친다 (2026-08-20).

사용자 지적: 「s05p01 을 보면 병꼴균이 여전히 물 안에 들어가있지 않고 포자 표현이 뭔가 별로야」
새 그림에서 고쳐진 것
  · 병 모양 집이 **개울 한가운데** 물속에 서 있다 — 물이 사방을 감싸고 물결이 퍼진다
  · 꼬리 달린 포자가 **열린 문에서** 헤엄쳐 나온다 — 첫 마리가 문간에 걸쳐 있어 출처가 보인다
  · 물속 집 위에는 떠오르는 점이 **없다** — 공기 포자는 오른쪽 두 집에만

★ 후크 하나 신설 — 글로메로균 = **가죽 글러브**
  Glomero-  ↔  글러브(glove). 소리 후크 ①.
  손가락이 나무껍질을 파고드는 것이 그대로 **수지상균근(AM)**이다 — 뜻과 소리가 겹친다.
  전에는 「손가락 뿌리」였는데, 그것만으로는 이름이 실리지 않았다.
"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from hookedit import swap
from pc_judge_batch1 import add_row

SPEC = [
    ('s05p01', '개울에 발을 담근',
     '★ <b>개울 한가운데</b> 물속에 선 병 모양 집 — 물이 사방을 감싸고 물결이 퍼진다. 셋 중 이 집만 그렇다',
     ' 나머지 둘은 물에서 떨어진 마른 둔덕에 있다 — 왼쪽에서 오른쪽으로 갈수록 물에서 멀어진다'),
    ('s05p01', '그 집 앞 물속을',
     '★ <b>열린 문에서</b> 꼬리를 흔들며 헤엄쳐 나오는 포자 — 첫 마리는 아직 문간에 걸쳐 있다',
     ' 포자가 어디서 나오는지가 보여야 <b>이 집의 포자</b>다'),
    ('s05p01', '나무에 파고드는 손가락 뿌리',
     '★ 나무 밑동을 감싼 <b>가죽 글러브</b> — 그 <b>손가락들이 껍질 속으로 파고든다</b>',
     ' ★ <b>글러브</b>가 곧 이름이다 — <b>글로메로</b>균. '
     '손가락이 나뭇가지처럼 갈라져 세포벽 안으로 들어가는 것이 <b>수지상</b>(arbuscular)이다'),
]

NEWROW = ('s05p01',
          '오른쪽 두 집 위로만 점 같은 포자가 <b>공중에 떠오르고</b>, 물속 집 위에는 <b>하나도 없다</b>',
          '병꼴균만 <b>물이 있어야</b> 퍼진다 — 유영포자는 헤엄쳐 가지 날아가지 않는다. '
          '나머지는 공기로 퍼지므로 물에서 멀어져도 산다', [])


def main():
    rc = swap(SPEC)
    if rc: return rc
    add_row(*NEWROW)
    return 0


if __name__ == '__main__':
    sys.exit(main())
