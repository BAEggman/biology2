#!/usr/bin/env python3
"""s21p02 — 외군 배지 편집 ★ 보류 (2026-08-20 시도, 실패)

목표는 「전원이 단 배지 = 조상형질 / 일부만 단 배지 = 파생형질」을 그림에 넣는 것이었다.
Gemini 에 배지를 하나 더 달라고 시켰더니 **배지는 하나 더 붙었는데 논리가 무너졌다** —
  · 외군도 새 배지를 달았다 (전원 공통이라 대비가 안 생긴다)
  · 내군 배지가 저마다 다른 도형이라 「공유」파생형질로 안 읽힌다

★ 배운 것: **「누가 가지고 누가 안 가졌는가」는 편집으로 못 시킨다.**
  Gemini 는 물건을 더하고 빼는 것은 잘하지만 **소유의 논리**는 못 지킨다.
  이런 것은 처음부터 그 대비만을 위한 **단독 판**으로 세워야 한다.

→ 다음 안: 「모두가 쓴 모자 · 몇몇만 쓴 모자」 단독 판.
   나뭇가지에 매달린 사람 다섯, 전원이 <b>같은 밀짚모자</b>를 쓰고,
   그중 오른쪽 셋만 <b>붉은 목도리</b>를 둘렀다. 왼쪽 끝 하나는 멀리 떨어져 있다(외군).
   밀짚모자=조상형질, 붉은 목도리=공유파생형질.
   ★ 도형이 아니라 **있다/없다**로 가르는 것이 급소다.
"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from pc_judge_batch1 import add_row

NEWROW = ('s21p02',
          '★ 그런데 <b>네모 배지</b>는 <b>전원이</b> 달았다 — 멀리 떨어져 매달린 <b>외군까지</b> · '
          '앞의 배지는 갈림점 <b>아래만</b> 달았다',
          '<b>외군 분석</b>의 원리다. 외군과 내군이 <b>함께</b> 가진 형질이 <b>조상형질</b>이고, '
          '내군 <b>일부에만</b> 있는 형질이 <b>파생형질</b>이다. '
          '★ 계통을 추론할 때 쓰는 쪽은 <b>파생형질</b>이다 — '
          '전원이 가진 것은 누가 누구와 가까운지를 하나도 말해 주지 않는다',
          ['K2-18', 'K2-18#1', 'K2-18#2'])


def main():
    add_row(*NEWROW)
    return 0


if __name__ == '__main__':
    sys.exit(main())
