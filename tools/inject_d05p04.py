#!/usr/bin/env python3
"""d05p04 「반성·한성·종성」 — d05 네 번째 도해.

★ 세 가지를 가르는 그림 문법 — 3×2 칸의 무늬가 서로 다르다
  반성  남자 줄만 칸이 셋이 아니라 **둘**이다   (X가 하나뿐 = 반접합)
  한성  한 성이 **전부 안 나타남**이다          (성이 잠근다)
  종성  **가운데 칸(이형접합)만** 갈린다        (우열이 뒤집힌다)

칸 안 위쪽 원이 유전자형, 아래쪽 표시가 나타나는지 여부다.
어휘는 d05 안에서 통일 — 막대 = 염색체 · 원 = 대립유전자(채운 우성 / 빈 열성) ·
청록 = 성염색체(d05p03과 같다) · 회색 = 상염색체.

⚠ 그린 것만 건다 — 모건의 흰눈 교배 자체(E0-111·114·116~119)는 여기 없다.
   교배 격자가 아니라 「어디에 있고 누가 표현하나」만 그린 도해다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')
SVGF = os.path.join(ROOT, 'tools', '_d05p04.svg')

BR = ('★ <b>칸의 무늬</b>가 셋을 가른다 — <b>반성</b>은 남자 줄만 칸이 <b>둘</b>(X가 하나뿐), '
      '<b>한성</b>은 한 성이 <b>전부 안 나타남</b>(성이 잠근다), '
      '<b>종성</b>은 <b>가운데 칸에서만</b> 갈린다(우열이 뒤집힌다). '
      '가르는 물음은 둘뿐이다 — <b>유전자가 어디 있나</b>, 그리고 <b>성별에 따라 무엇이 달라지나</b>.')

BX = """<p><b>★ 이름을 외우지 말고 무늬를 본다.</b> 세 낱말이 서로 비슷해서 정의로 외우면 반드시 헷갈린다. 유전자형 셋 × 성 둘을 늘어놓고 <b>칸이 어떻게 생겼는가</b>만 보면 한눈에 갈린다.</p><p><b>반성유전 — 남자 줄만 칸이 둘이다.</b> 유전자가 <b>X 염색체</b> 위에 있다. 여자는 X가 둘이라 유전자형이 셋(우성 둘·섞임·열성 둘)인데, 남자는 <b>X가 하나뿐</b>이라 가질 수 있는 것이 <b>둘뿐</b>이다. 그래서 동형접합이니 이형접합이니를 따질 수가 없고, 이 상태를 <b>반접합(hemizygous)</b>이라 한다. 열성 대립유전자를 하나만 받아도 <b>가릴 짝이 없어</b> 그대로 나타난다 — 남자가 X연관 열성 형질을 더 자주 보이는 직접적인 이유가 이것이다. 사람에서는 <b>색맹·뒤셴 근위축증·혈우병</b>, 초파리에서는 <b>눈색</b>이 대표다.</p><p><b>한성유전 — 한 성이 통째로 안 나타난다.</b> 유전자는 <b>상염색체</b> 위에 있다. 그런데 형질 자체가 <b>한 성에서만</b> 표현될 수 있다. 그림에서 수컷 줄은 유전자형이 무엇이든 전부 안 나타남이다 — 유전자를 가지고 있어도 성이 잠가 버린다. 닭의 <b>벼슬·수염</b>, 젖 분비 같은 것이 그렇다.</p><p><b>종성유전 — 가운데 칸에서만 갈린다.</b> 역시 <b>상염색체</b> 위인데, 이번에는 <b>남녀에서 우열 관계가 달라진다</b>. 우성 동형접합은 남녀 다 나타나고 열성 동형접합은 남녀 다 안 나타나는데, <b>이형접합에서만</b> 남자는 나타나고 여자는 안 나타난다. <b>대머리</b>가 그 예이고, 갈리는 까닭은 <b>성호르몬 환경</b>이 표현형 발현에 영향을 주기 때문이다. 「가운데 칸만 다르다」가 종성의 서명이다.</p><p><b>★ 두 물음이면 끝난다.</b> ① <b>유전자가 어디 있나</b> — 성염색체면 <b>반성</b>이다. ② 상염색체라면, <b>성별에 따라 무엇이 달라지나</b> — <b>나타나는지 여부</b>가 달라지면 <b>한성</b>, <b>우열 관계</b>가 뒤집히면 <b>종성</b>이다. 한성과 종성의 공통점은 둘 다 <b>상염색체 유전자인데 성별에 따라 표현 양상이 달라진다</b>는 것이고, 갈리는 지점이 바로 ②다.</p><p><b>전달 규칙 — 방향이 문제다.</b> 아버지는 아들에게 <b>Y</b>를 준다. 그러니 <b>아버지의 X연관 대립유전자는 아들에게 가지 않고 딸에게만</b> 간다. 반대로 어머니의 X는 <b>아들과 딸 모두에게</b> 갈 수 있고, 아들은 X가 하나뿐이라 어머니에게서 받은 것이 <b>그대로 나타난다</b>. 그래서 아들이 X연관 열성 형질을 보일 확률은 <b>어머니의 유전자형</b>에 직접 좌우된다.</p><p><b>기출이 꼬는 자리.</b> ① 「한성유전은 성염색체 유전이다」 → <b>거짓</b>(상염색체다). ② 「종성유전은 성별에 따라 나타나는지가 달라진다」 → <b>거짓</b>(우열 관계가 달라진다). ③ 「아버지가 색맹이면 아들도 색맹이다」 → <b>거짓</b>(아들은 아버지에게서 Y를 받는다). ④ 「남자는 X연관 유전자에 대해 동형접합이다」 → <b>거짓</b>(반접합이라 동형·이형을 따질 수 없다).</p>"""

ROWS = [
    ('첫째 열 위 — 긴 청록 막대에만 원이 있고, 짧은 막대에는 자리가 아예 없다',
     '<b>반성유전</b>은 <b>성염색체에 있는 유전자</b>의 유전이다. Y에는 짝이 되는 자리가 없다',
     ['E0-93']),

    ('★ 첫째 열 아래 줄만 칸이 셋이 아니라 둘이다',
     '남자는 <b>X가 하나뿐</b>이라 동형접합·이형접합을 따질 수 없다 — 이 상태를 '
     '<b>반접합(hemizygous)</b>이라 한다',
     ['E0-96']),

    ('그 둘째 칸 — 빈 원 하나뿐인데 그대로 안 나타남으로 간다',
     '열성 대립유전자를 하나만 받아도 <b>가릴 짝이 없어</b> 그대로 표현된다. 수컷이 X연관 열성 형질을 '
     '더 자주 보이는 직접적인 이유다',
     ['E0-95']),

    ('첫째 열 맨 아래 줄 — 사람 색맹·뒤셴·혈우병과 초파리 눈색',
     '사람의 대표적 <b>X연관 열성</b> 질환 셋은 <b>색맹·뒤셴 근위축증·혈우병</b>이고, '
     '반성유전의 대표 초파리 예는 <b>눈색</b>이다. 흰눈 대립유전자는 <b>X 염색체</b>에 있고 '
     '빨간눈에 대해 <b>열성</b>이라 그림에서도 빈 원이다',
     ['E0-94', 'E0-100', 'E0-112', 'E0-113']),

    ('둘째·셋째 열 위 — 청록이 아니라 회색 막대 한 쌍이다',
     '한성유전과 종성유전은 유전자가 <b>상염색체</b>에 있다. 성염색체가 아니다',
     ['E0-103']),

    ('★ 둘째 열 아래 줄 — 유전자형이 무엇이든 세 칸 모두 안 나타남이다',
     '<b>한성유전</b> — 상염색체 유전자인데 <b>한 성에서만</b> 표현된다. 유전자를 가지고 있어도 '
     '성이 잠근다',
     ['E0-101']),

    ('둘째 열 맨 아래 줄 — 닭 벼슬·수염·젖 분비',
     '한 성에서만 나타나는 형질들이 한성유전의 예다',
     ['E0-102']),

    ('★ 셋째 열 — 양 끝 칸은 위아래가 같은데 가운데 칸만 위아래가 다르다',
     '<b>종성유전</b> — 상염색체 유전자인데 <b>남녀에서 우열 관계가 달라진다</b>. '
     '동형접합은 남녀가 같고 <b>이형접합에서만</b> 갈린다',
     ['E0-104']),

    ('셋째 열 맨 아래 줄 — 대머리, 그리고 성호르몬 환경',
     '종성유전의 대표 예는 <b>대머리</b>다. 남녀에서 우열이 갈리는 까닭은 <b>성호르몬 환경</b>이 '
     '표현형 발현에 영향을 주기 때문이다',
     ['E0-105', 'E0-106']),

    ('아래 판정 줄 왼쪽 — ① 유전자가 어디 있나',
     '성염색체면 <b>반성</b>이다. <b>반성과 한성</b>의 가장 큰 차이가 이것이고, '
     '<b>반성과 종성</b>도 여기서 먼저 갈린다',
     ['E0-107', 'E0-108']),

    ('아래 판정 줄 오른쪽 — ② 상염색체면, 성별에 따라 무엇이 달라지나',
     '<b>나타나는지 여부</b>가 달라지면 한성, <b>우열 관계</b>가 뒤집히면 종성이다. '
     '둘의 공통점은 상염색체 유전자인데 성별에 따라 표현 양상이 달라진다는 것이고, '
     '이 두 물음이 셋을 가르는 <b>핵심 기준</b>이다',
     ['E0-109', 'E0-110']),

    ('맨 아래 왼쪽 상자 — 아버지의 X는 딸에게만',
     '아들은 아버지에게서 <b>Y</b>를 받는다. 그래서 남성의 X연관 대립유전자는 '
     '<b>아들에게 직접 전달되지 않고 딸에게</b> 간다',
     ['E0-97', 'E0-98', 'E0-121']),

    ('맨 아래 가운데 상자 — 어머니의 X는 아들·딸 모두에게',
     '여성의 X연관 대립유전자는 <b>아들과 딸 모두</b>에게 갈 수 있다. 아들은 X가 하나뿐이라 '
     '어머니에게서 받은 것이 그대로 나타나므로, 아들의 표현형은 <b>어머니의 유전자형</b>이 정한다',
     ['E0-99', 'E0-115', 'E0-120']),
]


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'d05p04'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    svg = open(SVGF, encoding='utf-8').read().strip()
    assert '`' not in svg and '\\' not in svg, 'SVG에 백틱·역슬래시'
    assert svg.startswith('<svg') and svg.endswith('</svg>')

    i = src.index("{id:'d05p03'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'d05p04',t:'반성·한성·종성',svg:`" + svg + "`,br:'" + BR + "',bx:`" + BX
             + "`,f:[" + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    k = out.index("{id:'d05p04'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('d05p04') == ids.index('d05p03') + 1, '위치 오류'
    cards = re.findall(r'"(E0-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복'
    print('패널 %d개 · d05p04 행 %d · 카드 %d장 · SVG %d바이트'
          % (len(ids), len(ROWS), len(cards), len(svg)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
