#!/usr/bin/env python3
"""d05p08 「비분리 — 감수 I이냐 II냐」 — d05 여덟 번째 도해.

★ 이 도해가 가르는 것 — 배우자 넷 중 정상을 세면 끝난다
      정상 감수분열   정상 4개
      감수 I 비분리   정상 0개   n+1 · n+1 · n-1 · n-1
      감수 II 비분리  정상 2개   n · n · n+1 · n-1

★ 그린 것만 건다 — 아래 CHECK가 각 행이 대는 문자열이 SVG 안에 실제로 있는지
  주입 전에 확인한다. 하나라도 없으면 주입을 멈춘다. (d05p07과 같은 형식)
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')
SVGF = os.path.join(ROOT, 'tools', '_d05p08.svg')

BR = ('★ <b>배우자 넷 중 정상을 먼저 센다</b> — <b>0이면 감수 I, 2면 감수 II</b>다. '
      '감수 I 비분리는 상동염색체 둘이 한쪽으로 몰려 <b>n+1 · n+1 · n−1 · n−1</b>이 되고 '
      '정상이 하나도 없다. 감수 II 비분리는 감수 I이 제대로 갈린 뒤에 생기므로 '
      '<b>n · n · n+1 · n−1</b>로 정상이 둘 남는다. 같은 n+1이라도 속이 '
      '<b>다른 상동염색체 둘</b>이면 I, <b>같은 자매염색분체 둘</b>이면 II다.')

BX = """<p><b>★ 외울 것이 아니라 세는 것이다.</b> 비분리 문제는 기작을 말로 외우면 반드시 헷갈린다. 그림의 맨 아래 상자 셋을 나란히 읽으면 <b>4 · 0 · 2</b>다. 문제에서 배우자 넷을 주면 <b>정상(n)이 몇 개인지부터 센다</b> — 하나도 없으면 <b>감수 I</b>, 둘이면 <b>감수 II</b>다. 이 한 줄이 이 단원 문항의 대부분을 그대로 푼다.</p><p><b>염색체는 동원체로 센다.</b> 그림에서 염색체 하나마다 작은 원이 하나씩 박혀 있다. 복제가 끝난 염색체는 막대가 <b>둘</b>로 보이지만 원은 <b>하나</b>이므로 염색체도 하나다. 이것을 놓치면 모세포의 염색체 수부터 틀린다. 세 칸의 모세포는 모두 2n = 2이고, 원이 둘이다.</p><p><b>감수 I 비분리 — 상동염색체 둘이 한쪽으로 몰린다.</b> 가운데 칸의 감수 I 뒤를 보면 왼쪽 세포에 <b>파랑과 주황이 함께</b> 들어가 있고 오른쪽 세포는 아무것도 받지 못했다. 그 뒤 감수 II는 정상으로 진행되므로 왼쪽에서 나온 배우자 둘은 <b>n+1</b>, 오른쪽에서 나온 둘은 <b>n−1</b>이 된다. 넷 어디에도 정상이 없다 — 애초에 정상적으로 하나씩 나눠 가진 세포가 없었기 때문이다.</p><p><b>감수 II 비분리 — 한쪽 세포에서만 자매염색분체가 안 갈린다.</b> 오른쪽 칸의 감수 I 뒤는 정상 칸과 똑같다. 문제는 그다음이다. 파랑 쪽 세포는 제대로 갈려 <b>n이 둘</b> 나오고, 주황 쪽 세포만 자매염색분체가 붙은 채 넘어가 <b>n+1과 n−1</b>이 하나씩 나온다. <b>정상 배우자가 둘 살아남는 것</b>이 감수 I과 갈리는 지점이고, 그 이유는 <b>감수 I이 제대로 끝났기 때문</b>이다.</p><p><b>★ 같은 n+1이라도 속이 다르다.</b> 아래 띠가 그것이다. 감수 I 비분리에서 나온 n+1은 <b>파랑 + 주황</b>, 곧 <b>서로 다른 상동염색체</b> 둘이 함께 들어간 것이다. 감수 II 비분리에서 나온 n+1은 <b>주황 + 주황</b>, 곧 <b>같은 염색체의 자매염색분체</b> 둘이다. 유전자 표지로 배우자 속을 들여다보는 문제는 이 차이를 묻는다 — 아버지형·어머니형 대립유전자가 <b>둘 다</b> 있으면 감수 I, <b>한쪽만 두 벌</b>이면 감수 II다.</p><p><b>그 배우자가 수정되면.</b> n+1이 정상 배우자와 만나면 <b>3염색체성(2n+1)</b>이다. 살아남는 경우가 있지만 <b>유전자 용량이 어긋나</b> 표현형 이상이 나타난다. n−1이 만나면 <b>1염색체성(2n−1)</b>인데, <b>필수 유전자 수가 모자라</b> 정상 발달이 어려워 대개 치명적이다. 많은 쪽보다 <b>적은 쪽이 더 위험하다</b>는 것이 이 두 상자의 색 차이다.</p><p><b>기출이 꼬는 자리.</b> ① 「비분리가 일어나면 정상 배우자는 나올 수 없다」 → <b>거짓</b>(감수 II면 둘 나온다). ② 「n+1 배우자가 나왔으니 감수 I 비분리다」 → <b>거짓</b>(둘 다 n+1을 만든다. 정상 배우자 수나 n+1의 속을 봐야 한다). ③ 「3염색체성은 유전자가 많으니 오히려 유리하다」 → <b>거짓</b>(용량 불균형이 문제다). ④ 「복제된 염색체는 분체가 둘이니 염색체도 둘이다」 → <b>거짓</b>(동원체가 하나면 염색체는 하나다).</p>"""

# (소품, 사실, 카드, 그림에 반드시 있어야 할 문자열)
ROWS = [
    ('범례 오른쪽 — 작은 원 하나가 염색체 하나다',
     '염색체는 <b>동원체 수</b>로 센다. 복제가 끝나 막대가 둘로 보여도 '
     '<b>원이 하나면 염색체는 하나</b>다. 세 칸의 모세포는 모두 원이 둘, 곧 2n = 2다',
     [], ['동원체 — 원 하나가 염색체 하나다(분체가 둘이어도)', '모세포 (2n = 2, 복제 끝)']),

    ('① 칸 맨 아래 청록 상자 — 정상 배우자 4개',
     '정상 감수분열이면 배우자 넷이 <b>모두 n</b>이다. 나머지 둘을 견줄 기준이 이 칸이다',
     [], ['① 정상 감수분열', '정상 배우자 4개']),

    ('★ ② 칸 배우자 넷 — n+1 · n+1 · n−1 · n−1이고 n이 하나도 없다 · '
     '아래 상자에 「정상 배우자 0개」',
     '<b>감수 I 비분리</b>의 수치 패턴이다. <b>정상 배우자가 하나도 없이</b> '
     'n+1 둘과 n−1 둘만 나온다 — 이 패턴만 보면 바로 감수 I이다',
     ['E0-136', 'E0-174'], ['② 감수 I 비분리', '정상 배우자 0개', 'n+1', 'n−1']),

    ('② 칸 감수 I 뒤 — 왼쪽 세포에 파랑과 주황이 함께 있고 오른쪽 세포는 「안 받았다」',
     '감수 I에서 <b>상동염색체 둘이 한쪽으로 몰렸다</b>. 정상적으로 하나씩 나눠 가진 세포가 '
     '애초에 없었으므로 뒤따르는 배우자 넷에 정상이 없다',
     [], ['안 받았다', 'I에서 상동염색체 둘이 한쪽으로 몰렸다']),

    ('★ ③ 칸 배우자 넷 — n · n · n+1 · n−1이고 아래 상자에 「정상 배우자 2개」',
     '<b>감수 II 비분리</b>면 정상 배우자가 <b>둘 있다</b>. '
     '감수 I이 제대로 갈린 뒤라 한쪽 세포는 멀쩡히 n을 둘 내놓기 때문이다',
     ['E0-175'], ['③ 감수 II 비분리', '정상 배우자 2개', 'I은 정상 · II에서 자매염색분체가 안 갈렸다']),

    ('★ 세 칸의 아래 상자를 나란히 읽으면 4 · 0 · 2다',
     '둘을 가르는 핵심은 <b>정상 배우자가 있느냐 없느냐</b> 하나뿐이다 — '
     '<b>0이면 감수 I, 2면 감수 II</b>. 기작을 외우지 말고 이것부터 센다',
     ['E0-173'], ['정상 배우자 4개', '정상 배우자 0개', '정상 배우자 2개',
                  '★ 문제를 보면 배우자 넷 중 정상을 먼저 센다 — 0이면 I, 2면 II다']),

    ('가운데 띠 — 같은 n+1인데 왼쪽은 「파랑 + 주황」, 오른쪽은 「주황 + 주황」이다',
     '★ 같은 n+1이라도 속이 <b>서로 다른 상동염색체 둘</b>이면 감수 I, '
     '<b>같은 염색체의 자매염색분체 둘</b>이면 감수 II다. 대립유전자 표지로 배우자 속을 '
     '들여다보는 문제가 묻는 것이 이 차이다',
     [], ['파랑 + 주황', '주황 + 주황', '감수 I 비분리의 n+1', '감수 II 비분리의 n+1']),

    ('맨 아래 왼쪽 노란 상자 — n+1 → 3염색체성(2n+1)',
     '살아남는 경우가 있어도 <b>유전자 용량 불균형</b>이 생겨 표현형 이상이 나타난다',
     ['E0-177'], ['n+1 → 3염색체성(2n+1)']),

    ('맨 아래 오른쪽 붉은 상자 — n−1 → 1염색체성(2n−1)',
     '<b>필수 유전자 수가 모자라</b> 정상 발달이 어려워 <b>대개 치명적</b>이다. '
     '많은 쪽보다 적은 쪽이 더 위험하다',
     ['E0-176'], ['n−1 → 1염색체성(2n−1)']),
]


def close_brace(t, i):
    op = t[i]
    cl = {'{': '}', '[': ']'}[op]
    d, q, j = 0, None, i
    while j < len(t):
        c = t[j]
        if q:
            if c == '\\':
                j += 2
                continue
            if c == q:
                q = None
        elif c in '"\'`':
            q = c
        elif c == op:
            d += 1
        elif c == cl:
            d -= 1
            if d == 0:
                return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'d05p08'" not in src, '이미 있다'
    assert '`' not in BX and "'" not in BR
    svg = open(SVGF, encoding='utf-8').read().strip()
    assert '`' not in svg and '\\' not in svg, 'SVG에 백틱·역슬래시'
    assert svg.startswith('<svg') and svg.endswith('</svg>')

    bad = []
    for prop, _f, _c, needs in ROWS:
        for n in needs:
            if n not in svg:
                bad.append('%s ← 「%s」' % (prop[:26], n))
    if bad:
        print('✗ 그림에 없는 것을 걸려고 한다:')
        for b in bad:
            print('   ' + b)
        return 1

    i = src.index("{id:'d05p07'")
    e = close_brace(src, i)

    rows = []
    for prop, fact, cards, _n in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards:
            r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')

    panel = ("{id:'d05p08',t:'비분리 — 감수 I이냐 II냐',svg:`" + svg + "`,br:'" + BR
             + "',bx:`" + BX + "`,f:[" + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    k = out.index("{id:'d05p08'")
    blk = out[k:close_brace(out, k) + 1]
    ids = re.findall(r"\{id:'([sd]\d+p\d+[ab]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    assert ids.index('d05p08') == ids.index('d05p07') + 1, '위치 오류'
    cards = re.findall(r'"(E0-[\w#]+)"', blk)
    assert len(cards) == len(set(cards)), '카드 중복'
    print('패널 %d개 · d05p08 행 %d · 카드 %d장 · SVG %d바이트'
          % (len(ids), len(ROWS), len(cards), len(svg)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
