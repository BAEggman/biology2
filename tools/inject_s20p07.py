#!/usr/bin/env python3
"""s20p07 「두 콩팥이 벌리는 틈」 — 기공의 여닫음 (s20p06 뒤에 붙인다).

★ 기공이 덱에 통째로 없었다
  「기공」「공변세포」「증산」으로 훑어도 0건이었는데, 관련 미연결 카드는 여덟 장이 넘었다.
  광합성(광호흡)·수송(증산)·식물의 반응(청색광)이 다 여기서 만난다.

★ 그런데 닫는 쪽은 이미 그려져 있었다
  s20p01b(멈추게 하는 셋)의 「잎의 셔터를 끌어내려 거의 닫음」이 ABA의 기공 폐쇄다.
  그래서 이 판은 **여는 쪽**과 **닫히면 잎 속이 어떻게 되는가**를 맡는다.

★ 문법 — 같은 두 사람이 부풀었나 홀쭉한가
  콩팥 모양 둘이 **빵빵하면 서로 밀어내 틈이 벌어지고**, **홀쭉하면 붙어 닫힌다**.
  팽압이 곧 문의 여닫음이다.

★ 방울 두 빛깔이 셈을 나른다
  어두운 방울 = CO₂ · 밝은 방울 = O₂.
  열리면 어두운 것이 들어가고 밝은 것이 나온다.
  닫히면 오갈 수 없어 **어두운 것은 바닥나고 밝은 것만 가득 찬다** — 그것이 광호흡의 조건이다.
"""
import os, re, sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SK = os.path.join(ROOT, 'sketchy.html')

BR = ('★ 문을 여는 것은 <b>팽압</b>이다 — 콩팥 모양 둘이 <b>빵빵해지면 서로 밀어내</b> 틈이 벌어지고, '
      '<b>홀쭉해지면</b> 붙어 닫힌다. 여는 신호는 <b>푸른 빛 · 시계 · 잎 속에 모자란 어두운 방울</b> 셋이고, '
      '닫는 신호는 <b>마른 바람</b>이다. ★ 닫히면 어두운 방울이 바닥나고 밝은 것만 남는다.')

BX = """<p><b>기공은 한 쌍이 만드는 틈이다.</b> 문짝이 따로 있는 것이 아니라, <b>공변세포</b> 두 개가 마주 보고 있다가 <b>부풀면 벌어지고 쭈그러들면 붙는다</b>. 그래서 여닫음은 곧 <b>팽압</b>의 문제다 — 물이 들어오면 열리고 나가면 닫힌다. 콩팥 모양인 것도 까닭이 있다: 안쪽 벽이 두꺼워 부풀 때 바깥쪽만 늘어나므로 활처럼 휘어 틈이 생긴다.</p><p><b>여는 신호 셋.</b> ① <b>청색광</b> — 아침 햇빛이 신호다(그래서 파란 렌즈가 달려 있다). ② <b>잎 속 CO₂가 낮을 때</b> — 재료가 모자라니 문을 연다. ③ <b>생체시계</b> — 신호가 없어도 낮이 되면 저절로 연다. 셋 다 「<b>지금 광합성을 해야 한다</b>」는 뜻이다.</p><p><b>닫는 신호 하나.</b> <b>건조 스트레스</b>다. 마르면 <b>ABA</b>가 나와 공변세포에서 물을 빼내 쭈그러뜨린다. 그 셔터를 내리는 그림은 옆 판(멈추게 하는 셋)에 있다. ★ 여는 쪽은 셋인데 닫는 쪽은 하나다 — 물이 걸리면 다른 것은 다 뒤로 밀린다.</p><p><b>★ 닫히면 잎 속이 뒤집힌다.</b> 문이 막혀도 광합성은 계속 돌아간다. 그러면 <b>CO₂는 계속 쓰이니 줄고</b>, <b>O₂는 계속 나오니 쌓인다</b>. 그림에서 오른쪽 방이 밝은 방울로만 가득한 것이 그것이다. ★ 그리고 이 상태가 정확히 <b>광호흡</b>이 심해지는 조건이다 — 루비스코가 모자란 CO₂ 대신 넘치는 O₂를 붙잡기 때문이다. 「덥고 건조 → 기공 폐쇄 → CO₂↓ O₂↑ → 광호흡↑」이 한 줄로 이어진다.</p><p><b>그래서 식물은 늘 저울질한다.</b> 문을 열면 CO₂를 얻지만 물을 잃고, 닫으면 물을 지키지만 굶는다. C4와 CAM 식물이 각기 다른 방식으로 이 저울질을 피해 가는데, 그 이야기는 광합성 편에 있다.</p><p><b>기출이 꼬는 자리.</b> ① 「기공이 닫히면 잎 속 O₂도 줄어든다」 → <b>거짓</b>. 상대적으로 <b>늘어난다</b>. ② 「기공은 CO₂만 드나드는 통로다」 → <b>거짓</b>. O₂와 <b>수증기</b>도 같은 문으로 나간다. ③ 「어두우면 언제나 닫힌다」 → 대체로 맞지만, <b>생체시계</b>가 있어 신호 없이도 리듬을 탄다. ④ 「ABA가 기공을 연다」 → <b>거짓</b>. 닫는다.</p>"""

ROWS = [
    ('★ 왼쪽 — 콩팥 모양 둘이 <b>빵빵하게 부풀어</b> 서로 밀어내 가운데 틈이 <b>활짝 벌어졌다</b>',
     '<b>기공</b>은 문짝이 아니라 <b>공변세포 한 쌍</b>이 만드는 틈이다 — '
     '<b>부풀면 열리고 쭈그러들면 닫힌다</b>. 여닫음은 곧 <b>팽압</b>의 문제다',
     ['M0-6']),

    ('벌어진 틈으로 <b>어두운 방울</b>이 <b>안으로</b>, <b>밝은 방울</b>이 <b>밖으로</b> 오간다',
     '<b>CO₂를 들이고 O₂를 내보내는 통로</b>다 — 광합성의 재료와 산물이 같은 문으로 드나든다. '
     '수증기도 이 문으로 나간다(증산)', ['C0-23']),

    ('★ 왼쪽에만 있는 것 셋 — 위에서 내려오는 <b>푸른 빛</b>과 그것을 받는 <b>파란 렌즈</b> · '
     '벽에 걸린 <b>작은 시계</b> · 방 안에 <b>어두운 방울이 드물다</b>',
     '<b>여는 신호가 셋</b>이다 — <b>청색광</b>(아침 햇빛) · <b>생체시계</b>(신호 없이도 낮이면 연다) · '
     '<b>잎 속 CO₂가 낮을 때</b>(재료가 모자라니 연다). 셋 다 「지금 광합성을 해야 한다」는 뜻이다',
     ['O1-22#1']),

    ('★ 오른쪽 — 같은 둘이 <b>홀쭉해져</b> 틈이 <b>딱 붙었다</b> · '
     '위에는 <b>갈라진 땅과 마른 바람</b>뿐이다',
     '<b>닫는 신호는 하나</b> — <b>건조 스트레스</b>다. 마르면 <b>ABA</b>가 공변세포의 물을 빼내 '
     '쭈그러뜨린다. 셔터를 끌어내리는 그림은 옆 판(멈추게 하는 셋)에 있다',
     ['O1-22']),

    ('★ 오른쪽 방 안 — <b>어두운 방울이 거의 없고</b> 밝은 방울만 가득하다',
     '문이 막혀도 <b>광합성은 계속 돈다</b>. 그러니 <b>CO₂는 쓰여 줄고</b> — '
     '들어올 길이 없으니 바닥난다', ['C0-190']),

    ('★ 같은 방이 <b>밝은 방울로만</b> 빽빽하다',
     '<b>O₂는 계속 나오는데 빠질 길이 없어 상대적으로 늘어난다</b>. '
     '★ 줄어드는 것과 늘어나는 것이 <b>한 방에 같이</b> 있다', ['C0-191']),

    ('★ 어두운 것은 바닥나고 밝은 것만 남은 그 방이 곧 답이다',
     '<b>광호흡</b>이 심해지는 조건이 정확히 이것이다 — <b>덥고 건조</b>해 기공이 닫히고 '
     '<b>CO₂가 부족</b>할 때. 루비스코가 모자란 CO₂ 대신 넘치는 O₂를 붙잡기 때문이다',
     ['C0-189']),

    ('★ 여는 쪽은 <b>셋</b>인데 닫는 쪽은 <b>하나</b>다',
     '물이 걸리면 나머지는 다 뒤로 밀린다 — 문을 열면 CO₂를 얻지만 <b>물을 잃고</b>, '
     '닫으면 물을 지키지만 <b>굶는다</b>. 식물은 늘 이 저울질을 한다', []),
]


def close_brace(t, i):
    op = t[i]; cl = {'{': '}', '[': ']'}[op]
    d, q, j, esc = 0, None, i, False
    while j < len(t):
        c = t[j]
        if q:
            if esc: esc = False
            elif c == '\\': esc = True
            elif c == q: q = None
        elif c in '"\'`': q = c
        elif c == op: d += 1
        elif c == cl:
            d -= 1
            if d == 0: return j
        j += 1
    raise SystemExit('괄호 안 닫힘')


def q2(s):
    assert '"' not in s and '\\' not in s, s
    return '"%s"' % s


def main():
    src = open(SK, encoding='utf-8').read()
    assert "{id:'s20p07'" not in src, '이미 있다'
    assert os.path.exists(os.path.join(ROOT, 'img', 's20p07.webp')), '그림이 없다'

    i = src.index("{id:'s20p06'")
    e = close_brace(src, i)
    rows = []
    for prop, fact, cards in ROWS:
        r = '[' + q2(prop) + ',' + q2(fact)
        if cards: r += ',[' + ','.join(q2(c) for c in cards) + ']'
        rows.append(r + ']')
    panel = ("{id:'s20p07',t:'두 콩팥이 벌리는 틈',br:'" + BR + "',bx:`" + BX + "`,f:["
             + ',\n     '.join(rows) + ']}')
    out = src[:e + 1] + ',\n   ' + panel + src[e + 1:]

    ids = re.findall(r"\{id:'([sd]\d+p\d+[a-z]?)'", out)
    assert len(ids) == len(set(ids)), '패널 id 중복'
    b2 = out[out.index("{id:'s20p07'"):]
    b2 = b2[:close_brace(b2, 0) + 1]
    cs = re.findall(r'"([A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*-[\w#]+)"', b2)
    assert len(cs) == len(set(cs)), '카드 중복: ' + str(cs)
    print('s20p07 신설 · 행 %d · 카드 %d장' % (len(ROWS), len(cs)))
    open(SK, 'w', encoding='utf-8').write(out)
    return 0


if __name__ == '__main__':
    sys.exit(main())
